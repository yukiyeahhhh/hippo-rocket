# HANDOFF — hippo-rocket 引き継ぎ

> ## ★Codex追記（2026-06-26 #4対応・GARAGE/DESTINATION分割＋背景v2採用）
> **実装済み・未push。** 旧SELECTを実ゲーム上で **GARAGE（機体選び）/ DESTINATION（行き先選び）** に分割した。`?screen=SELECT`は互換でGARAGEに読み替え、確認用は `?screen=GARAGE` / `?screen=DESTINATION`。
> - **DESTINATION未合意論点の採用方針**：選んだ機体の小表示＋推奨機体バッジは出す／DESTINATIONにコインは出さない／順序は `TITLE → GARAGE → DESTINATION → PLAY`。
> - **DESTINATION背景を生成**：最初の`destination_bg_v1.png`は細密ファンタジー寄りで、コビトカバ/鳥スプライトと絵柄差が強いため不採用・repoから除外。採用版は **`assets/ui/destination_bg_v2.png`**（丸い玩具レンダー寄せ、空に浮かぶ島/領域）。プレビューは **`assets/ui/_destination_preview.html`**。
> - **実装内容**：`index.html`に`#t-destination` DOM画面を追加。`#t-select`はGARAGEとして背景=`select_bg_v1.png`、ステージ選択を非表示にし「行き先をえらぶ」CTAへ変更。RESULTの「つぎへ」は次ステージを即開始せずDESTINATIONへ戻す。SETTINGSは開いた元画面へ戻るよう`settingsBack`を追加。初回DOM表示がバックグラウンド検証で出ない問題を避けるため、起動時に`syncDOMUI()`を一度即実行。
> - **ドキュメント更新**：`画面遷移.md`のDESTINATION要検討を採用方針へ更新済み。
> - **検証**：`node -e`構文OK / `node tools/smoke.cjs` OK（GARAGE/DESTINATION追加） / `node tools/validate.cjs` OK。in-app browserで`?screen=GARAGE`/`?screen=DESTINATION`を目視し、DOM表示・ロック状態・v2背景参照を確認。
> - **次の一手**：実機で画面遷移を触って、DESTINATIONカードの密度・ロック時の可読性・背景とUIの馴染みを微調整。余力があればSETTINGS/RESULTもDOM土台へ移行し、旧canvas版`drawSelect`/`drawShop`を整理。
> - **Codex追加微調整**：ユーザーFB「ガレージのカバが上すぎる」「タイトルのカバ上昇が中途半端」を反映。GARAGEの機体表示をデッキに乗る位置へ下げ、機体名/説明は白＋影で可読化。TITLE発射アニメは画面外へ抜ける距離と尺を増やし、GARAGE遷移開始を後ろへずらした。スマホ実機確認用に `tools/serve_lan.cjs` を追加（例：`node tools/serve_lan.cjs 8124`）。このPCのWi-Fi IPは確認時点で `192.168.10.119`、URL候補は `http://192.168.10.119:8124/`。PC内 `127.0.0.1:8124` はOK、LAN IP直打ちは接続不可だったため、スマホで開けない場合はWindows FirewallでNode.jsのプライベートネットワーク受信許可が必要。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #4・UI基盤の作り直し＝シーン背景/画面分割/フロー。上限直前バトン）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。Claude分は全コミット済み。
> ### この回(Claude)でやったこと＝UIの“上流”を作り直した（重要な方針転換）
> - **TITLE/SHOP/SELECTをDOM/CSS化済み**（前パス）。だが「ボタンがチープ/色がケンカ/急にオレンジ/変なグラデ」とFB。診断→**根本は“上流の設計工程”を飛ばしていたこと**だった。以下を順に確立：
> - **① 色テーマをKVから抽出**：`title_kv_v5.png`の実色を採取＝空青`#3A7AD4`/ピーチ`#F4B07A`/**ロケットの赤錆coral`#E85D43`(=主アクセント)**/cream`#FFF4E2`/ink`#2A3550`/plum`#5B4167`/gold`#FFCB3E`。「急にオレンジ」は不採用、**主アクション色=coral(ロケット色)**に。1画面=主アクセント1つ。根拠＝`assets/ui/_theme.html`。
> - **② “背景はグラデでなくシーン”に転換**（ユーザー指摘「ガレージならガレージらしい背景」）。**Codex画像生成で雲上ガレージ背景を生成→`assets/ui/select_bg_v1.png`**。UI合成プレビュー＝`assets/ui/_select_scene.html`（背景＋カバ＋coralボタン＝製品級になった）。**今後の背景は各画面のシーンを生成する方針**。
> - **③ 画面フロー(遷移図)を作成**（ユーザー指摘「遷移図描いてなかったから概念ズレた」）。`画面遷移.md`＋`assets/ui/_flow.html`。**旧SELECT(機体＋行き先)を GARAGE(機体だけ) と DESTINATION(行き先だけ) に分割**＝1画面1決定1場所。戻り(back)動線も明記。
> - **④ 画面ごとの要件 認識合わせ**：`画面遷移.md`「画面ごとの要件」に各画面の必須要素/載せないもの/見せ方を記述。**DESTINATIONは要合意の論点あり**（下記）。
> - 方法論はknowledgeに昇格：[[画面フローを先に描く]]（工程0＋画面ごと要件合わせ）/[[UIの構図は先人の型から起こす]]。**工程＝①フロー→②画面ごと要件→③構図(先人の型)→④DESIGN.mdトークン→⑤シーン生成・実装**。
> ### ★次の一手（この順で。まずDESTINATIONの認識合わせから）
> 1. **DESTINATIONの未合意論点をユーザーに確認**（`画面遷移.md`記載）：①選んだ機体の小表示＋推奨機体バッジを出すか ②コイン表示要否 ③GARAGE→DESTINATIONの順 vs 逆順。**行き先は「空に浮かぶ島/領域」として空間的に見せる**のが要件（リスト羅列にしない）。
> 2. **DESTINATIONのシーン背景を生成**（雲に開けた発射デッキ／空に行き先の島。画風＝KV/garageと統一。`-i assets/ui/title_kv_v5.png assets/ui/select_bg_v1.png`。生成→`assets/ui/`へcp→`_preview`を開く）。
> 3. **実ゲームに組込**：`scene`の`SELECT`を **`GARAGE`/`DESTINATION`に分割**。GARAGE＝機体カルーセル＋ステータス＋ショップ＋「行き先をえらぶ」、背景=`select_bg_v1.png`。DESTINATION＝行き先選択＋「これで出発」、背景=新規。**coralパレットを全DOM画面に適用**（`_theme.html`の役割）、変なCSSグラデは廃しシーン背景に。**戻り動線**は`画面遷移.md`通り。
> 4. **ボタン等の画像化**（ユーザー希望＝フル画像UI）：主役CTA/看板など“ご褒美”が要る所からKV画風で生成。小物(chip/coin/gear/pip)はCSS+coralで可。
> 5. 旧canvas`drawSelect`/`drawShop`は保険で残置中。全DOM化が固まったら整理。
> ### 検証・運用
> - `node tools/smoke.cjs`/`node tools/validate.cjs`、`?screen=...&debug`/`&reset`で目視。`.shots/`・`.chrome-*-profile`はgitignore済。**画像生成したら必ず`_preview.html`をStart-Processで開く**。コミットするがpushしない。
> ### 根拠ファイル一覧（消さない）
> - `画面遷移.md`(フロー＋画面ごと要件＝正本) / `assets/ui/_flow.html`(遷移図) / `assets/ui/_theme.html`(KV由来パレット) / `assets/ui/select_bg_v1.png`(ガレージ背景) / `assets/ui/_select_scene.html`(背景+UI合成) / `_shop_*.html`(SHOP構図根拠) / `_ui_polish.html`(チープ要因の診断)。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #3・SHOPをV3で確定→Claudeが実装まで完了）
> **担当はまだClaude（上限未達のため継続）。** push しない。`AGENTS.md`「双方向バトンパス」準拠。
> ### この回(Claude)でやったこと＝SHOPを参照ドリブンで設計→DOM実装まで
> - **参照ドリブン設計**（先人の型を実例深掘り→定石抽出→安いHTMLモックで構図確定）を実施。新ナレッジ `knowledge/UIの構図は先人の型から起こす.md` に工程を明文化。
> - **SHOP構図＝V3「下隅の案内役」型に確定**。根拠モック＝`assets/ui/_shop_mock.html`(A〜D)／`_shop_a_refined.html`(A定石反映)／`_shop_placement.html`(店主の置き方V1〜V3)。**`DESIGN.md`のShop節がV3仕様の正本**。不採用＝上ヒーロー型（「店主が大きすぎて商品が探しにくい」）。
> - **✅ V3を`index.html`にDOM/CSSで実装済み**（タイトルと同じ`#ui`オーバーレイ機構）：
>    - 新規 `#t-shop`（CSS＋HTML）。**店主＝左下に半見切れの固定オーバーレイ**(`shop_merchant_sunglasses.png`)＋吹き出し。**商品棚＝全幅スクロール**を店主の上に。**ヘッダー(看板/通貨右上/もどる左上)・店主は固定、スクロールは棚だけ**。空グラデ＋ひさし/看板/棚はDOM/CSS、商品＝機体スプライト＝**重いAI生成ゼロ**。
>    - JS：`syncDOMUI`が`scene==='SHOP'`で`#t-shop`を表示し`renderShop()`。`renderShop`は`VEH_META.buy`の機体だけカード化（絵→名前→一言→価格→購入の固定順、購入ボタンだけアクセント色）、所持=「そうび中/所持ずみ」、資金不足=グレー化。購入は`buyVeh`→即再描画。**スクロールが要る時だけ**「▾もっと見る」/フェード表示。canvasの旧`drawShop`は`domShown==='shop'`の時は描かない（`?screen`等の保険で残置）。
>    - 検証：`node tools/smoke.cjs` OK、ヘッドレスChromeで`?screen=SHOP&coins=120/200`を目視（`.shots/shop_v3*.png`＝gitignore）。購入可/不可の出し分け・店主固定・棚スクロールを確認。
> ### ✅ 追記：SELECTもDOM化完了（雲上ガレージ＝カルーセル型）
> - **`#t-select`をDOM/CSSで実装**（同じ`#ui`オーバーレイ）：ヘッダー(ギア/タイトル/コイン右上)＋**機体カルーセル**(中央大＋左右プレビュー＋‹›、所持1つの時は‹›と side を自動非表示)＋名前/説明/**ステータスバー(加速/軽さ/旋回の5pip)**＋**ステージ選択**(解放分のみ・★/ベスト表示・選択中ハイライト)＋フッター(出発する→`go(launch)`／ショップ→SHOP)。
> - JS：`renderSelect()`が`save`から全部組む。`selPick(±1)`で機体送り。canvasの旧`drawSelect`は`domShown==='select'`の時は描かない。`smoke.cjs`/`validate.cjs` OK、`?screen=SELECT&debug`/`&reset`で複数所持/単一所持の両方を目視確認。
> ### ★次の一手（仕上げ＋残り画面）
> 1. **SETTINGS / 結果画面も同じDOM土台へ**（最後のcanvasメニュー。`knowledge/UIの構図は先人の型から起こす.md`の工程で）。
> 2. **微調整・質感UP**（任意・実機）：SHOP店主の大きさ/位置、SELECTカルーセルの送り演出、ボタン/看板の画像素材化（DESIGN.md「Shared UI Parts」）。
> 3. 旧canvas版`drawSelect`/`drawShop`は`?screen`等の保険で残置中。全DOM化が固まったら整理してよい。
> ### 前回Codexの未コミット在庫の扱い（整理済み）
> - `shop_merchant*` / `ui_btn_*` スプライトは**今回のコミットに取り込み済み**（index.htmlのpreload/SHOPが参照するため）。`ui_btn_*`画像は現状の実装では未使用（SHOPのボタンはCSS製）だが、SELECT/結果のボタン画像化で使える在庫として残す。`assets/states/*`は各PNGのcutout元（再生成用）。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #2・タイトル画面のデザイン刷新パス）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。全部コミット済み・クリーン。
> ### この回でやったこと（タイトル画面を“1枚のデザイン”として作り直し・全コミット済み）
> - **メニューUIをDOM/CSS化**（canvasと同じ仮想解像度432×768に`transform:scale`で重ねる`#ui`オーバーレイ＝同期はfit()の1行。プレイ画面はcanvasのまま）。**タッチで進めない不具合も修正**（DOMメニュー表示中=domShownはtouchのpreventDefaultをスキップ＝ボタンのclickを潰さない）。
> - **タイトルのキービジュアル(KV)を反復生成して確定**。最終＝`assets/ui/title_kv_v5.png`（背中ロケットで自力飛行・暖色の雲→星空へ上昇・健気な決意の表情・一貫ポップ画風）。**Codex画像生成をClaudeが駆動**。Geminiは却下。
> - **★KV=完成像 → “画風一致の動かせる素材”に分解して実装**（ユーザー方針）：①`title_bg_v5.png`=カバ抜き背景プレート ②`title_hippo_green.png`=背中ロケットで飛ぶカバを同画風+クロマキー緑で生成→`tools/cutout.cjs`で透過。DOMタイトル=背景プレート＋透過カバ（常時ふわふわ`hippoFly`＋「出発する」タップで上にビュン`hippoLaunch`→go()でSELECT＋バイブ）＋`twinkle`。**古いゲームスプライト/CSS層(hero/鳥/雲)は全部非表示=素材流用ゼロ・全部v5画風で整合**。`?screen=TITLE`不要、デフォルトでTITLE表示。
> - **art-bibleに設計の芯を明文化**（重要・厳守）：(a)**キャラの芯=「決意に満ちて応援したくなる健気なunderdog」。こぶし突き上げ/勝ち誇りは解釈違い**。(b)**飛び方の正=背中ジェットパックで自力飛行。ロケットに乗り込む/座るは概念ミス**。(c)**行き先=空の上/星・宇宙(星脈)。山・頂・陸は描かない**（「登山/頂上」は比喩）。(d)**KVは一貫ポップ画風で完結。半写実×トイの混在禁止。ゲーム素材を流用せずKV画風で新規素材を作る**。
> - **画像レビュー運用を徹底**：生成のたびに`assets/ui/_title_preview.html`を更新して`Start-Process`で開く（人がフォルダを掘らないで済むよう）。**Codexも生成したら必ずプレビューを開くこと**（HANDOFF素材パイプライン参照）。
> ### ★次の一手（タイトル仕上げ→他画面へ）
> 1. ✅ **ロゴのデザイン**：Codexで文字込み生成の`assets/ui/title_logo_v2.png`を採用候補として実装。既定の白文字/CSSフォント合成は撤回し、v5画風の雲プレート＋星＋ロケット噴射＋手描き風「カバロケット」ロゴに置換。生成元は`title_logo_v2_green.png`、旧CSS合成用ベースは`title_logo_base*.png`として比較用に保持。
> 2. **微調整**：透過カバの位置/サイズ(`.titlehippo` width/top)、CTA質感、きらめき量、背景の`object-position`。
> 3. **同じKV方式を他画面へ**：SELECT/SHOP/SETTINGS/結果も「KV(背景)＋画風一致の動かせる素材＋DOM UI」で“魂”を揃える。
> ### 開いた宿題（前パスから継続）
> - **画像素材の質**（敵/雲/UIを同じライティング/輪郭/影で一括更新）。**boost/hit差分スプライト**（fall同様の方式）。**落下fall.bodyHの実機微調整**（`?cap&veh=N&pose=fall`）。詳細は下の「気になってることリスト」#1-14。
> - **「初期は何で作るか」のベスプラをknowledge化**（UI=最初からDOM/CSS）。**中身設計の発散→収束**はknowledge追記済。
> ### 2026-06-26 Codex追記：タイトルロゴv2＋カバ位置調整
> - ✅ **文字込みロゴを生成・実装**：`assets/ui/title_logo_v2.png`を追加し、TITLE DOMは`<img class="logo-img">`で表示。日本語フォントの既製感が強かったため、ロゴ文字も画像側で作った。`assets/ui/_title_preview.html`も更新し、`Start-Process`でプレビューを開いた。
> - ✅ **カバ位置を下げた**：`.titlehippo`を`top:66% / width:68%`へ調整。上部ロゴの領域とカバの決意表情がぶつかりすぎない構図にした。
> - ✅ **検証**：`node -e`構文OK、`node tools/smoke.cjs` OK、ロゴPNGの四隅alpha=0確認OK。敵/機体/ステージは触っていないので`validate.cjs`は未実行。Chromeヘッドレスで`.shots/title_logo_v2.png`を撮影して目視確認（スクショ/Chromeプロファイルは一時ファイル）。
> - **次の一手**：タイトル内の微調整（CTAの位置/質感、ロゴの最終サイズ、カバの上下）を実機で見て詰める。良ければ同じKV方式をSELECT/SHOP/SETTINGS/結果へ展開。
> - **2026-06-26 Codex追加調整**：ユーザーFB「カバがまだ上すぎる」「ボタン/UIパーツが質素で統一感がない」を反映。`.titlehippo`を`top:66% / width:68%`へさらに下げ、CTA/設定ボタンをロゴと同じクリーム縁＋青リム＋オレンジ立体の質感へ寄せた。`node -e`構文OK、`node tools/smoke.cjs` OK、Chromeヘッドレスで`.shots/title_ui_polish.png`を目視確認（スクショ/Chromeプロファイルは一時ファイル）。
> - **2026-06-26 Codex追記：UI全体の画面構想をDESIGN.mdへ追加**：ユーザーFB「ボタン自体も画像にした方がよい/SELECTやSHOPも磨きたい/SHOPは商人コビトカバ案」を受けて、`DESIGN.md`に`Screen Direction`を追加。方針は①大ボタン/購入ボタン/戻るボタンを画像ベース+DOM文字へ ②SELECT=雲上ガレージ/発射デッキ ③SHOP=商人コビトカバの雲上屋台＋商品棚 ④SETTINGS/RESULTも共通素材へ。外部参考では、NPC/店構えが上、商品カードが下、通貨右上、購入ボタン商品ごとの型が多かったため採用候補にした。次はこの順で実装。
> - **2026-06-26 Codex追記：SELECT/SHOP磨き込み途中**：共通UI素材を生成・追加（`ui_btn_orange.png`=無地ボタン、`ui_btn_start.png`=文字込み「出発する」、`ui_btn_shop.png`=文字込み「ショップ」、`ui_btn_back.png`=文字込み「もどる」）。SHOP商人はユーザーFBでサングラス版`shop_merchant_sunglasses.png`を生成し、SHOP上部に雲上屋台＋商人＋商品棚を実装。SELECTは雲上ガレージ見出し/明るい展示台/文字込み「出発する」「ショップ」ボタンへ寄せた。文言は「出撃」ではなく「出発」に統一。`node -e`構文OK、`node tools/smoke.cjs` OK、PNG四隅alpha=0確認OK。ChromeスクショはSELECT/SHOP途中確認済みだが、追加撮影と一時ファイル削除/コミット操作は承認側の利用上限で止まる可能性あり。
>
> ## ★Claude→Codex 申し送り（2026-06-26・上限直前のバトン）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。全部コミット済み・クリーン。
> ### この回(Claude)でやったこと（全コミット済み）
> - 見た目FB一次対応の取込／**ゴール決勝バナー化**／**敵スプライトに接地影**／**雲の入場フェード**＋**雲海の即時描画**／**画面遷移トランジション(go())**／**ふうせん経済の作り直し(coinVal=拾得時ライブ適用)**／**こつぶ廃止**。
> - **各装備の落下パニック・スプライト**(つばさ/ふうせん/コメット/マグ)をCodex画像生成→cutout→drawHippoが状態別スプライト自動使用。**落下時に体が激変する不具合を修正**(状態別`VEH_ART[].fall.bodyH`で体高正規化)。※**fall.bodyHの最終微調整はCodex任せ**＝中間値で仮置き。`?cap&veh=N&pose=fall`で確認。小さいほど体が大きい。
> - **メニューUIのDOM/CSS化を開始**＝canvasと同じ仮想解像度に`transform:scale`で重ねる`#ui`オーバーレイ(同期はfit()の1行)＋**タイトル画面をHTML/CSSで試作**。プレイ画面はcanvasのまま。`scene==='TITLE'`でDOM表示・タップ→SELECT/ギア→SETTINGS・DOM表示中はcanvas入力停止。
> ### ★次の大仕事＝「タイトル/UIに“デザインの魂(こだわり)”を入れる」(ユーザー指摘・最優先)
> 今のDOMタイトルは**構造の勝ち(DOM路線の実証)だが、デザイン哲学が無いスキャフォールド**。ユーザー批評そのまま：
> - 「**この画面でワクワクするのか？**」＝**狙う感情/コンセプトが無い**。まず「タイトルで何を感じさせるか」を1文で決める(例：ずんぐりカバが空へ挑む“コミカル×温かい×上を目指す高揚”)。それを構図・動き・色で実装する。
> - 「**後ろの雲が同じものの連続**」＝**手抜きに見える**。タイル反復をやめ、雲は形・大きさ・速度・ボケを変えた複数層＋遠景に“頂上(ゴール)”や山影、鳥/きらめき等で**奥行きと物語**を出す。
> - 「**タイトルにコイン情報いる？**」＝**情報設計の精査**。タイトル＝ブランド＋開始に絞る。コイン/所持は SELECT/SHOP の領分。**コインピルは外す**方向。ギアも控えめに。
> - 「**タイトルの出方これでいい？**」＝**登場演出**(ロゴがバウンドで降る/カバが発射台にトンと乗る/CTAが遅れて出る等の入場アニメ)。
> - 「**配色は？**」＝**意図あるパレット**を決める(例：朝焼け＝新たな冒険、空青＋暖色アクセント＋カバのプラム色)。**トークン化**して全UIで統一＝knowledge [[DESIGN.mdでUIのテイストを固定する]]に沿って**`DESIGN.md`(YAMLトークン＋意図の散文)を作る**→以後の全画面(SELECT/SHOP/SETTINGS/結果)もそれで起こす。
> - 「**デザイナーの魂が無い(こだわりが無い)**」＝**既定値の寄せ集めを脱する**。frontend-designの観点(明確な階層/1アクセント/意図的な余白/テンプレに見えない選択)で、**hero(カバ)を主役に・UPの引力・ゴールの示唆**を効かせる。カバは座らせるより**噴射で昇る/上を見上げる**等“動き”を。
> ### Codexの進め方(おすすめ順)
> 1. **`DESIGN.md`を作る**(コンセプト1文＋ムード＋カラートークン＋タイポ＋余白/角丸/影の基準)。knowledge [[DESIGN.mdでUIのテイストを固定する]]の雛形。
> 2. それで**タイトルを作り直す**(コンセプト→構図→非反復の奥行き背景→入場演出→情報を開始に絞る)。背景キービジュアルは必要ならCodex画像生成(1枚絵)も可。
> 3. 良ければ **SELECT/SHOP/SETTINGS/結果も同じDOM土台＋DESIGN.mdで順次DOM化**(座標/hotspotはcanvas版を参考に。プレイ画面はcanvasのまま)。
> ### 2026-06-26 Codex追記：タイトル/UIデザイン土台を実装
> - ✅ **`DESIGN.md`追加**：コンセプト1文「ずんぐりしたコビトカバが、朝焼けの空へ本気で挑むコミカルで温かい上昇感。」を正本化。朝焼け空/プラム/オレンジCTA、M PLUS Rounded 1c、余白・影・角丸・タイトル情報設計をトークン化。
> - ✅ **DOMタイトルを作り直し**：`index.html`のTITLE DOM/CSSを差し替え。横反復の雲を廃止し、個別配置の雲5層＋遠景の山/ゴール＋きらめきで奥行きを作成。ロゴ落下、カバ/発射台の着地、CTA遅延出現の入場演出を追加。
> - ✅ **情報設計を開始に絞る**：タイトル右上のコインピルを撤去。表示はブランド、短い気持ちの一文、開始ボタン、控えめな設定アイコンのみ。コイン/所持は従来通りSELECT/SHOP側の領分。
> - ✅ **検証**：`node -e`構文OK、`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。ヘッドレスChromeで432x768/1280x760のTITLEスクショを目視し、文字はみ出し・旧UI露出なし。
> - **次の一手**：このDESIGN.mdを読ませながら、SELECT/SHOP/SETTINGS/結果を同じDOM土台へ順次寄せる。まずSELECT（装備選択）で、コイン/所持/ステージ情報をタイトルから移した受け皿として整えるのが自然。
> ### その他の開いた宿題
> - **画像素材の質**(ユーザー希望＝安っぽくない/フリゲっぽさ薄い/AI素材感薄い)：素材生成時に**ライティング/輪郭/影のルールを固定**して敵/雲/UIを一括更新(art-bible＋DESIGN.md連動)。雲は3〜5種へ。
> - **「初期は何で作るか」のベスプラを knowledge 化**(未)：ゲームは“プレイ=canvas / UI=DOM/CSSを最初から”。canvas手描きUIを仮スキャフォールドにすると恒久的にダサい。固定解像度ゲームはscaleラッパー1枚でDOM重ねが安い。LLMはHTML/CSSのUIが最も得意=DOM化でAI支援も最大化。→ Claudeが次回 knowledge に起こす(ユーザー要望)。
>
> ## ★気になってることリスト（ユーザー発・未解決のみ。これが正本）
> 2026-06-26 ユーザーが口頭で挙げた気になり点。Codexへ口頭で伝えたが上限で文字化できていなかった分を、ここに集約＝**散らさずここを更新する**。タグ＝〈✓済/要素材=画像生成/要実機=数値チューニング/設計=方針判断〉。
> 1. **雲が単調＋急にボンって出る**：「急に出る」は読み込みフェードで一次対応✓／「1種類で単調」は**要素材**（遠景薄雲・中景雲海・近景綿雲・縦流れ雲片の3〜5種）。
> 2. **スタートから近い雲が見えすぎ**（遠い雲が見えるのはOK、近くが多すぎ）：高度ゲートで一次対応✓→**要実機**で効き確認。
> 3. **全体のAI感・はりぼて感**（背景/雲/敵/オブジェ/ゴール/ボタン/モーダル）：**コード描画で潰せる分はほぼ消化**＝ゴール決勝バナー化✓・敵スプライトに接地影✓(貼っただけ感解消)・ボタン/モーダル/タイトル/ショップは監査済で既に良好(立体ボタン/影/ツヤ有)。**残るのは要素材**＝カバ/鳥/雲スプライトそのもののAI感(再生成しないと根本解決しない)＝Codexの画像生成案件。pad/地面は現状許容範囲。
> 4. **装備選択が崩れてる→カルーセルで選択中を大きく**：✓対応済（中央大プレビュー＋左右薄＋‹›、`?screen=SELECT&debug`で確認済）。
> 5. **鳥/障害物のバリエ・配置・ステージ増にまだ伸びしろ**：未着手。**★ルーティング指示＝敵バリエは従来Claude起点が多かったが、次はCodex起点の案を見たい**（CodexにキックオフでこのNo.5を渡す）。
> 6. **お金のマークが変**：`¢`→丸コイン描画に置換✓。
> 7. **クリア後カバがゴールでぴくぴく**：停止処理で✓（boost切/速度0/bob0/cleared時idle）。
> 8. **BGM/SE作る（Geminiで予定）→良いBGMリスト欲しい**：ステージとは別に**ショップ/装備/設定・ポーズも個別BGM**にしたい。候補は下の「Gemini向けBGM候補」に整理済み＝そのまま渡せる。
>
> **追加バッチ（2026-06-26 #2／Claudeが改善ループで対応）**
> 9. **装備画面の背景雲がフェードしない（popする）**：✓対応（drawMenuCloudsに入場フェード、sceneT基準でふわっと下から）。
> 10. **雲海ステージが突入後に初めて雲描画される**：✓対応（B限定で高度ゲートに下限を入れ突入直後から雲を敷く）。
> 11. **出発ボタン後の画面切替が唐突＝フリゲっぽい**：✓対応（`go()`遷移カバー＝カバーin→中点で実切替→カバーout、ロケット閃光。ゲーム⇄メニュー境界に適用）。
> 12. **ふうせんが「1.5倍だが1枚ずつ拾うので無意味なデメリット機体」**：✓対応（coinVal()で拾得時にライブ適用＝ふうせん2倍、+2ポップ表示。最終バランスは**要実機**）。
> 13. **機体の種類が少ない**：**方針確定(2026-06-26)**＝コビトカバ路線継続・**動物スキンは保留**・**こつぶ廃止✓**・**装備ごとに画像を書き出す**。**各装備の落下パニック画像＝✅完了(2026-06-26)**＝つばさ/ふうせん/コメット/マグの`veh_*_fall`をCodex生成(fall.png主参照でトーン統一)→cutout→drawHippoが状態別スプライトを自動使用(中央基準描画・左ズレ修正済)。`?cap&veh=N&pose=fall`で確認可。**残る不足画像＝boost/hit差分**(同方式で量産可、参照をboost/hit.pngに替えるだけ)。新機体そのものを増やすなら[機体設計.md]「★候補ブレスト拡張/次に出す機体」(ダッシュ/バブル＝発注文確定済)。
> 14. **コイン以外の収集物・強化・回復**：**収集→開放を採用(2026-06-26)**＝コイン(ショップ装備)と別トラックで、ステージ毎の隠し収集物コンプ→ショップ外の特別装備/(将来)動物スキン解放。設計＝[開発計画.md](開発計画.md)「★収集→開放(2トラック進行)」。強化要素の方向(rare回復ハート/グレイズ倍率可視化○、恒久強化×)も同doc。実装は装備カタログ確定後の中規模機能。
>
> ## ★バトン受け渡しメモ（2026-06-26 Codex→Claude へ復帰／次は誰でも可）
> - **担当交代**：**双方向の交代制**＝自分の上限/切り上げ時は、止まる前にこのHANDOFFを最新化＋1コミットして相手へ戻せるようにする（ルールは [AGENTS.md](AGENTS.md)「上限での双方向バトンパス」）。**push しない**。
> - **2026-06-26 Claude復帰パス**：①Codexの未コミット作業（見た目FB一次対応）を検証して取り込みコミット(4152fbb)。②**ゴールラインを決勝バナー化**(b4d9fd2、`drawGoal()`、確認`?cap&stage=A&alt=388&hold`)。③上の「気になってることリスト」を正本化(a25a5b7)。④**敵スプライトに接地影**(a1ef6b6＝貼っただけ感の解消・カバと統一)。⑤TITLE/SELECT/SHOPを監査=既に良好で変更なし。**コード描画で潰せる視覚AI感はここで打ち止め**＝残りはスプライト再生成(要素材=Codex画像生成)。全部コミット済み・クリーン・未push。
> - **2026-06-26 Claude改善ループ第2弾**：ユーザー指示「言われなくても磨ける所を自分で回せ」＋新気になり6点(リスト#9-14)。コードで潰せる4点を実装＝⑨雲入場フェード/⑩雲海の即時描画/⑪画面遷移トランジション(go())/⑫ふうせん経済の作り直し(coinVal)。各コミット＋validate/smoke/構文/目視OK。#13機体追加と#14収集/強化は設計メモ化(機体設計.md「次に出す機体」/開発計画.md「収集/強化要素の方針」)＝**着手はユーザーの方向確認待ち**。
> - **Codexが戻ったら最優先で渡すもの**：(a)気になりリスト#5＝**鳥/障害物のバリエ・配置・ステージ増をCodex起点の案で**（ユーザー希望）。(b)#13＝**次の機体スプライト生成**（ダッシュ/バブル＝発注文は機体設計.mdに確定済、生成→1行追加で入る）。(c)#1#3の**要素材**＝雲3〜5種・敵/カバスプライトのAI感低減リテイク。(d)#2近い雲ゲートの実機確認はユーザー待ち。
> - **直近の状態**：git 全部コミット済み・クリーン。**敵の手触りはユーザーOK**（「元より相当良くなった」）。コア＝避け/グレイズ/登る＋コイン経済＋ショップ＋機体5種が一周回る。
> - **今回のCodex実装**：**パルサー**追加（膨張収縮する発光球＝縮んだ瞬間に抜けるタイミング敵）＋**D 星脈**追加（パルサー主役の4面）＋**設定画面/セーブ削除UI/ミュート付きWebAudio SE/横ドラッグ感度**追加。ステージ選択に★評価・ベスト高度/タイム表示、**ポーズ画面**、**リザルトのコイン内訳**、**tools/smoke.cjs**も追加。A→B→C→Dの線形解放、`?debug`全解放、`node tools/validate.cjs` / `node tools/smoke.cjs` 対応済み。
> - **2026-06-26 Codex追記（見た目FBの一次対応）**：雲のロード直後フェード・低高度の近景雲抑制・雲の反転/散らし、装備選択の中央大プレビュー＋左右カルーセル化、コイン記号`¢`撤去＋丸コインアイコン化、クリア後のカバ停止を実装。ユーザー指摘の「画像がはりぼて/AI感」「ゴールライン/UI/敵/背景をもっと良くしたい」は未解決の大きな素材制作テーマとして残す。
> - **次の一手（おすすめ順）**：①**実機チューニング**(D星脈のパルサー周期/半径・スウープ/グレイズ・配置頻度/SE音量/感度/ポーズ導線)②**落下表情**(全機体・art重め)③さらに機体④D以降のステージ増築⑤BGM。なお見た目の「AI感」対応は**コード描画で潰せる箇所**（ゴール済✓／次候補=発射台pad・地面のディテール・敵の影/輪郭統一）と**素材生成が要る箇所**（雲3〜5種・敵スプライト刷新）に仕分け済み＝前者はClaude単独で進められる。
> - **開いている懸念（ユーザー指摘）**：**画像/スプライトのサイズ感**が気になる箇所あり（機体やカバ・敵の相対サイズ/画面占有率）。具体箇所はユーザーに実機で確認しつつ、`DRAW_H`/`veh().size`(全1.0固定)/`VEH_ART.bodyH`/`BIRD.drawH` 等で調整。雲は画像種類が少ないので、最終的には`cloud_small/cloud_big`だけでなく遠景薄雲・近景綿雲・縦に流れる雲片の3〜5種へ差し替えたい。
> - **Gemini向けBGM候補**：タイトル/出発導入、装備選択（ガレージ/工房ループ）、ショップ（少しコミカルな買い物ループ）、設定/ポーズ（薄い待機ループ）、Stage A「群れの空」（明るい上昇感）、Stage B「雲海」（柔らかい霧と少し緊張）、Stage C「嵐」（風/雷/パーカッション強め）、Stage D「星脈」（パルス系シンセ/タイミング感）、クリアジングル、墜落/被弾ジングル。SEはコイン、グレイズ、被弾、購入、UI決定/戻る、ロケット噴射、横風、パルサー膨張収縮、雲バンク突入を後で置換。
> - **要実機チューニング**：経済の回り(初機体までの回数)・パータイム係数0.22・コメット/マグ/敵速度の手触り＝係数は即調整可。**`?debug`で全解放+デバッグ**、**`?reset`でセーブ初期化**。
> - **画面デザイン**：本番ビジュアルは外部ツール(Figma等)書き出し待ち＝canvas手描きUIで凝らない。

**新しいチャット/セッションはまずこれを読む。** 設計の本丸は [敵設計.md](敵設計.md)＋[機体設計.md](機体設計.md)＋[開発計画.md](開発計画.md)（★メタ含む全体ロードマップ）＋ knowledge [[ゲームの中身を設計する順序]]。次に [art-bible.md](art-bible.md)／[素材棚卸し.md](素材棚卸し.md)。
> ★方向性が拡大（2026-06-25 FB）：単発プロト→**メタを持つゲーム**（タイトル→装備選択→プレイ→コイン→ショップで機体購入→収集）。**初期はロケットのみ所持**。コビトカバ体格は固定（sizeで大小しない／小型は別キャラ）。こつぶは一旦外す。
> ★**Phase 0+1 実装済み(commit 6942613)**：シーン状態機械(TITLE/SELECT/SHOP/PLAY)＋セーブ(localStorage `hippo_save_v1`)＋タイトル/装備選択/ショップ/結果でコイン加算＋★評価＋機体購入＝**コイン経済が一周回る**。旧DOMバー撤去・canvas hotspotボタン。`?screen=TITLE/SELECT/SHOP`でメニュー撮影可。機体の正体は[機体設計.md]カタログ参照。
> ★**画面デザイン方針(決定)**：UI/画面ビジュアルは**別ツール(Figma等)で作って書き出す**。canvas手描きで凝らない。現状のメニューUI(commit dc3ea9d=立体ボタン/アニメ背景/カード)は**動く土台**＝外部デザインが来たら「描画を画像drawに差し替え」。座標/hotspotの仕組みは流用。
> ★**経済/評価/バランス実装済み(commit 1cceda9)**：コイン=収集/経済・★=腕前(①クリア②速さ=パータイム内③ノーダメ)。bankRun=収集+グレイズ×coinMul+クリア15+ノーダメ8+速さ12。runTime計測でパータイム(高さ×0.22s)＝**速さに価値**。ふうせんcoinMul1.5。体格は全機体1.0固定。価格つばさ80/ふうせん160。係数は[機体設計.md「実装バランス」]に記録＝**実機チューニング前提**。
> ★**Phase 2 進行中**：機体パイプラインをデータ駆動化(VEH_META: price/coinMul/fx/mag、VEH_ART: bodyH/cy/cx)＝機体追加は行追加+スプライトで済む。**機体5種**＝ふつう/つばさ/ふうせん(coin1.5×ハイリスク)/**コメット(速さ特化・price240)**/**マグ(コイン磁石=半径MAG_R引き寄せ・price140)**。こつぶ(3)はbuy:false。**B/Cにも反応脅威(斜めスウープ)展開**＋**パルサー(タイミング敵)**追加、さらに**D 星脈**追加＝全ステージで迫る脅威+グレイズ+一部タイミング判断が機能(`node tools/validate.cjs`確認済み)。drawHippoにcx(横アンカー)追加=左右非対称リグ対応。**`?reset`でセーブ初期化**(テスト用)。**`?debug`でデバッグON+全解放**(P=ON/OFF、I=無敵/H=当たり可視化/U=全解放/C・1-6=機体/J=+150m/K=頂上、?debug=hitで可視化も)。
> ★**次の候補**：①D星脈/パルサー/スウープ/グレイズ/SE音量/感度/ポーズ導線の実機チューニング②落下表情(全機体・art重め)③さらに機体(ホバー=静止/バブル=保険 等[機体設計.md])④D以降のステージ増築⑤BGM。デザイン本番は外部ツール書き出し待ち。**要実機(最重要)**：経済の回り(初機体までの回数)・パータイム達成可否・コメット/マグ/敵速度の手触り＝係数はすぐ調整可。
最終更新: 2026-06-26 Codex。ポーズ画面＋画面スモークテスト追加パス。**push はしていない**（指示があるまでしない）。ローカル実行＝`node`簡易サーバ(`scratchpad/serve.cjs`相当)→ http://localhost:8123/index.html （無ければ `node` でindex.htmlを8123配信）。

## ★最新の現在地（2026-06-25 / 実機FB7＝面白さの軸を①反応+②攻め得に転換）
**実機FBで「鳥の列を抜けて大きい鳥をよける、の繰り返しでつまらない／迫る障害物を反応で避ける遊びがない」と判明。** 上かぶり/フロート(下記)でY軸は埋めたが、遅い降下ゲート＝事前X位置取りの静的パズルに収束していた。面白さの軸を**①反応アクション(迫る脅威)＋②リスク/リターン(攻めて得する)**に転換し、**ステージAを叩き台に実装**(commit予定)。
- ✅ **横スウープ**＝端の高度レーンで予告(赤い危険ライン＋矢印)→高速横断。**押す/離すで高度をずらして反応回避**(ワンボタンが反射に効く)。①の本命。`spawnSwoop`/`swoop`ビート。
- ✅ **ニアミス加点**＝脅威を当てずにかすめて離れると**コイン+速度ご褒美**(全敵に常時)。避けるが"攻めて得する"に変わる(`GRAZE=22`/`popups`)。②の核。
- ✅ **危険ダイブコイン**＝安全な登り線の下へ伸びるコイン列(攻めて落ちて取る)。`spawnDive`/`dive`ビート。
- ✅ **Stage A再構成**＝スウープ主役、ゲートは少なめ・速め(`dmul=1.5`)、タカ/ダイブ/上かぶりをteach→混ぜる→畳み掛け。
- **検証**：構文OK／バリデータ(`scratchpad/validate.cjs`)で**A/B/C全ステージ例外なく頂上まで通る**／`?cap&demo=swoop/dive/over/float`で各メカを目視確認。
- **次の一手**：①**実機で手触り確認**(スウープ速度760/予告タイミングH*0.40/dmul/グレイズ幅)→数値はすぐ調整可。②良ければ**B/Cにも反応脅威を展開**(B/Cはまだ旧構成)。③機体の存在理由をコイン/タイムに。設計の詳細＝[敵設計.md]「方針転換(FB7)」。

## ★前パスの現在地（2026-06-25 / 敵メカ実装パス＝Y軸の空白を埋めた）
**ビジュアル/UI/機体/ステージ区間/コインは実装済み**で遊べる。**この回で[敵設計.md]ロードマップ①〜⑤を実装**し、核心課題だった「離す(落下)が勝ち筋でない＝Y軸の空白」を埋めた。**※下の「いまどこ」以降は古い記述＝末尾の実機FB記録＋[敵設計.md]の「実装状況」が最新。**
- **この回で実装(commit予定)**：
  - ✅ **①上かぶりゲート**(`spawnOverhang`／`over`ビート)＝入口スイフト列＋穴の真上に**ペリカン板**(box当たり)。板は穴より広く**直進(上げ続け)を罰し、離して脇から抜ける**。
  - ✅ **②下脅威フロート**(`spawnFloat`／`move:'float'`／`float`ビート)＝下からせり上がる泡(落とし罰)。横に必ず穴。
  - ✅ **③危険側コイン**＝板の脇/フロートの穴の低い所(攻めると取れる＝落下/横の報酬)。
  - ✅ **④生成バリデータ**＝`scratchpad/validate.cjs`(ブラウザstubでspawn実行)で**全機体×全パターンに横の通路あり**を確認。
  - ✅ **⑤難度カーブ**＝`diff=alt/40`(早熟)→**ステージ進捗全体**に。
  - 🔧 **既存構成のブラッシュアップ**(指摘「下からの話だけでない」)＝**"大きい鳥=デカいスイフト"を単独行から廃止**(役割重複解消・largeは母体専用)／スイフト列の**形状を設計値**で(line/vee/stream)／calm圧縮／各ステージ**teach→混ぜる→畳み掛け**で新メカ配置(A=上かぶり/B=フロート/C=風中の上かぶり)。
- **検証済み**：構文OK／ヘッドレスChromeで上かぶり・フロート・各ステージ描画OK(`?cap&demo=over/float`で新敵を中央固定表示＝確認用に追加)／バリデータで全機体通過OK。
- **次の一手**：①**実機チューニング**(CAP_DY/FLOAT_RISE/頻度/板hw/難度の効き＝要プレイ)②**上かぶりのポケットに薄い下脅威を重ねる**(L2の★最高レバレッジ＝1遭遇で「落とす→落としすぎ前に再点火」)③**機体の存在理由をコイン/タイムに結ぶ**(遅い安全機体が無条件で強い問題・保留中)④ペリカン/フロートの**Codex生成スプライトへ差し替え**(コード描画→`sprites.pelican/float`があれば自動使用)。

## いまどこ（マイルストーン）
**動くスプライト版プロトタイプまで完成。** 最初の「動画で発想 → コンセプト → コアの面白さ → 動くプロト＋一貫素材」が一周つながった状態。
- [index.html](index.html) をブラウザで開けば遊べる（押す＝上昇 / 離す＝落下、鳥を避けて高度を稼ぐ）。
- 主役カバはスプライト化済み（idle/boost/fall/hit）＋コード演出（炎パーティクル・風スピードライン・状態ごとの煽り揺れ）。
- **鳥2種を動き方で差別化**：小=速い斜めジグザグ／大=遅いゆらゆら（種類＝挙動で読みやすく）。
- **HP3制**（即死やめ）：被弾でHP-1＋無敵1.2秒＋点滅＋赤フラッシュ＋右上ハートUI。**地面落下は即死**。
- **接地スタート**：開始は発射台に接地（`START_Y`で浮き解消）。
- **背景/地面を画像素材化**（コードのシェイプは安っぽいので移行）：`ground`(草+土の全面塗り板/切出なし)・`pad`(発射台)・`hills`(遠景ティール)・`cloud_big/small`(雲2層パララックス)。空グラデのみコード。緑キー注意＝緑の草/丘は工夫（地面rawで使う／丘はティール化）。詳細[素材棚卸し.md](素材棚卸し.md)⑤。
- **スター無敵パワーアップ**：`star`取得で`INVINC=5秒`の無敵（`power`=クワッ大口スプライト＋金オーラ＋キラキラ＋HUDタイマー、鳥を弾き飛ばす）。`STAR_GAP`で出現間隔。
- **横移動＋乗り物プロト（検証中）**：タップ＝上昇は維持しつつ「軽い横移動」を任意の第2軸として追加（←→ / A D / ドラッグ、`hipX`）。`VEHICLES`に4種（ふつう/つばさ/ふうせん/こつぶ）。各乗り物は **上がりやすさ(thrust)・重さ(grav)・横の曲がり(steer)・大きさ(size=当たり判定&見た目)** を変える＝必ず「強み＋弱み」のトレードオフ。切替は数字1〜4 or 画面下ボタン。
  - **設計の決定**：移動変数は「既存の数値いじり＝安全」＋「横軸＝任意の第2軸」だけで作る。**燃料ゲージ／風／スコア通貨は当面入れない**（今の"のんびりタップ"を別物に変えるリスクのため、必要時に単独で議論）。"大きさで死にやすさを表現"、変わり種(例:HP減るほど強い"火事場")は**その乗り物だけの個性に閉じる**（全体ルールにしない）。
  - **直近の調整**：`vfall`(落下の最高速)を乗り物ごとに追加＝風船の「ゆっくり止まって急加速で落ちる」不自然さを解消。横移動は**相対ドラッグ＋慣性(`smax`/`sacc`)**にして「指でカバが隠れる」「ワイパーみたいに雑にごしごしで勝てる」を解消。
  - **ペーシング(ゾーン制)＋雲の壁ギミック（検証中）**：単調さ対策として、エンドレスに高度ベースの「ゾーン」(`ZONES`/`ZONE_SEQ`＝calm/normal/dense/bonus/wall)を導入し緩急を作る。新ギミック`雲の壁`=横一列に隙間があり**横移動で抜ける**(鳥と違う避け方／横軸に意味)。スターは**bonusゾーンに集約**（＝「スター置きすぎ」も解消）。画面上の「ZONE 〇〇」は検証用表示。これらは**ステージの部品として流用**する前提。背景の雲パララックス逆転バグも修正済み(登る=雲が下へ)。
  - **未解決（保留中）**：**速さに価値がない＝つばさ(操作性)最強**問題。原因は「ゆっくりでも同じ高さに行ける＝速さに報酬がない」。下からの追跡(ネガティブ)は入れない方針。**ポジティブ/時間記録ベース**で速さに意味を持たせる案（タイムアタック＋スター＝＋時間 等）は出たが**一旦保留**。ジェット系の速い機体を活かすにはいずれ要対応。

## ゲームのコア（確定）
- **1文**：重いコビトカバがロケットで上を目指す縦スクロール。ブーストで昇り、**あえてエンジンを切って落下し**、鳥などをかわして高度を競う。
- **毎秒の駆け引き**：ブースト（高度↑だが上の障害に突っ込む）/ 落とす（避ける・距離を取るが高度↓と下の危険）。
- **設計の決定**：コストは1軸（位置）に絞る＝**燃料は入れない**。横位置固定＝左右向きスプライト不要。
- **キャラ表現はマスト**：状態ごとに体勢/表情が変わる（企画.md参照）。「クワッ大口」は**無敵/パワーアップ専用**に予約。

## 確定した素材・絵柄
- **絵柄**：3Dプリレンダ風の2Dスプライト（マットなマスコット、写実NG）。詳細 [art-bible.md](art-bible.md)。
- **主役マスター＝ `assets/ref/hero_master.png`（cand3：ぽっちゃり・キリッ）でロック済み**。以後すべてこれを `-i` 参照に一貫生成。
- **完成済みスプライト**：`assets/sprites/{idle,boost,fall,hit}.png`（透過・切り出し済み）。元の緑背景版は `assets/states/`。
- 学んだ外れ（art-bibleのルーブリックに反映済み）：**白目＝人間っぽい→黒目ドット維持／細長→チョンキー維持／大口開け＝きもい（無敵専用）／焼き込み炎は身長正規化でカバが小さく見える→炎なしで出し炎はコード**。

## 次の一手（小分け／[素材棚卸し.md](素材棚卸し.md) 順）
1. ~~**鳥スプライト2種**を生成して三角を置換~~ ✅完了（`bird_small`=ティール/橙スイフト・小速い／`bird_large`=紫ペリカン・大遅い。原画は右向き、`draw()`で`b.dir<0`なら左右反転、羽ばたきbobはコード。高度が上がると大鳥比率↑）。
2. ~~**パワーアップ1種＋無敵（クワッ大口）**状態~~ ✅完了（`star`を取ると無敵`INVINC=5秒`：`power`スプライト=クワッ大口、金色オーラ＋キラキラ＋HUDタイマー、無敵中は鳥を弾き飛ばす。`STAR_GAP`間隔で出現）。
3. コイン/星などの収集物（コイン未。スコア/通貨を入れるならここ）。
※「動きの大半はコード（炎/煙/キラキラ/拡大/なびき）」。原画は状態の種類を小分け追加。アニメ連続フレームが要る時だけ"1枚シートでまとめて生成→コード切り出し"。

## いまの本筋：ステージ制（コンセプト違い3本）＋作りこみライン ←イマココ
単調さの本丸＝**決め打ちビート列のステージ**（理屈 knowledge [[敵ウェーブと緩急の設計]]）。難易度別でなく**コンセプト違い3本**（自由選択・各ステージ内で緩急カーブ・**頂上500m到達でクリア＝finite**）。**実装済み（Codexレビュー反映済み／実機チューニングは要）**：
- **枠組み**：`STAGES`＝高度キーの`beats`列。種類＝`calm/trickle/dense/bonus/cloud`＋イベント`swarm/elite(=ダイバー)/wind/obstacle`。`stageTopY()`がビート合計を100m丸め＝各**500mでクリア**→クリア画面→タップで次ステージ。`?cap&stage&veh&alt`で撮影モード(視覚レビュー用)。ランダムゾーン/`loopFrom`は廃止。
- **A 群れの空**：`swarm`＝整列フォーメーション(line/vee/arc・必ず通れる隙間＋最小間隔66・群れ全体が横ゆれ＝飛んでる)。空=青。得意=つばさ。
- **B 雲海**：`cloud`＝上下2枚の厚い霧(視界減fog)＋その中に**ダイバー(タカ)**が潜む。空=グレー。得意=こつぶ。
- **C 嵐**：`wind`＝**横風**（`WIND_ACC=650`でhvelに外力＝機体のsacc/smaxで抵抗が変わる）。`obstacle`=雷雲(静止)。空=暗。得意=ふつう。
- **敵は見た目で挙動が分かる**：スイフト(`bird_small`:速いジグザグ/群れ)・ペリカン(`bird_large`:大ゆったり)・**タカ**(`hawk`:画面内で起動→赤予告リング→ダッシュ離脱)・**雷雲**(`storm`:静止障害物)。`b.sprite`で描画上書き。
- **Codexレビューで是正済み(重要)**：①started前は敵/時間/ダイバー停止 ②ダイバーは画面内で起動＋予告 ③当たりは**スイープ判定**(prevHippoY→hippoY)でフレーム落ちすり抜け防止 ④**飛行中は機体変更ロック**(`canSwapVeh`) ⑤ふうせん縦性能ダウン(thrust0.8/vrise0.82) ⑥風は縦押し→**横押し**へ転換 ⑦頂上超えspawn禁止＋群れ隙間クランプ＋デッドコード除去。
- **要チューニング(実機)**：横風`WIND_ACC`(現650・少し強いかも)/各ビート長/フォーメーション密度・隙間/ダイバー追尾0.85秒/ステージ長500m。

**作りこみライン（並行で力を入れたい）**：背景のリッチ化／**機体ごとのコビトカバ画像**（つばさ=翼、ふうせん=風船…）。ゴール/クリア/全体の"チープさ"も**後でまとめて画像化**したい（ユーザー方針）。
- ★**機体画像の鉄則**：カバ本体は全機体**同じ大きさで描く**。大小は**機体リグ＋コード(`VEHICLES[].size`/`DRAW_H×size`)**で出す＝**絵でカバを大きく/小さく描かない**（二重適用防止）。詳細 [art-bible.md](art-bible.md)。

## このチャット末の引き継ぎ
- ~~**UI/UX修正(HIGH)**：①飛行中は検証ボタン非表示 ②「あとNm」見切れ ③HUD暗スクリム~~ ✅完了(commit 3b09788・ヘッドレスChromeで目視確認済み)。①は`canSwapVeh()`連動で停止時だけ表示／②右マージンW-12／③上部132pxに暗→透スクリム。
- **スクショ撮影＝実証済みコマンド**（Gemini視覚レビュー用。ヘッドレスChrome・要待機budget）：
  ```
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu \
    --hide-scrollbars --window-size=480,860 --run-all-compositor-stages-before-draw \
    --virtual-time-budget=2500 --screenshot="<out>/play_A.png" \
    "file:///c:/Users/mr_ba/Documents/hippo-rocket/index.html?cap&stage=A&veh=1&alt=150"
  ```
  `?cap`なしで撮ると待機画面(ボタン表示状態)。`alt`指定で敵を画面に出す。
- **レビュー体制**：Codex(ロジック/バランス)=稼働OK・反映済み。**Gemini(視覚/UX)=APIキーで疎通確認済み**（`~/.gemini/.env`にキー＋`settings.json`の`security.auth.selectedType="gemini-api-key"`必須／pro混雑時は`-m gemini-2.5-flash`）。**次やる＝上記スクショ→`gemini --skip-trust -m gemini-2.5-flash -p "…@shot.png"`で視覚レビュー**。手順 knowledge [[AI協業の運用]]/[[Gemini CLIの使いどころ]]。クロスチェックは**2人目に1人目の結論を渡さない**。
- ~~**未コミット**：knowledge「Antigravity記述の訂正」~~ ✅コミット済み(knowledge 2fb71b9)。hippo-rocketのコードも全コミット済み。
- **Gemini視覚レビュー＝2周完了・コード対応分はほぼ消化**（A/B/C 3ステージ・gemini-2.5-flash）。総合レビューで「**トンマナは非常に一貫・浮くステージなし／HUDは非常に読みやすい**」評価。コード反映済み：
  1. ~~嵐の背景シェブロン格子が安っぽい~~ ✅**風で寝た斜めストリークへ**(commit a030e90・Geminiで「ノイズ解消・風が伝わる」確認)。
  2. ~~雲が単調・奥行き不足~~ ✅**被写界深度**(b4b3eaf=遠景blur+大気ヘイズ/近景くっきり)。
  3. ~~3ステージのトンマナ不統一~~ ✅実質解決(↑の雲/風統一で「違和感なし」評価)。
  4. ~~のりものブロックが大きい~~ ✅**詳細ステータスは停止時(選択時)だけ表示**(f4bafe6)。
  5. ~~暗背景でカバが埋もれ／あとNmがバーと重なる~~ ✅**バックライト+ドロップシャドウ／ラベルをバー左へ**(5128bfc)。
  - 低優先の残り：ステージ名は黒縁(hudText)で可読だがGeminiは半透明パネルも提案(任意)。
  - **残るアート＝素材生成が必要なもの**：~~機体ごとのカバ画像~~ ✅**4種完成**(commit 99879c3。つばさ=羽/ふうせん=黄色い風船/こつぶ=プロペラ帽/ふつう=既存ロケット。hero_master参照でCodex生成→cutout透過)。★体サイズ二段構え実装＝原画は全機体同体高、drawHippoが`bodyH=DRAW_H[st]*veh().size`で体基準正規化＋`VEH_ART.cy`縦アンカー→「絵で大小を描かずコードのsizeで大小」を担保。噴射炎はふつうのみ。**残りの作りこみ**：地面/発射台/ゴールのリッチ化。
  - **機体アート＝Geminで検証済み**「4機体とも同一キャラ・リグで判別可・体サイズ意図(v2大/v3小/v0v1中)破綻なし」。①風船位置=結果良好 ②~~boost演出~~ ✅追加(commit 83ed486=空気パフ/上昇キラキラ/機体別ポンプ。炎はふつう専用)。残り③idle/boost/fallは機体base共用(hit/powerは共通)＝必要なら機体別の状態差。原画は`assets/states/veh_*.png`(緑背景)で再生成可。
  - ※cap撮影で稀にカバが半透明に写る=被弾iframe点滅をたまたま捉えただけ(バグでない)。cap撮影は`&show=dead`で死亡パネルも確認可。
  - **背景/UIの作り込み(追加)**：~~地面~~ ✅リッチ化(ec7bb28=土の濃淡/地層/石/根/草の小花)・~~遠景の丘~~ ✅多層大気遠近化(cd95d7b)・~~クリア/死亡画面~~ ✅専用カード化(0506234=星3/後光/ピル型ボタン+結果画面はプレイHUD非表示)。いずれもGemini開始画面スキャン(#1地面/#2丘)とレビュー方針に沿う。
  - **次の候補(任意)**：機体別の状態差(idle/boost/fall)・発射台padのリッチ化・スター/ハート等の小物アイコン・キービジュアル/タイトル。
- **実機プレイFB(1回目)反映済み**：①~~風船の体がデカい~~ ✅体は普通サイズ・大風船3つで吊る構図に再生成(ee3741d。size1.5→1.0)。②~~死因でメッセージ未分け~~ ✅衝突=「ぶつかった！」/落下=「墜落…」(die(cause))。③~~フォントがダサい~~ ✅"M PLUS Rounded 1c"丸ポップ体に全面差替。④~~各ステージで遊びが変わらない/つまらない~~ ✅**3ステージ再設計**(28fbf6b・knowledge[[敵ウェーブと緩急の設計]]):A=隊列読み主体(stream斜め帯追加)/B=霧主体+潜むタカ/C=風中に雷雲壁を挟み隙間スレッディング。汎用trickleを削減し主役メカを前面化。
  - **要・再プレイ確認**：ステージ再設計とバランス(隊列の隙間幅/風の強さ/障害壁の間隔)は実機チューニング前提。各ステージ約400m(ビート合計の100m丸め)。
  - **実機FB(2回目)修正済み**：①~~300m付近フリーズ~~ ✅(c090563=頂上手前で全ビート消化しbeatI=配列長→HUDのbeats[beatI].type未定義参照で例外停止。beatIをクランプ)。②~~鳥の種類違いが重複~~ ✅(同コミット=短いイベントビートを先読みが飛び越しnextSpawnが後戻り→重ね置き。event枝をnextSpawn=Math.max(nextSpawn,beatEndY)で前進のみに)。両方ユーザー実機で解消確認。
- **実機FB(3回目)＝設計の深掘り**。方針はユーザー選択「**敵の哲学だけで深く(新動詞なし)／スターは廃止して収集要素**」。knowledge[[ゲームの面白さの型]]([[敵ウェーブと緩急の設計]])準拠。反映済み(35f3c5b/b016182)：
  - ~~スター無敵が支配的(ギミック無視でゴール直行)~~ ✅**廃止→コイン収集**(スコア・避ける以外の正の動機)。invinc/power/aura全撤去。code描画コイン+HUD枚数+クリア画面。
  - ~~機体が没個性・操作感が変わらない~~ ✅**数値差を増幅**(つばさ=上昇激弱+超滑空+横俊敏/こつぶ=落ちやすい極小)。
  - ~~ふうせんが普通と変わらない・縦長で役割不明~~ ✅**hitW1.8の横長当たり判定+横広3風船に描き直し**=「上の障害に当たりやすい高リスク/ほぼ落ちない」役割を明確化(体は普通サイズ維持)。
  - ~~鷹の存在理由不明・脅威でない~~ ✅**再定義**=上部で狙い定め(縦の赤ビーム予告)→まっすぐ急降下し"列"を塞ぐ=同じ横位置で上げ続けを罰し横回避を迫る。
  - ~~距離が短すぎる~~ ✅**A700/B900/C700m**へ延長。
  - **★次パス(未着手・最重要)＝敵の哲学を本格化**：今は「天井ハザード(上げすぎ罰)/床ハザード(下げすぎ罰)/動く隙間」が未実装で、まだ「小鳥よけ＋鷹＋風/壁」止まり。ブースト⇄落下⇄横の3択を毎瞬迫る配置(上に居座る敵/下から迫る静止トゲ帯/横に動く隙間)を入れるのが核。これでvel<0(落下)に意味を与える。
  - **次パス2＝機体の表情差分**(idle/boost/fall顔)：今もbase共用でfall顔が無い。各機体のfall(パニック)顔を素材生成 or 顔オーバーレイ。art-bibleの2レイヤー方針。
  - 撮影モード: `?cap&stage=&veh=&alt=` + `&show=dead&cause=fall/hit`。ローカルは`node scratchpad/serve.cjs`→localhost:8123。
- **実機FB(4回目)＝操作ストレス/敵哲学**。方針=ユーザー選択「**通り過ぎる群れに全面作り直し＋被弾ノックバック**」。反映済み(fd2dc02)：
  - ~~速い縦横無尽な敵＋急ブレーキ不可で同じ鳥に何度も当たる~~ ✅**ジグザグ(diag)廃止→sweep(一方向に横切って画面外へ)**=読める・再衝突しない。鳥速度↓密度↓隙間↑。被弾ノックバック(鳥と反対へ＋少し上)で再衝突ループを断つ。
  - ~~同じ画像で別挙動~~ ✅**1種1挙動1見た目**(小鳥=速い小群れ/大鳥=遅い壁/タカ=予告急降下)。formも一方向へ流れる。
- **実機FB(5回目)＝敵が散漫/横スポーンが見える/縦軸の陣形がいい。AIレビュー回して自律で進める指示。**
  - ~~鳥が画面中央に湧く~~ ✅**Codexレビューで根本特定・修正**(0506234系→aheadSpawnY): イベントビートがnextSpawnでなく古いbeatStartYで生成→可視域に湧いていた。全生成経路を画面上端+SPAWN_TOP_PAD(140)より上にクランプ。
  - ~~横スポーン/横に消える/縦横無尽~~ ✅**「登って抜ける陣形ゲート」に作り直し**(spawnBird=横一列/斜め/V字に1〜2の穴、altを保持し上から来る、隊形ごと横流れ=穴も動く)。ジグザグ廃止。
  - ~~陣形が散漫で穴が読めない/雨線がノイズ~~ ✅**Gemini視覚レビューで可読化**(横一列基調・縦揺れ控えめ・穴広め・密度緩急・drawWind薄く)。Gemini「dense=穴が一目で分かる」確認。
  - ~~ステージ=1つの体験(区間設計)~~ ✅**区間構成に再構成**(導入静→波急→コイン回廊(敵なしご褒美nobird)→休符静→…→クライマックス→ゴール前静)。A600/B約700/C約600m。
  - ~~速い/同じ鳥に再衝突~~ ✅鳥速度↓・被弾ノックバック・1種1挙動1見た目。
  - 自己QA済み(Codexはタイムアウトで未完): コインはクランプ済nextSpawn使用で画面外/陣形は常に通れる穴あり/4機体とも登坂可/不可能な壁なし。
  - **★まだ残る大物=「よける以外がない」の本丸＝敵の哲学(天井ハザード:上げすぎ罰/床トゲ帯:下げすぎ罰/動く隙間)**。これでvel<0(落下)に意味。今は陣形ゲートを「登って横で避ける」が主で、落下の決断がまだ弱い。鷹(予告急降下)は入れたが種類が少ない。
  - 次候補2＝機体のfall表情(顔差分・素材生成。Codex画像生成は5分超でタイムアウトしがち=要分割/リトライ)。
  - **設計の残論点**：速さの価値づけ・コアの深み等は従来どおり保留。ローカル確認は`node`簡易サーバ(scratchpad/serve.cjs)でhttp://localhost:8123。
- **保留の設計論点**：速さの価値づけ(タイムアタック/時間記録)・チェックポイント+1kmゴール案・各ステージ配置の"癖"の深掘り・コアの深み(かわす行為自体)。

## 素材パイプライン（検証済みの叩き方）
- **生成（Codex内蔵画像生成・ChatGPT枠＝無料）**。マスターを参照に：
  ```
  printf '%s' "<英語プロンプト+chroma green bg+No text>" | codex exec --skip-git-repo-check \
    -C "c:/Users/mr_ba/Documents/hippo-rocket" -s workspace-write \
    -i "c:/Users/mr_ba/Documents/hippo-rocket/assets/ref/hero_master.png"
  ```
  - 生成先 `~/.codex/generated_images/<id>/*.png`。**最新フォルダから `cp` でrepoへ取り込むのは呼び出し側（Claude）でやる**。
  - SKILL.md読込で `windows sandbox: spawn setup refresh` エラーが出るが**無害**（生成は通る）。
- **透過抜き＋切り出し**：`node tools/cutout.cjs`（クロマキー緑→透過、bbox切り出し。sharpは hippo_fall の node_modules を絶対パス参照）。出力 `assets/sprites/`。
- **★ルール：画像を生成したら必ずプレビューを開く**。`assets/.../_preview.html`（`<img>`グリッド）を作って `Start-Process` で開き、人が並べて選ぶ。AI自身も Read で目視。
- **身長正規化の注意**：表示高さは index.html の `DRAW_H` で調整。焼き込み要素（炎等）があると枠が伸びてカバが小さく見える＝**炎なしで出して炎はコード**が鉄則。

## チューニング箇所
- [index.html](index.html) 冒頭の定数：`GRAV/THRUST/V_RISE/V_FALL`（手触り）、`DRAW_H`（表示サイズ）、`HIPPO_R`（当たり）。

## 関連ナレッジ（汎用手法・knowledge vault）
このゲーム固有でない手法は `c:\Users\mr_ba\Documents\knowledge` にある：
- ゲーム発想の進め方（目的→決断→操作→検証→可視化）
- ゲームの面白さの型（駆け引きの型と実例・審問リスト）
- 動画駆動開発（コンセプトを動画で可視化）
- Codexで画像生成する／visual-asset-kit スキル（素材の一貫量産）

## git
- hippo-rocket・knowledge とも全コミット済み・**未push**。push はユーザー指示時のみ。
