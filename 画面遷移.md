# 画面遷移図（hippo-rocket）

各画面が「何のための場所か（1つの決定）」を先に定義する。これを描かずに各画面を作ると、概念と場所がズレる（例：行き先選択をガレージの中でやってしまう）。
方法論は knowledge [[画面フローを先に描く]]。

## フロー図

```
            ┌──────────┐
            │  TITLE   │  ブランド/発射前のワクワク
            │ (発射前)  │  決定＝「出発する」だけ
            └────┬─────┘
        出発する  │                 ⚙ → SETTINGS
            ┌────▼──────┐
            │  GARAGE    │  ★機体(装備)を選ぶ
            │ 機体選び    │  場所＝雲上ガレージ（格納庫）
            │            │  ‹›で機体・ステータス確認
            └─┬───┬────┘
       ショップ │   │ 行き先をえらぶ        ⚙ → SETTINGS
        ┌──────▼┐  └─▼───────────────┐
        │  SHOP  │   │  DESTINATION       │  ★行き先(ステージ)を選ぶ
        │ 屋台   │   │  行き先選び          │  場所＝雲に開けた発射デッキ/空
        │       │   │ (発射デッキ・空)      │  各ステージ＝空に浮かぶ島/領域
        └──┬────┘   └───┬───────────────┘
       もどる│  ←戻り先     │ これで出発
        (GARAGE)          ▼
                      ┌────────┐
                      │  PLAY   │  本編（canvas）
                      └───┬────┘
              クリア / 失敗  │
                      ┌─────▼──────┐
                      │  RESULT     │  もう一回 → PLAY
                      │  結果        │  装備を変える → GARAGE
                      │             │  つぎの空(clear) → DESTINATION
                      └────────────┘

  SETTINGS … 各画面の ⚙ から開く小パネル。閉じる＝元の画面へ戻る（行き止まりにしない）。
```

## 各画面の責務（1画面＝1つの決定＝1つの場所）

| 画面 | 1つの決定（役割） | 場所/シーン | 主要操作 | 出口 |
|---|---|---|---|---|
| TITLE | 始めるか | 発射前の空（KV v5） | 出発する / ⚙ | → GARAGE / SETTINGS |
| GARAGE | **どの機体で飛ぶか** | 雲上ガレージ（格納庫・展示台） | ‹›機体送り・ステータス確認・ショップ・⚙ | → DESTINATION / SHOP / SETTINGS |
| DESTINATION | **どの空へ飛ぶか** | 雲に開けた発射デッキ／空（島で行き先） | 行き先タップ・★/ベスト確認・これで出発 | → PLAY / 戻る=GARAGE |
| SHOP | 何を買うか | 雲上の屋台（店主＝下隅固定） | 商品スクロール・購入・もどる | → GARAGE |
| SETTINGS | 設定変更 | 軽い雲＋半透明パネル | 各設定・閉じる | → 元の画面 |
| PLAY | 飛ぶ | 本編ステージ（canvas） | 長押し上昇 ほか | → RESULT |
| RESULT | 次どうするか | 成功＝星/失敗＝雲クッション | もう一回・装備変更・つぎの空 | → PLAY / GARAGE / DESTINATION |

## 原則（崩さない）
- **1画面＝1つの決定**。決定が2つ以上あるなら画面を分ける（旧SELECTは「機体＋行き先」を混ぜていた＝分割）。
- **画面は「場所」として考える**。その決定が起きるのに相応しい場所か？（行き先選び＝外/空。装備選び＝ガレージ。）
- **戻り先を必ず定義**。どの画面からも前へ戻れる。行き止まり/孤児画面を作らない。
- この図を**先に描いてから**各画面の背景シーン・構図・UIを作る（[[UIの構図は先人の型から起こす]] はこの後）。

## 実装メモ（現状との差分）
- 旧 `scene='SELECT'`（機体＋ステージ）→ **`GARAGE`（機体のみ）** と **`DESTINATION`（ステージのみ）** に分割する。
- GARAGE背景＝生成済み `assets/ui/select_bg_v1.png`（雲上ガレージ）。DESTINATION背景＝別途生成（空に開けた発射デッキ）。
- 順序は TITLE → GARAGE → DESTINATION → PLAY を既定とする（行き先を後にして「これで出発」で締める）。※「行き先を先に選んでから推奨機体を見せる」逆順も設計上あり得る＝要検討。
