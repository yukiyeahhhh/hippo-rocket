# GOAL — 縦エンドレスモードの最小プロト（チャンクをシャッフルして無限連結）

> 依頼(2026-07-06 夜・yukiya就寝→明日試す・無人運転)：hippo-rocketに「縦のまま何m登れるか」の独立エンドレスモードの最小プロトを作る。既存の物理・敵・アートを**全部流用**。ステージの枷を外した素のコアが楽しいかを実機で試すのが目的。
> 方向性の合意：①縦型のまま（横スクロールにしない）②独立したエンドレスモード（既存コースモードは壊さない）③既存資産を全流用（新規アート生成なし）。

## 背景（コード構造のキモ）
- ステージ＝`STAGES[key].beats`＝`{type,len,...}` の列。**このbeat 1個＝チャンク**。エンドレス＝beatをシャッフルして無限に供給すればよい。
- `scene` state machine：'SPLASH'|'TITLE'|'HELP'|'GARAGE'|'DESTINATION'|'SHOP'|'SETTINGS'|'PLAY'。
- 地域＝A鳥/B雲海/C嵐/D星脈。beatのtypeは地域固有のものがある（wind/drift/pulsar等）。

## Definition of Done（検証可能な最終ゴール）★全項目クリア（2026-07-06 夜・Opus）
- [x] タイトルから「エンドレス β」を選んで開始できる（既存コース独立・既存フロー不変）。`?mode=endless`でも即開始。→ title.png目視・startEndless()追加
- [x] ステージ末尾で終わらず、beatチャンクをシャッフルして**無限に敵が湧き続ける**。→ endless_check：13→128個生成・フロンティア常に前方3000px超・32000px登坂で例外0
- [x] 高度スコア＋自己ベストが出て、死んだ時に記録が残る。→ HUD「N m / BEST N m」・結果「新記録！ これまで N m」・save.endlessBest保存。air.png/high.png目視
- [x] 難易度が高度で上がる。→ 0-250m=易(押し罰ゲート0)／1100m+=濃(elite/over/dense計18)・calmは低高度のみ。4チャンクごとに休符強制（緩急）。endless_check合格
- [x] 検証：validate回帰PASS／smoke 13画面PASS／`node tools/endless_check.cjs` 全項目PASS
- [x] 検証：ブラウザ4状態スクショ目視（ready/飛行/結果/タイトル）＝UI崩れ・フォールバック混入・絵文字残存なし（進捗バーはエンドレスで非表示を確認）
- [x] 既存コースモード回帰なし（validate全24面✓／smoke全画面✓）

## マイルストーン
1. [ ] 既存の**beat消費ループ・敵spawn・スクロール・勝敗(頂上到達)判定**を読み切る（endlessでどこを差し替えるか特定）。
2. [ ] **エンドレス生成器**：既存beatを「難易度別プール」に分類→高度に応じてプールから引いてシャッフル連結する supplier を実装（頂上=無し、無限）。
3. [ ] **モード分岐**：`endlessMode`フラグ＋起動口（タイトルのボタン and/or `?mode=endless`）。頂上到達クリアを無効化し、死亡=終了→記録保存→即リトライ。
4. [ ] **スコアUI**：高度m常時表示（既存流用可なら流用）＋自己ベスト保存/表示。★評価モーダルはendlessでは出さずスコア結果に。
5. [ ] **緩急**：一定間隔でbonus/updraft系の休符チャンクを挟む（既存typeを流用）。
6. [ ] **検証**：validate/smoke＋Nodeでの生成ループ＋ブラウザスクショ（視覚QA）。既存モード回帰確認。
7. [ ] コミット（pushはしない）。HANDOFF.md/GOAL.mdに現在地と「明日試す手順」を残す。

## 停止条件（ここに当たったら提案で止める・無人運転でも）
- **新規アートの採用・生成**（既存アセット流用のみ。新しい絵柄・キャラ・トンマナ変更はしない）。
- **公開**（push / gh-pages 反映）— コミットまで。pushはyukiyaの明示指示待ち。
- 課金・破壊的操作・不可逆な判断。
- 見た目・体験の**採用**判断が要る局面（新レイアウトの可否など）＝複数案＋比較材料を置いて提案で止める。既存UIの最小流用で崩れない範囲は実装裁量で進めてよい。

## 現在地／次の一手
- 現在地：**★完了（2026-07-06 夜）**。最小プロト実装＋全検証グリーン＋視覚QA4状態通過。未push（コミットのみ）。
- 明日yukiyaが試す手順：ハードリロードして**タイトル上部中央「エンドレス β」**をタップ→押してスタート→どこまで登れるか。死んだらリトライ即再挑戦。
- 割り切った点（実機FB後の次段候補）：
  1. **単一地域（A鳥）の絵のまま**無限。ビートの変化はA語彙内（群れ/スウープ/上かぶり/ダイブ/階段/上昇気流/タカ/密）。長く登ると単調に感じたら次段＝**高度バンドで地域背景を巡回**（B雲海→C嵐→D星脈、既存アート流用・BGM切替）。
  2. **道中コインが薄い**（bonus/updraft/gust/diveのチャンクのみ）。generateRouteCoinsは全コース事前配置専用でエンドレスでは走らないため。コアは敵回避の登坂なので割り切り。要れば「区間ごとにルートコイン生成」を足す。
  3. 入口の**タイトル「エンドレス β」ボタンの見た目採用はyukiya判断**（プロトの控えめピル。位置/文言/常設可否）。
- 検証の再実行：`node tools/endless_check.cjs`（エンドレス専用）／`node tools/validate.cjs`（回帰）／`node tools/smoke.cjs`（全画面）。
