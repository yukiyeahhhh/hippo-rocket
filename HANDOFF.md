# HANDOFF — hippo-rocket 引き継ぎ（現在地の正本）

> **運用ルール**：ここは「最新の現在地／次の一手／未解決の気になり／生きた手順」だけを保つ。古い追記は [docs/handoff-archive/](docs/handoff-archive/) へ移す（目安：現在地が5KBを超えたら）。過去ログ＝ docs/handoff-archive/2026-06-26_07-02の追記ログ.md

## 現在地（最新の追記）

> ## ★エンドレスモード（縦のまま何m登れるか）最小プロト（2026-07-06 夜・Opus。未push・yukiya実機レビュー待ち）
> yukiyaの「チャリ走みたいなモードが欲しい＝シンプルにそれが楽しいのでは」を受けた検討→**軸(横)でなく構造(スコアアタック/エンドレス)が芯**と結論。縦の物理・敵・アートを全流用したまま、ステージの枷を外して「どこまで高く登れるか」の独立モードを新設。企画1文ピッチ「どこまで高く行けるかを競う」への原点回帰。
> ### 何を作ったか（既存資産を全流用・新規アート生成なし）
> - **beat=チャンク**という既存構造を利用。ステージの`beats`配列を消費するのと同じ仕組みで、**A地域の語彙だけでチャンクを生成・シャッフルして無限連結**（`endlessNextBeat`/`ensureEndlessBeats`/`initEndless`）。視覚は鳥地域(A)を流用（`stageKey='A'`固定・BGMもstageA）。
> - **難度ランプ**：生成高度でプールを段階化（0-250m=易/250-600/600-1100/1100m+=濃）。押し罰ゲート(elite/over/dense)は高高度のみ、休符(calm/bonus/updraft)を4チャンクごとに強制＝緩急。難度は絶対高度で上げる(1400mで頭打ち)。
> - **入口2つ**：タイトル上部中央「エンドレス β」ボタン（ガレージ/行き先を経由せず即プレイ）＋`?mode=endless`（`&alt=N`で空中フレーム撮影）。
> - **UI**：進捗バー/「あとNm」はエンドレスで非表示（頂上が無い）。結果パネルは「新記録！ これまで N m」を表示。ベストは`save.endlessBest`に専用保存（コースAのbestを汚さない）。頂上到達クリアは`!endless`でガード。
> ### ★実機で見てほしい（要ハードリロード）
> タイトルの**「エンドレス β」**→押してスタート→どこまで登れるか。①素のコア（枷なし登坂）が楽しいか ②難度の上がり方は妥当か（開幕易しく→高高度で濃く）③単一地域の絵のままで単調に感じないか ④入口ボタンの見た目/位置/文言。
> ### ★検証（全グリーン）
> - `node tools/endless_check.cjs`（**新規**：エンドレス専用。32000px登坂で例外0・beats13→128無限供給・フロンティア常時前方3000px超・全高度で通れる隙間あり・難度ランプ確認）
> - `node tools/validate.cjs` 回帰PASS（既存24面✓）／`node tools/smoke.cjs` 13画面PASS
> - 視覚QA4状態スクショ目視：ready/飛行(1556m)/結果(墜落599m・新記録行)/タイトル(βボタン非重なり)。崩れ/フォールバック/絵文字なし。
> ### ★次段（実機FB後の候補・GOAL.md参照）
> - 単調なら**高度バンドで地域巡回**（B→C→D、既存背景/BGM流用＝「1回で旅する」）。道中ルートコインが薄い（generateRouteCoinsが事前配置専用のため）。タイトルβボタンの見た目採用はyukiya判断。
> ### 触ったファイル
> - `index.html`（endless状態/生成器/reset・nextBeat・spawnAhead分岐/HUD・結果/入口ボタン＋CSS/query）／`tools/endless_check.cjs`（新規）／`GOAL.md`（作業ログ）。

> ## ★ラウンド6 モーダル崩れ修正＋手触り差別化＋隠しを最長最難に＋レビュー手続き修正（2026-07-04 昼・Opus。未push）
> 実機FB6巡目6点＋**「レビュー手続きが漏れてる、フローチェックして」**への対応。Codexレビュー(REJECT2件)も修正済み。
> - **【プロセス修正・最重要】UI/描画の目視検証がゲートから抜けていた**：validate/smoke/Codexは全部ロジックで、レイアウト崩れは死角。毎回yukiyaに崩れを指摘させていた。→AGENTS.mdレビューゲートに「UI/描画を変えたら`?show=clear&pick=8`等でスクショ目視してからコミット」を明記。knowledgeの[[ゲームの自動検証]]手法4(stub→SVG再現)も既にあった＝手順が存在したのに従っていなかったのが真因。
> - **クリアモーダル崩れ**：ラウンド5で星内訳を2行足した押し出しでresultTipと内訳見出しが5px重なり。常時tipを撤去し内訳を元位置へ。`?show=clear&pick=8`でスクショ目視して重なり解消を確認。
> - **雲クラゲの縦軸強化**：横sin×縦cosの楕円運動に(DRIFT_BOB=34)。鳥の単純降下と別の"両軸で避ける"翻弄に。
> - **シャッターのトゲ**：開口の縁だけ(一番当たらない面)→バー上下面の全長に。SHUTTER_SPIKE定数で描画と判定の縦厚を連動＝「トゲに触れた=ダメージ」を厳密化(Codex指摘)。
> - **流星の差別化**：鳥(swoop)=自機Xを狙い横避け／流れ星=自機Yを狙い**水平フラットに全幅横断**し縦(高さ)避け＝直交。予告を全幅の危険ラインにし実軌道(vy=0/grav=0)と一致(Codex指摘で重力除去)。
> - **隠し(最終)を最長最難に**：normalizeで隠しを900m目標(本編最長800m超)＝「最難関が最短」の美学違反を解消。密度UP＋bonus回廊でコイン整理。
> ### ★実機で最優先確認（build 0704-1305）
> ①クリアモーダルが崩れてないか(他画面も) ②雲クラゲが縦にも動いて鳥と別の避け方になったか ③シャッターのトゲが整列し触れた所でダメージか ④流れ星が"その高さを空けろ"の縦避けとして鳥と差別化できてるか ⑤隠しステージ(設定→全解放で開く)が最長で歯応えあるか・コイン配置が自然か。
> ### ★気になり
> - B雲海の難度(drift追尾＋縦bobでさらに上がった可能性。playtestボットは新挙動に弱く不正確だが要警戒)。理不尽ならDRIFT_CHASE/BOB/数を下げる。
> ### ★ラウンド6の後続微修正（同日午後・build 0704-1455まで）
> - **マップ(地域選択)は名前だけ**に（全5コース/〜m撤去）。全5面★3で隠し解放済みの地域に⭐。
> - **評価の星を入れ子化**（★2=ノーダメ、★3=ノーダメ＋パー以内）＝独立点灯だと「被弾したが速い」で中央だけ欠けた→必ず左詰め連続点灯。クリアモーダルをスクショ目視で確認。
> - **「岩→ブロック」の本体は上かぶり(旧ペリカン)だった**（debrisでなく）。自然な岩棚が宙に浮く違和感が原因→**石ブロックの横並び**の生成アートに差し替え＋フォールバックも石ブロックのバーに。スクショ確認済み。debrisも読みやすく(大きく/影/回転緩め)。
>
> ## ★ラウンド5 ギミック存在感UP＋星の意味づけ＋隠しステージ（2026-07-04 午前・Opus。未push・統合レビュー用）
> yukiya実機FB5巡目「だいぶ面白くなった！」＋6点への一括対応。**全validate24面(隠し4含む)✓／smoke✓／新敵Node実loop✓**。
> - **雲クラゲをゲッソー的に**：ゆらゆら揺れの中心が自機Xへじわ寄り(DRIFT_CHASE=48、自機横移動~300より十分遅く必ずかわせる範囲)＝翻弄する。存在感UP。
> - **流星が自機の進路を必ず横切るように**：meteorは中央湧き＋予告0.6s中に落下点を自機Xへ寄せて狙う→ほぼ真下に落ちる(かわす)。shootingstarは予告中に横断の高さを自機Yへ寄せる。上限つきaim＝予告後は軌道固定で動けばかわせる。テレグラフ線も実軌道に合わせ修正。
> - **シャッター門にトゲ**(開口側の縁に鋸歯)＝「障害物なのにダメージ」への回答。ダメージは維持し"触れたら危険"を明示(当たり判定は不変・見た目のみ)。
> - **星の基準を明確化＆正確化**：被弾カウンタhitCountを新設(hp--3箇所全てに加算)、ノーダメ=hitCount===0(ハート回復では消えない)。結果パネルに①クリア✓ ②ノーダメ(✗なら被弾N) ③タイム(パー以内か・実タイム)を色つきで明示。コメントも実績連動＋隠し目標に。
> - **★3の報酬＝隠しステージ**：各地域の5面すべて★3で「真・◯◯」(その地域の全ギミック総合の最難関)が解放。AX/BX/CX/DX追加。STAGE_ORDERには入れず本編フローは不変(stageAfterはundefined化)。地域選択グリッドに自動で隠しカードが出る(未解放は🔒＋条件表示)。
> - **遊び方を刷新**：「空域ごとに敵が変わる」「星を集めて隠しへ(★1クリア/★2ノーダメ/★3パー以内)」カードを追加＋既存文言を現状に。
> ### ★実機で見てほしい（build 0704-1050 なら最新）
> ①雲クラゲの翻弄が気持ちいいか/理不尽でないか ②流星が実際に自機を塞ぐか・予告通りかわせるか ③シャッターのトゲでダメージ理由が読めるか ④結果の星内訳が分かりやすいか ⑤全★3で隠しが出るか(デバッグ解放で確認可) ⑥遊び方が現状に合ってるか。
> ### ★気になり/次段
> - **B雲海の難度**：drift追尾追加でさらに上がった可能性。playtestボットでB5一部100→0%。理不尽ならDRIFT_CHASE/drift数/FLOAT_RISEを下げる。
> - 隠しステージの中身は"総合remix"で最低限(各500m)。面白さの微調整・専用の絵/演出は実機FB後。★3の別報酬(機体/コスメ)も検討余地。
>
> ## ★ラウンド4 敵バリエーション＝地域ごとの配役を作り直し（2026-07-04 早朝・Opus。未push・統合レビュー用）
> yukiya実機FB4巡目「敵がどのステージも変わり映えせず動きも単調／ゆらゆら・せり上がる・横一曲線に駆け抜ける等のバリエーションが絵でも動きでも要る／鳥モチーフに固執するな／星が挙動のせいで見えない／地域選択のm表示がコース長とずれてる・BEST不要」への一括対応。**全validate20面✓／smoke✓／新敵をNodeで実loop90フレーム完走(例外なし)✓**。※Chromeスクショは環境起因で頻繁にハング＝目視は実機で。
> ### 真因（beat集計で判明。感覚でなくデータ）
> - **D星脈が鳥だらけ**(dive/swarm/elite/dense が全面)＝「星の地域なのに鳥を避けてる／変わり映えしない」の主因。**B雲海は逆に"生き物"がほぼ無い**(floatが各面1回＋気流ギミックのみ)。**地域選択の"◯m"は自己ベスト距離で、コース全長ではなかった**＝500m化しても表示が古いベストのままで「変わってない」に見えていた。
> ### 対応（アートは雲クラゲ1体だけ新規、他は既存絵＋動きで成立）
> - **新敵①雲クラゲ(drift/sprite=drifter)＝ゆらゆら大きく横揺れしながらゆっくり降下**。B雲海の主敵に。群れ同位相で揺れる＝隙間も一緒に動き必ず通れる。**新規生成アート**(マット3D・眠り顔・ラベンダー→桃/floatのクラゲと別物)。
> - **新敵②流れ星(streak kind=shootingstar)＝横一曲線に駆け抜ける弧**(重力で下カーブ)。**meteor絵を再利用**(新規アート不要)。D星脈に多数配置＝「横断の弧」の動き。
> - **float(泡)を"下からせり上がる"が読めるよう修正**：FLOAT_RISE 54→112(緩降下/静止区間ではカメラを上回り昇って見える。落下より遅く詰みでない)。Bに濃く配置。
> - **D星脈から鳥beatを全廃**→流星/流れ星/パルサー/星門に置換(星の地域＝天体タイミング面に)。**B雲海を無鳥化**(B4のマグ経済diveのみ温存)＝気流＋雲クラゲ(揺れ)＋泡(せり上がり)＋霧。
> - **地域選択のm表示をコース全長に修正＋BEST撤去**(stageLenM追加)。地域カード=「全5コース/500〜800m」、面カード=「★★☆ / 全長500m / ⏱88s」。**これで500m化が可視化＝『コース長変わってない』の根本解決**。
> - 各地域の"顔"が別に：**A=鳥(隊列/急降下/斜めダッシュ)／B=雲クラゲ(揺れ)+泡(せり上がり)+気流／C=横風+瓦礫+雷+swoop／D=流星(落下)+流れ星(横断弧)+星門+パルサー**。
> ### ★実機で見てほしい（要ハードリロード。右下 build 0704-0620 なら最新）
> `http://192.168.10.130:8124/index.html` を **Cmd+Shift+R**。①B雲海の雲クラゲがゆらゆら揺れて隙間を読ませるか ②泡が下から昇ってくると読めるか ③D星脈が鳥でなく流星/流れ星/星門/パルサーになり"星"が見えるか ④流れ星が横一曲線に駆け抜けるか ⑤地域/面選択のmが実コース長(全面≥500・後半ほど長い)と一致しBESTが消えたか。
> ### ★次段（実機FB後）
> - C嵐のswoop(鳥)を嵐固有の敵にするか(現状は"風に飛ばされる鳥"として許容)／drift・shootingstarのアート/サイズ/速度の微調整は実機で／pulsar・shutterのアート化は要否判断。
>
> ## ★ラウンド2 磨き込み・夜間バッチ3＋アート生成（2026-07-04 早朝・Opus。未push・統合レビュー用）
> yukiya実機FB3巡目への対応＋「画像素材はマスト」を受けた**Codex画像生成でのアート制作**。全validate20面通し✓／smoke✓／Codexレビューゲート通過(High1・Med1修正=キーボードで面スキップ)。
> - **【アート生成が確立】Codex内蔵image_gen(read-onlyサンドボックスで動作)→cutout.cjs透過→webp→配線**。art-bible準拠(マット・チャンキーなマスコット3D風・クロマキー緑)。生成物:
>   - **debris(飛来岩)**＝単体チャンキーな小惑星のかけら／**meteor(流星)**＝発光する氷ブルーの彗星+顔+尾／**上かぶり=横長の岩棚**(生き物→無機物の障害物に)。全て実機表示確認(debris)・スプライト配線済み。手続き描画はフォールバックに残す。
> - **コース長=全面≥500m・後半ほど長い昇順に正規化**(実機FB『300は短い/全部500以上/難しい面ほど長く』)。STAGES後に1関数で不足分を地域ギミックのフィラーで補填→全地域 500/600/600/700/800m。
> - **上かぶりをダメージ→"壁(通り抜け不可)"に**(実機FB『板でダメージ理由が不明・トゲじゃない』)。当たると上向き速度を止め脇へ促す(ダメージなし)。無敵不可も"壁"なら自然。
> - **シャッターの緑枠GO撤去**(ビーム誤認・実機FB)。閉まる時の赤い収束矢印は残す。
> - (3巡目でOK確認: B気流の上下判別=暖色▲/寒色▼、横風、クリア花火、突入splash、シャッター主役面)
> ### ★実機で見てほしい(要ハードリロード＝古い版キャッシュ疑いあり)
> `http://192.168.10.130:8124/index.html` を **Cmd+Shift+R**。①新敵の生成アート(岩・流星)と上かぶりの岩棚 ②コース長が全面≥500で後半ほど長いか ③上かぶりが壁(ダメージなし・通れない)になったか ④シャッター緑枠が消えたか。
> ### ★次段
> - 生成アートの微調整(流星の向き/サイズ等は実機で)／pulsar・shutterもアート化するか(現状は抽象的な手続き描画で可の想定)／A地域の新敵/バランスは実機確定後。
> - **アート生成パイプラインは確立したので追加素材は容易**(codex exec --sandbox read-only で image_gen→パス報告→cp→cutout→webp→ローダ&cutout files配列に追加)。
>
> ## ★ラウンド2 磨き込み・夜間バッチ2（2026-07-04 深夜・Opus。未push・朝の統合レビュー用）
> yukiya実機FB2巡目「一晩作業しきって磨き込みまくった状態に」を受けた大バッチ。**全validate20面通し✓／smoke✓／Codexレビューゲート通過（High1・Med1修正済み＝キーボードのクリア/死亡決定が遷移中に面スキップ→2ビート後+go()経由に統一）。**
> ### 実機FB(2巡目)への対応
> - **B気流の上昇/下降が判別しにくい** → **上昇=暖色(金橙)＋▲上向き矢印／下降=寒色(青)＋▼下向き矢印**に作り替え(色＋形の両方で方向を一目。`?demo=columns`で確認可)。
> - **C横風がまだぱっとしない・風に邪魔されたい** → **上かぶりの穴を風上側(風の押しと逆)に配置**＝横風に抗って隙間へ寄せる噛み合わせに(spawnOverhang windBias)。強度/WIND_SUS/teachは1巡目済み。
> - **コース長・先頭が最長のまま** → **各地域finale(5面)にクライマックス追い込みを追加して最長最難に**、A導入を短縮。結果 A400/300/300/300/500・B500/400/500/500/600・C500/500/400/400/600・D400/400/300/300/400＝**先頭の単独最長を解消・先が長く手応え**(完全に滑らかな1<2<3<4<5は次段)。
> - **クリアがダサい/おめでとう感は視覚で** → **花火(色つき放射バーストを時間差)＋星キラッ＋控えめな小さめ文字**に(大きな文字を廃止)。
> - **シャッターが他障害物と同じ・主役ステージ欲しい** → **開閉に溜め(pow整形)＋閉まる=赤収束矢印/開いた=緑枠GOの合図**で"タイミングを読む"性格を明示。**D3『星門』をシャッター主役面**に改組。
> - **突入スプラッシュもっと長く** → 2.0→2.6(つぎへも2.4)。
> - **ペリカン無敵の不整合(Option B)** → **見た目を『木の梁(障害物)』に手続き描画で作り替え**(木目+金具+黄黒警告ストライプ=くぐる合図。無敵は物理のまま=地形なので自然)。旧pelicanスプライト不使用。
> ### ★実機で見てほしい
> `http://192.168.10.130:8124/index.html`(LAN稼働中)。①B気流の上下が一目で分かるか ②C横風で穴が風上=抗う噛み合わせ ③コース長が先ほど長く感じるか ④クリアの花火 ⑤シャッターの閉/開合図＋D3の門ラッシュ ⑥上かぶりが障害物に見えるか(無敵不整合の解消)。
> ### ★次段(タスト/アートが要る＝意図的に残す)
> - **新敵(瓦礫/流星)とペリカンの"リアルAIアート"**：Codex内蔵image_genはCLI非対話での確実生成が不透明＋art-bibleのタスト確認が要る＋深夜の自律生成は世界観破壊リスク→**手続き描画で確実に成立させ、AIアート化は朝のタスト確認付きで**([[visual-asset-kit]]運用)。ペリカンは既に障害物の手続き描画で不整合解消済み。
> - **コース長の完全に滑らかな昇順ランプ**(今は先頭最長を解消＋finale最長。中盤2-4の谷は残る)。
> - **C横風×ステージのさらなる噛み合わせ**・A地域の新敵(親子連隊/反転隊)・全5面ロールアウトの詰め。
> - バランスは実機の手触り確定後(playtestは横風/気流を測れない)。
> ### 運用メモ：権限autoモード設定は次回起動で有効(今セッションはまだ聞かれる)。全て独立コミット・未push。
>
> ## ★ラウンド2 地域パッケージ・一括磨き込み（2026-07-04 夜間・Opus。未push・朝の統合レビュー用）
> yukiya「お任せ・一気に進めて朝に統合判断」を受けた大バッチ。実機FBの全指摘に対応。**全てvalidate全20面通し✓／smoke✓／Codexレビューゲート通過（Medium2件修正済み）。playtestの絶対値は非信頼(特に横風/下降ドラフトはbotが運ばれて易しく出る＝実機だけが真)。**
> ### 直したもの（実機FBの各指摘に対応）
> - **C 嵐＝横風を"felt"に**：`WIND_SUS`(機体別の流されやすさ＝ふうせん/つばさは軽く流される・コメットは踏ん張る)、既定強度1.5→1.9、初見コーチ『横風で流される！当て舵で線を保て』、風の流線を強度連動で濃く。→「押される・戦う」を体で＋説明を補った。
> - **B 雲海＝気流の柱を背骨に**：全5面をupdraft/downdraft/columns中心に再構成(高頻度)。**下降ドラフトを上昇柱と同じ視覚言語(光る柱+流れるリング)に統一し色=青紫/流れ=下向きで"兄弟"に**(バラバラ解消)。**columnsビート=上昇柱(コイン=正解)と下降柱を横並び→ルート選択を明示**。雲/霧の見た目は保持。
> - **新敵をreskinから脱却＝別挙動**：`meteor`=予告テレグラフ(落下線)→一拍遅れて高速落下＝「予告を読んで逃げる」決断／大型化・尾を長く(見える)。`debris`=横風を受けて加速/曲がる＝「風に乗る」(自機性能非依存に修正済み)。鳥と別の読みに。
> - **シャッターゲートを機能させる**：開口を左右にランダム配置(W*0.28〜0.72)→「読んで移動＋開くタイミングで抜ける」決断に(中央固定で読まず避けられた問題を解消)。
> - **コース長の並び**：導入が最長の問題をC(700→500)/D(600→400)で解消。A導入(500)はゲーム全体のチュートリアルのため許容。
> - **クリア演出を2ビート化**＝おめでとう感(祝福ジングルsfxClear＋紙吹雪＋大きな『クリア！』→間→結果パネル)。ゲームオーバー2ビートと対。
> - **突入スプラッシュを厚く**：出発を dur1.15→2.0＋『この空へ出発』＋地域の一言＋地域色(移動中イメージ)。キーボード/タップ経路も統一。
> - **上かぶり(ペリカン)の無敵不整合＝Option B確定**(見た目が生き物なのに無敵が効かない→障害物の絵に作り直す方針)。**アート生成が要るので実装は次段**(下記)。
> ### ★実機で見てほしい(felt差の判断＝これが本題)
> `http://192.168.10.130:8124/index.html`(LAN再起動済み)をリロード。①B=気流の柱を捌く感じ・上昇/下降の見分け・ルート選択・雲は残ってるか ②C=横風で押される/戦う・機体で流され方が違う・瓦礫が風に乗る ③D=流星が予告→落ちる(鳥と別)・見えるか ④シャッターが「読んで移動」になったか ⑤クリア/突入の演出。
> ### ★次段(意図的に残した＝タスト/アートが要る)
> - **アート(Codex画像生成・art-bible準拠)**：上かぶり→障害物化(流木/岩棚/雲塊)＋無敵は物理のまま／debris(岩)・meteorのリアル絵。**自律生成は世界観を壊すリスクが高く、生成物のタスト確認が要るため夜間はやらず先送り**(現状は手続き描画)。
> - **地域パッケージの全5面ロールアウト**(今はB全面＋C/Dは導入+クライマックス中心)。A地域の新敵(親子連隊/反転隊)。
> - **finale最長への完全な長さランプ**(今は導入突出を解消しただけ＝まだ後半が最長ではない)。
> - **バランス調整**は実機の手触り確定後(playtestで測れない領域)。
> ### 運用メモ
> - **権限をautoモードに設定した(~/.claude/settings.json)が"次回起動/リロード"で有効**。今セッションはまだ聞かれる。朝の新セッションからはルーティンbashが聞かれなくなるはず。
> - このバッチは細かく独立コミット(未push)。git logで各修正を追える。
>
> ## ★ラウンド2・改＝地域パッケージを実装（2026-07-04・Opus。未push・要実機レビュー）
> A案(beat重み替え)を実機棄却→**yukiya提案「1地域=1シグネチャーギミック＋地域固有の新敵＋見た目保持」を採用**し、B/C/D全地域に実装（「嵐に限らず全地域」指示）。**お任せ・一通り後レビュー方針**。
> - **新敵『飛来』streak（`3d..`系コミット）**＝汎用の横切る脅威。**手続き描画で専用アート不要**。debris(瓦礫=C嵐で風向きに乗って高速横断)/meteor(流星=D星脈で斜めに横切る)。挙動+当たり+撮影demo(`?demo=debris/meteor`)。見た目は確認済み(瓦礫=岩塊○/流星=小さめ光頭+尾、星空にやや埋もれ=要調整候補)。
> - **C 嵐＝横風ドリフトを"背骨"に**：windStr(地域別強度)を新設しC嵐のwind既定を1.5倍(体で押される)。瓦礫をC導入teach+クライマックスに配線。
> - **B 雲海＝気流の柱**：**下降ドラフト(DOWNDRAFT_ACC)を新規実装**(上昇気流と対称=押し下げられる柱、ブースト長押しで押し返す/避けて通る。灰青+下向きシェブロン)。B導入teach+クライマックスに上昇/下降柱。**雲/霧の見た目は保持**(削らない約束)。
> - **D 星脈＝流星**：D導入teach+クライマックス+D2「流星の拍」(名のとおり濃く)に配線。既存のパルサー/シャッター(タイミング)背骨に上乗せ。
> - **A 鳥＝基本(隊列teach)として維持**。
> - validate全20面通し✓(通路保証)・smoke✓・新敵の見た目スクショ確認済み。**playtestの絶対値は信用しない**(横風はbotが運ばれて易しく出る=風と戦う難度を測れない。あなたの用心どおり)。
> - **⚠️要実機レビュー(felt差の判断)**：①B/C/Dが「1文で別の面」に感じるか(気流を捌く/風と戦う/流星と拍を読む) ②新敵の手触り・見え方(流星が小さい等) ③難度。**OKなら**→全5面へのロールアウト＋新敵アート化(Codex画像生成・art-bible準拠)＋バランス調整。**ダメなら**方向修正。
> - 継続タスク：全5面ロールアウト／新敵アート／A地域の新敵(親子連隊等)／演出磨き(クリア/突入)／B案の他候補動詞。
>
> ## ★ラウンド2 A案・B地域を個性化＋方針正本化（2026-07-03・Opus。未push）
> 実機FB「小鳥・タカ避けだけで背景が変わるだけ・飽きる」に着手。**全20面をbeats頻度で動詞抽出**し、B/C地域が5面ほぼ同一(=飽きの主犯)、D地域は面内分化(=D2が面白い理由)と数で確定→[開発計画.md](開発計画.md)「★ラウンド2実行：面の動詞マップ」に正本化。
> - **B地域を面ごとに動詞を分けて実装（未push）**：B2=パルサー"待つ"／B3=フロート"下脅威をいなす"／B4=dive危険コインを"集める"経済／B/B5=teach・総合。**validate全通過＝遊べる**。純データ変更のためレビューゲート免除。
> - **⚠️バランスは実機判断待ち**：自律レベル設計で最初B4を簡単にしすぎ(脅威剥ぎすぎ)→脅威背景を戻したが、なお易しめ。**playtestの絶対値は信用しない前提**(既知の汚染バグ歴＝2値化→v3ジッターで段階化の経緯。ツール自体のレビューも候補)。効きの最終判断は実機で。
> - **C地域は未着手＝Bの手触りを実機で見てから**（同アプローチを横展開してよいかの検証点。ツール不信＋設計はタスト依存のため、Bを実機判断してからCへ）。
> - **B案(避ける以外の新動詞1〜2個)は次段エスカレーション**として開発計画に文書化済み(選ぶ/溜めて使う/集める→運ぶ/押し返す)。
> - **演出の磨き候補を積んだ**(開発計画「★演出の磨き候補」)：①ステージクリアの演出が薄い→ゲームオーバー2ビート型の"やった！"演出＋クリアジングル ②ステージ突入スプラッシュが短い→"移動中"イメージで長く。
> - **BD無敵×コース長の鋭化**を開発計画 項目2に追記(無敵で短コースを走り抜けられる=支配戦略化。項目1の後)。
>
> ## ★実機FB#9対応（2026-07-03・Opus。未push・`91ac04b`）
> 実機FB#8の答え合わせ＝①タイトル初回BGM鳴る✓ ②途中から再生消えた✓ ③上昇音は「ロケットより機械の駆動音・もっと大きく」。＋新要望＝ダメージの手触りが薄い／ゲームオーバーがあっさりしすぎ(マリオ/カービィのような"やられた"表現が無い＝実際に今の設計に無かった)。
> - **上昇音を作り直し**：ノコギリ波2本(機械音)→白色ノイズのローパス「ゴォー」(噴射)＋低音の体。音量0.05→0.14。まだ調整余地(数値だけで振れる)。
> - **被弾の手触り強化**：非致死ヒットに**シェイク(今まで無かった)**＋強い赤フラッシュ＋厚い衝撃音`sfxHit`＋粒子。**ヒットストップは死亡時のみ**(非致死だと世界dtを縮めてbot軌道=難度が変わる＋以前撤去したBDの"カクつき"再来のため。baseline不変を確認)。
> - **ゲームオーバー2ビート化**：即パネルだったのを「①やられた瞬間(止め+大シェイク+フラッシュ+ジングル`sfxDead`+大きな一言) →間1.15s→ ②結果パネル(フェードイン、出切ってからボタン)」に。落下アニメは座標系(ハイポは画面固定・世界がスクロール)が沼なのでv1では入れていない＝**もっと動きが欲しければ次段**。
> - baseline不変・validate/smoke/構文OK・Codexレビュー済み(ロジック退行なし)。
> - **続報(`60983df`・実機FB反映)**：上昇音は「ちょういい」＋点火サージ音(押し始めの勢い)を追加／被弾OK／ゲームオーバーの一言テキストは「出方・色味・演出感が微妙」→**廃止してやられた演出に集中**(モーダルは出す)。＋**ステージ→ガレージ帰還(ホームへ)も「つぎへ」と同じ尺のある章切替に**(HOME_TRANS)。Codex指摘でlastBoostSurgeのreset漏れも修正。**次の実機で見る点**：サージ音の勢い・帰還遷移の尺・ゲームオーバーの演出集中の見え方。
>
> ## ★実機FB#8対応＋回帰レビュー一斉実施（2026-07-03・Opus。全て未push）
> yukiya実機FB4点への対応と、「うまくいってた機能が壊れる」心配を受けたCodex回帰レビューの一斉実施。**磨き込みループにレビューゲートを書き戻した**（下記）。
> ### A. 実機FB#8の音・演出4点
> 1. **タイトルBGM初回無音を修正（`98c9785`）**：バッチ3のプライム方式の競合＝primeBgmAudioの遅延pause(settle)が、同ジェスチャー内でupdateBgmが採用したtitle要素まで止めていた。settleで採用済み(=bgmAudio)を素通り。「一度ステージへ行って戻ると鳴る」の正体。
> 2. **BGMが途中から鳴るを修正（`98c9785`同梱＋`b2c8b28`）**：プールのAudio要素が再生位置を保持していた→曲切替時にcurrentTime=0で頭出し。**さらにリトライ/もういちど(同ステージ=同キー)は頭出しされない残り経路**をbgmForceRestartフラグで解消。
> 3. **クリア後「つぎへ」の遷移を章の変わり目として演出強化（`3d5d849`）**：yukiya裁定=演出を厚く・着地はDESTINATIONのまま。地域データにcol追加→遷移カバーを作り替え(地域色グロー＋アクセント下線＋次の空の一言desc、名前は中盤で長く保持するプラトー、尺1.05→1.8s)。動き/尺は実機で最終調整。
> 4. **上昇スラスト音(ぶおーーー)を追加（`4a4e493`）**：ブースト長押し中だけ鳴る連続エンジン音(持続オシレーター2本+lowpass、gainで開閉、上昇速度でピッチ)。Codex指摘でengineNodesはAudioContext作り直し時に自己修復。
> ### B. レビューゲートを書き戻した（`fe5ca6f`・恒久）
> **原因＝07-03の運用簡略化(Fable離脱)でCodex別視点レビュー段が手順から抜け、ロジック退行が自動テスト(validate/smoke/playtest=幾何/バランスしか見ない)の死角をすり抜けた。** AGENTS.mdに「★レビューゲート」を追加＝**ロジック(衝突/BD/音/UI状態/セーブ)を触ったバッチはpush/公開前に、実装していない側のAIへadversarial回帰レビュー必須**（純データ変更は免除）。所見は「明確バグ=直す/設計判断=yukiya裁定」で仕分け。
> ### C. 回帰レビュー3パスの所見と対応（Codex×自分の裏取り）
> - **直した明確バグ（`b2c8b28`）**：①ダッシュのフェイズ中もシャッター「ナイス通過」加点が漏れて入っていた(稼げない仕様違反)→ガード追加。②雲海の雲敷きが`stageKey==='B'`単一判定でB2〜B5を取りこぼし→`regionForStage(...).key==='mist'`で地域全体に。③設定画面がどこから開いてもガレージ曲→settingsBackで元画面の曲を維持(サウンド設計どおり専用曲なし)。
> - **ふうせんBDの無敵撤去（`e871772`・単独revert可）**：発動直後0.36sのiframeがゲート/シャッター/落雷を貫通(ロケットで直したのと同型の残存退行)。yukiya裁定=剥がす。**手触りが変わる変更**なので実機で要確認、戻すならこのコミット単独revert。
> - **ダッシュ説明文の実装一致（`(最新)`）**：descが旧仕様「ゲート/シャッターだけ」のまま→「敵も全すり抜け・稼げない」に。
> - **裁定で"現状維持"にしたもの**：つばさのフェザーバリアが落雷を1回分として防ぐ挙動＝yukiya裁定でそのまま(1回消費の保険は雷1発を受けてよい)。
> ### 実機で見てほしい点（最大3つ）
> ①タイトル初回でBGMが鳴るか＋各画面遷移/リトライで頭出しされ「途中から」が消えたか（特にiOS Safari）。②上昇スラスト音の気持ちよさ・BGMとの被り具合（音量0.05は控えめ狙い、うるさい/物足りないの調整余地）。③ふうせんBDが無敵でなくなった手触り（弱くなりすぎ？push+小型化で足りる？）。
> ### 未解決（yukiya裁定待ち）
> - **C コメット突破がboosting必須（3016行）**：指を離すと突破時間中でも効かず被弾。今回の退行ではなく既存実装。押下中限定でよいか、リング表示と整合してるか要判断（低優先で保留）。
>
> ## ★バッチ3完了（2026-07-03・Sonnet実装ワーカー＝実機FB#7の項目1〜5）
> 全5項目を独立コミット（項目4は指示どおり2コミットに分割）で完了。各コミットで構文チェック/validate.cjs/smoke.cjs/playtest.cjs --check-baseline 全通過（baseline差分なし＝BD系はbotが落下せず計測不能という既知の死角どおり）。**未push**（実機OKが出るまで公開に載せない）。
> - **項目1 BGM開始のタップ即時再生を撤去（`04bcce7`）**：go()内の即時`updateBgm(true,opts.bgm)`と、タイトル→GARAGE発射シーケンス(setTimeout)内の`gestureAudio('garage')`即時再生が原因と特定。両方を撤去し、BGM切替は`loop()`のtrans中点（またはGARAGEへの実際の切替タイミング）だけで発火する形に一本化。go()呼び出し側の`bgm:`オプションは全て削除（参照されなくなったため）。**モバイル無音化対策はプライム方式を採用**（「タップ即時再生に戻す」代替案は不採用）＝初回ジェスチャーで全8曲を`play()→即pause()`し(`primeBgmAudio()`)、以後は`bgmPool`にキャッシュした同一Audio要素を使い回す。**懸念どおりiOS Safari実機で本当に効くかは要確認**（効かなければ代替案＝音量0から中点まで無音→中点でフェードイン、に切り替える）。
> - **項目2 ロケットBD無敵の残り貫通経路＝落雷（雷ゲート）と特定（`6c8054b`）**：node stub（browser stub＋activateBackdraft()で本物の無敵を発動→各障害物タイプに直接衝突させる機械検査）で、上かぶり板・シャッター・フロート・斜めダイブ・タカ・パルサー・落雷の7経路を再現。**結果＝上かぶり板/シャッター/敵全般はc9ba32cどおり正しく機能していたが、落雷(static雷雲のbolt判定)だけがbackdraftShieldT<=0を条件に含んだままで無敵中に素通りできていた**。敵設計.md上、落雷は「テレグラフ→決断窓→解決」のゲート系ギミック(シャッターと同じ型)であり、機体設計.md旧候補表が"敵"側に誤分類していたのが実態と食い違っていた。条件からbackdraftShieldTを除去し、無敵が効かない対象を「ゲート/ブロック/シャッター/雷」に統一。機体設計.mdの候補表・実装ログも訂正済み。
> - **項目3 つばさのフェザーバリア＝時間切れ消滅を撤廃（`56b1051`）**：backdraftWingTの時間減衰を撤去し、再点火のたびに張り直され実際の被弾で消費されるまで持続する形に変更。BD強化Lv0-3の「バリア持続時間」の伸び(+0.35/lv)は削除し、その分を「割れた時の押し返し」(kick)へ寄せた(+25/lv→+45/lv)。node stubで衝突ゼロの環境を20秒相当経過させても残存すること、実際の被弾では正しく消費されることを確認済み。
> - **項目4 ダッシュのフェイズ＝敵も含む全すり抜けへ（2コミットに分割）**：①`4048a09`＝一般衝突判定の`b.box && backdraftPhaseT>0`から`b.box &&`を外し、落雷判定にも`backdraftPhaseT<=0`を追加。node stubで7経路全てフェイズ中は無傷を確認。②`c2fa5cb`（**このコミットだけrevert可能**）＝支配的戦略化を防ぐため、フェイズ中はコイン取得判定とニアミス(グレイズ)加点を無効化。node stubでフェイズ中は取得不可・フェイズ切れ後は通常どおり取得可能なことを確認済み。ハート回収は発注文の範囲外(コイン/グレイズのみ)のため対象外。
> - **項目5 時限効果の残り時間をBDチャージリングに転用（`07e7f2e`）**：新UIを足さず、既存の半径48リングを発動中だけ残り時間の減少表示に転用。対象＝ロケット/ダッシュ/コメット/マグ/ふうせん(バランス調整の副産物としてマジックナンバー0.72を`BALLOON_SHRINK_T`定数に切り出し)。つばさのバリアは時間制限撤廃済みのため対象外＝リングは通常のチャージ表示のまま、「点灯」はdrawHippo側の常時グローで表現。node stubで`ctx.arc`(半径48)の弧の割合を横取りし、5機体全てで発動直後1.0→時間経過で減少することを確認済み。
> - **実機で見てほしい点（最大3つ）**：①iOS Safari実機でタイトル→GARAGE遷移時に本当にBGMが鳴るか(プライム方式の効き目。鳴らなければ代替案への切替が必要)。②ロケットの無敵中、落雷の縦ビームに当たると今まで通り抜けていたのが正しくダメージを受けるようになった変化。③つばさのバリアが「当たるまで消えない」手触りになったか(強くなりすぎていないか)、ダッシュのフェイズが敵も含めて完全にすり抜けつつコインだけ取れない体感、BDチャージリングが発動中に「減っていく」合図として気づけるか。
> - 次＝実機判断待ち。OKが出たらpush→gh-pages反映の対象に追加できる。

> ## ★運用宣言（2026-07-03 yukiya）：磨き込みループはFableなしで回す
> ここから先の磨き込みは**「yukiya実機FB→HANDOFFに記録→Sonnet/Codexが実装→実機」の交代制**で回す（Fableへのエスカレーションは設計分岐だけ＝ふうせんの王の当て込み・ラウンド2の面設計・カーブ第3波の構造診断）。発注文は下のバッチ3のように**この形式で自己完結**させること：症状（yukiya原文の要旨）→仕様の裁定→実装指針→検証手順→懸念点。
>
> ## ★実機FB#7（2026-07-03 yukiya実機・BD6効果の答え合わせ）＝バッチ3発注
> 対象はすべてindex.html＝**単独ワーカーで**。項目ごと独立コミット／validate・smoke・--check-baseline（BD系はbaseline不変のはず）／pushしない（実機OKが出るまで公開に載せない）。
> 1. **BGMの開始が早い**：「タイトルをワンタップした時、（まだガレージに切り替わる前なのに）ガレージのBGMが鳴り始める」→ 裁定＝**BGM開始はtrans中点（シーンが実際に切り替わる瞬間）に一本化**し、タップ瞬間の即時再生をやめる。モバイルのアンロックは「初回ジェスチャー内で全Audio要素をplay()→即pause()するプライム」方式に変更し、以後はジェスチャー外play()を可能にする。**検証**＝コード上で全遷移経路の開始タイミングを確認＋HANDOFFの経路一覧（バッチ1項目2）を更新。**懸念**＝プライム方式がiOS Safari実機で効かない可能性→効かなければ「タップ即時再生に戻すが、音量0から中点まで無音→中点でフェードイン」の代替案で違和感だけ消す。どちらを採ったか記録。
> 2. **ロケットのBD無敵中、板/ゲート系の貫通が治っていない**（c9ba32cの修正が実機で効いていない）→ 仕様の裁定は不変＝**無敵は敵のみ・地形/開閉系は物理のまま**。`?demo=over`等の撮影モードで無敵中の衝突を全種類再現し（上かぶり板・シャッター・はね橋・ブロック・雷ゲート等、b.box以外の判定経路も全部）、貫通が残っている経路を特定して塞ぐ。**どの種類で貫通が残っていたかを必ず記録**。
> 3. **つばさのフェザーバリアが時間で消えるのは変**：「当たるまで消えない仕様だと思っていた」→ 裁定＝**時間制限を撤廃し「次の被弾まで残る」**（次のBD発動で張り直し＝上書き。1ヒット限定は不変）。BD強化Lv0-3の「バリア持続時間」は「割れた時の押し返しの強さ」等へ振り替え。**懸念**＝常設保険化で強くなりすぎる可能性→実機判断。
> 4. **ダッシュのフェイズが板とシャッターしか抜けられないのは違和感が強い**：「透明化なんだから敵も含めて通り抜けられるべき」→ 裁定＝**敵も含む全すり抜けへ変更**。ただしそのままだと「ロケット無敵＋地形抜け」の上位互換になり支配的戦略化するため、**別コミットで**「フェイズ中は実体がない＝コイン/グレイズも取得できない」を追加する（1文で言える差＝ロケット「当てて押し切る攻めの無敵・稼げる」／ダッシュ「存在を消して安全に通す・稼げない」）。実機で違和感が出たら取得不可コミットだけをrevertできる形にする。
> 5. **時間制限つき効果（無敵/バリア/フェイズ等）の残り時間が見えない**：「敵にぶつかって死ぬケースが多い」→ 新UIを足さず、**既存のBDチャージリングを効果発動中は「残り時間リング」として減少表示に転用**（機体色は既存のまま）。全時限効果共通。バリア（時間無制限化後）は点灯表示のみ。
> 6. ふうせん＝触らない（王の当て込みはBD手触り確定後・Fable案件）。
> **完了したら**：HANDOFF冒頭に結果追記＋実機で見る点を最大3つ。次のFBバッチもこの形式で回す。

> ## ★バッチ2b完了（2026-07-03・Sonnet実装ワーカー＝BD採用候補6効果を全機体へ実装）
> 機体設計.md「バックドラフト再設計の型」の採用候補表どおり、6機体すべてを1機ずつ実装→検証→独立コミットで進めた。**プッシュはしていない**（発注文どおり。gh-pagesにも触っていない）。
> - **ロケット**（`c9ba32c`）：既存のguard無敵がiframeを共有しており、実は上かぶり板/シャッターまで無効化するバグを内包していたことが判明。無敵の付与先をbackdraftShieldT専任にし、ゲート系(b.box・シャッター)はbackdraftShieldTの対象から明示的に除外。無敵と透明を別フラグにする禁則を、既存機体の隠れバグとして修正した形。
> - **つばさ**（`a5e2f8c`）：「風のベール(実質無敵+敵スロー)」を廃止し、フェザーバリア(backdraftWingTを1ヒット限定保険に転用)へ置き換え。時間内に被弾しなければ不発のまま消える。ゲート/シャッターには効かない。
> - **ふうせん**（`5223066`）：既存実装(落下中の小型化+再点火の風圧)がほぼ候補表どおりだったため維持。差分は「押す力」がBD強化で伸びていなかった点のみで、push/push2としてメタへ外出しし強化対象にした。
> - **コメット**：既存のstrike実装(小鳥のswift/swoop/form限定で突破)がそのまま候補表と一致しており**無変更**（コミットなし）。
> - **マグ**（`46c53df`）：「回収+敵スロー」の敵スローが生存効果であり注意書き「マグに生存効果を盛らない」に反していたため撤去し、「回収+危険側コインの吸引強化」へ置き換え。敵スローが使っていたbackdraftSlowT/edt(実効dt)の仕組みは他に使用箇所がなくなったため完全に削除した。
> - **ダッシュ**（`4b9784a`）：「真上ブリンク(位置移動)+無敵」を廃止し、位置を動かさないフェイズ(backdraftPhaseT)へ置き換え。上かぶり板とシャッターバーの当たり判定だけ無効化し、鳥/タカ/落雷などの敵判定には一切影響しない。
> - **頻度集計**（発注文§2・機械集計、スクリプトは使い捨てなのでリポジトリには置いていない）：ダッシュのフェイズが効くゲート/シャッター系は、A領域2〜2/ステージ・C領域3/ステージ・D領域1〜4/ステージに対し、**B領域(雲海系5ステージ)は全て0個**。B系はフロート/パルサー中心でゲート/シャッター型の障害物が無いため。ダッシュは元々A4/C4推奨機体でそちらは十分な頻度がある一方、B領域で選ぶとフェイズが一度も活きないランになり得る。**配置は足していない**（発注文どおりFable/yukiya判断待ち）。コメットの小鳥突破対象は全ステージ19〜74体で頻度上の懸念なし。
> - **検証**：6コミットそれぞれで構文チェック（`new Function`）→ `node tools/validate.cjs` → `node tools/smoke.cjs` → `node tools/playtest.cjs --check-baseline` を実行し全て通過。**baselineは6コミットとも差分なし**（発注文§7の想定どおり＝BD系はplaytestのbotが落下しないため計測できない既知の死角。差分が出ていたら異常だったはずだが出なかった）。
> - **実機で見てほしい点（最大3つ）**：①ロケットの無敵がシャッター/上かぶり板で効かなくなった変化（今までは無敵中に貫通できていたが、これが正しい仕様）②つばさのフェザーバリアが「1回だけ」であることの手触り(割れる時の押し返しが弱すぎ/強すぎないか)③ダッシュのフェイズが上かぶり板/シャッターですり抜けている実感と、B系ステージで選んだ時に「使い道がない」と感じるか。
> - **保留した機体はなし**（頻度が0のB領域はダッシュの本来の得意ステージではないため実装自体は保留せず、配置追加だけを保留した）。
> - 機体設計.md「採用候補」表に実装済みマーク＋実装ログ＋頻度集計を追記済み。
> - 次＝実機判断待ち。OKが出たらこのバッチもmaster push→gh-pages反映の対象に追加できる。

> ## ★バッチ2a完了（2026-07-03・Sonnet実装ワーカー＝BDの世界停止とズーム合図の撤去）
> 実機FB「0.2秒停止＋ズームは画面のカクつきとして感じて違和感が強い。元の（停止が知覚されない）状態が良かった」を受け、5a7454f（WORLD_STOP_BD 0.09→0.2＋発動瞬間の最大5%ズームイン）を巻き戻し、さらに世界停止そのもの（0.09も含め）を廃止した。
> - **worldStop機構＝完全に未使用と確認しコードごと除去**（`worldStopT`/`WORLD_STOP_BD`の宣言、`reset()`内の初期化、`activateBackdraft()`内の発火、`loop()`内の分岐（世界更新をスキップする`if/else`構造ごと解体し中身をそのまま実行）、`draw()`内のズーム演出、を全削除）。リポジトリ内（tools/含む）に他の参照なしを確認済み。
> - **パーティクルキャップ（PARTS_CAP/SPARKS_CAP）はフレーム落ち対策として維持**（無変更）。「踏ん張り」方向の新演出は今回追加していない（素の状態に戻すのみ）。
> - **検証**：構文チェック（`new Function`）OK／`node tools/validate.cjs` OK／`node tools/smoke.cjs` OK／`node tools/playtest.cjs --check-baseline` → 基準値と一致・差分なし（演出撤去のみでバランス変更なしの想定どおり）。
> - **コミット**：本追記を含む1本（**未push**。pushはFableが行う）。
> - 次＝バッチ2a→master push→gh-pages反映→バッチ2b（BD採用候補6効果）は下の発注文どおり。

> ## ★実機FB#6続報（2026-07-03 yukiya実機・バッチ1の答え合わせ）＋バッチ2発注
> - **BGM切替＝OK確定**（旧方式戻し・ガレージ帰還とも）。**コメット＝だいぶ良くなった**。**雲＝気にならない（可）**。
> - **BDタメ（0.2s停止＋ズーム）＝不採用**：「感じるようになったが画面のカクつきとして感じて違和感が強い。元の、BD発動時も重量が発生してる感じが良かった。不要な演出だったのかも」。イメージの言語化＝**ヨッシーの踏ん張り**（世界は止まらず、力んでいる感じ）。→〈発注・バッチ2a〉**BDの世界停止とズーム合図を撤去**（5a7454f相当の巻き戻し＋停止時間も0へ。worldStop機構が完全に未使用になるならコードごときれいに除去。**パーティクルキャップ＝フレーム落ち対策は残す**）。「踏ん張り」方向の新演出は今回足さない（まず素の状態に戻す）。
> - **ふうせんD案（風読み）＝却下**：「画面の情報量が増えるだけ、かつ（機体固有の王というより）基本機能っぽい」。**方針転換＝BD採用候補6効果を先に実装し、ふうせんの王はBDの手触りが出てから当て込みで再考する**（yukiya「BD案出したあとに当て込み考える方が良い」）。
> - **push許可（2026-07-03 yukiya明示）**：未push分のpush＋**公開版（gh-pages）をmaster最新に合わせてよい**。公開反映はknowledge[[gh-pagesデプロイの落とし穴]]どおり「index.htmlと参照アセットのコピーのみ」（設計md・tools・HANDOFF・未使用statesを公開に撒かない）。
> - **実行順**＝バッチ2a（BDタメ撤去）→master push→gh-pages反映→バッチ2b（BD採用候補6効果＝承認済み発注文。実機判断が出るまで**公開には載せない**）。

> ## ★バッチ1完了（2026-07-03・Sonnet実装ワーカー＝実機FB#6の項目2/3/4/5/8）
> 全5項目を独立コミットで完了。各コミットで validate/smoke/構文/--check-baseline 全通過（baseline差分なし＝5項目とも基準値不変）。**未push**。
> - **項目2 BGM無条件切替＋ガレージ帰還バグ修正（fe12a49）**：アンロック後は`loop()`のtrans中点（sceneが実際に切り替わる瞬間）で無条件に`updateBgm(true)`を呼ぶ方式へ。**帰還バグの真因**＝`tapAt()`とDOM `bind()`が`h.fn()`の直後に無条件`updateBgm(true)`を呼んでいたため、go()が正しく渡した`bgm:'garage'`を、まだ旧scene('PLAY')のままの`bgmSceneKey()`再計算で即上書きしていた。→`if(!trans)`でスキップし、切替はtrans中点に一本化。
>   **【2026-07-03 実機FB#7-1で訂正】**：①go()内の即時`updateBgm(true,key)`は「タップ瞬間にまだ切り替わっていないシーンのBGMが鳴る」バグの元だったため撤去した。go()呼び出し側の`bgm:`オプションも全て削除(参照されなくなったため)。**現在の全遷移経路は例外なくtrans中点(またはシーンが実際に変わる瞬間)でのみ`updateBgm(true)`(強制キーなし)を呼ぶ形に統一**：①go()経由（ポーズ/死亡/クリアのリトライ・ホームへ・つぎへ・もういちど、DESTINATION出発`dst-go`、canvas版出発）＝`loop()`のtrans中点の同期のみ。②タイトル→GARAGEの発射シーケンス(setTimeout)＝旧`gestureAudio('garage')`即時再生を撤去し、`scene='GARAGE'`に切り替わる瞬間(2250ms後)に`updateBgm(true)`を呼ぶ形に変更。③DOM直接遷移(splash→title、設定/ヘルプ/ショップの各もどる、GARAGE/DESTINATIONのbind群)＝各ハンドラ内`fn()後updateBgm(true)`(scene変更と同じ呼び出しの中)。④canvasボタン全般＝`tapAt()`のfn後同期。⑤キーボード＝`keyPrimary()`末尾の`updateBgm(true)`。⑥SPLASH→TITLE自動遷移はBGM切替を通らないが初回ロード時のみ＝実害なし。⑦ミュートON→`stopBgm()`/OFF→ユーザー操作内で再開。モバイル自動再生対策は「初回ジェスチャーで全曲play()→即pause()するプライム」方式(`primeBgmAudio()`)に変更し、以後は`bgmPool`の同一Audio要素を使い回す(ジェスチャー外play()を通すため)。iOS Safari実機で効くかは要確認。
> - **項目3 BDタメ延長＋ズーム合図（5a7454f）**：`WORLD_STOP_BD` 0.09→0.2秒。＋発動瞬間にhipX中心へ最大5%ズームインし、worldStopTの残量に比例して自然に1倍へ戻る「止まった」合図を追加（シェイクと同じくHUD外のsave/restore内＝HUDには掛からない）。updateParts/updateSparksがworldStop分岐の外で実dt駆動という既存構造は不変。
> - **項目4 クリアモーダル崩れ再発の修正（ee395d0）**：真因＝内訳が「道中で拾った分」込みの**3行**になった時、3行目とボタンの間が計算上1px。パネル高さ272→292でボタンごと下げ21pxに。**再発を見落とした理由**＝`?show=clear`はコインを拾わないため内訳が最大2行にしかならず、3行の状態を再現できなかった→`&pick=N`クエリを追加し3行状態を撮影可能に（AGENTS.mdにも記載）。ヘッドレス幅の注意（実効下限約500px）もAGENTS.mdに追記。
> - **項目5 コメットBDチャージ延長（dea0cad）**：charge 0.18→0.32（約1.8倍）。**重要な発見＝playtestはこの変更を計測できない**。3方策すべて`G.pressed=true`固定（常時上昇）でbotが一度も落下しない＝BDが一度も発動しない。前後レポート（[before](docs/audits/2026-07-03_batch1-item5-comet-before.md)/[after](docs/audits/2026-07-03_batch1-item5-comet-after.md)・965c640）は全行完全一致＝その証拠。baselineも不変のため--update-baselineによる基準値変更は発生しない。体感確認は実機で。**08ツールへの申し送り**＝BD/落下系の変更を測るには「離す」を含む方策が要る（勝手に足していない＝方策追加は要件定義マター）。
> - **項目8 雲3種の配線（1fb948f）**：縦長(cloud_stream_vertical)を除く3種（wisp_far/sea_mid/cotton_near）を`drawClouds()`の画像配列対応で遠景/中景/近景に混合。読み込みフェード・高度ゲートは既存の仕組みを踏襲。webpはローダ規約に合わせsharpで生成。**発注文との差分**＝「縦長は未コミットのまま残す」とあったが、実際は4種ともCodexが391e82fでコミット済みだった。縦長はwebp化も配線もせず、png/statesは不採用候補の記録として残置（完全削除するかはyukiya判断待ち）。
> - **実機で見てほしい点（最大3つ）**：(a) ステージ→ホームへ帰還・リトライ・つぎへ、でBGMが毎回正しく切り替わるか（特にiOS Safari実機）。(b) BD発動時の0.2秒の止め＋軽いズームが「タメ」として感じられるか、くどくないか。(c) コメットで意識的に落下を挟まないとBDが撃てなくなった手応え（ぬるさの解消）と、雲の多様化の見た目。
> - 次の一手候補＝ふうせんD案の決着（機体設計.md・yukiya協議中）／BD採用候補6効果の実装（承認済み発注）／ラウンド2（開発計画.md）。

> ## ★実機FBバッチ#6（2026-07-03 yukiya実機・B2/D2＋BGM逆輸入＋BDタメ後）＋バッチ1発注
> タグ＝〈実装/バグ/設計〉
> 1. **B2＝普通（可）。D2＝おもろい**：「メインギミック（シャッター系）が安く、そのうえで他のギミックも散らしてあるのがいい」。裏返しの大きな指摘＝**ほとんどのステージが『小鳥の間をぬける・タカを避ける』だけの共通の遊び方になっていて飽きが早い**→〈設計〉ラウンド2のテーマとして[開発計画.md](開発計画.md)「★ラウンド2候補」に正本化。今回のバッチでは触らない。
> 2. 〈実装〉**BGMはステージ入場で無条件切替の旧方式に戻す**（「ステージに入ったのにタップしないと切り替わらない」のは変）。＋**ステージ→ガレージ帰還時にBGMが切り替わらない不具合**を修正。方針＝初回のユーザー操作でオーディオをアンロックした後は、シーン遷移（go()）でジェスチャー外でも切替してよい（アンロック前だけ従来のジェスチャー待ち）。knowledge[[ゲーム音響とBGM切り替え]]参照。
> 3. 〈実装〉**BDのタメ（worldDt分離90ms）は体感ほぼゼロ・何も感じない**→停止時間を知覚できる長さへ延長＋「止まった」が見える合図（一瞬の明滅/ごく軽いズーム等）を足す。世界停止中もエフェクト/音は実dtで動かす既存構造は維持。
> 4. 〈バグ〉**ステージクリアのモーダルがまた表示崩れ**（ラウンド1項目1の修正後も再発）。`?show=clear`のスクショ確認を実機相当幅で必須。
> 5. 〈実装〉**コメットのBDチャージが早すぎてぬるい**→チャージ時間を延長し「落下の助走を挟む」設計意図（機体設計.md）に戻す。前後playtest比較を記録。
> 6. 〈設計〉**コース長300m程度だと難易度をあまり感じない**→カーブ第3波の材料（面の長さも変数に入れる）。今回は触らない。
> 7. 〈設計〉**ふうせん依然弱すぎ**。C案（受けて凌ぐ王）は「頑丈さがふうせんのモチーフと合わない」でyukiya却下→D案（風読み＝ふうせん限定の予告前倒し）を[機体設計.md](機体設計.md)に起案・yukiya協議中。**今回のバッチでは触らない**。
> 8. **雲候補＝素材として可。ただし縦長の1種だけ「こんな雲あるか？」の違和感**→〈実装〉縦長を除く3種を配線（気になり#1「雲が単調」の解消）。
> **→ バッチ1発注＝項目2・3・4・5・8**。厳守：index.htmlは単一エージェントのみ／項目ごと独立コミット／validate・smoke・--check-baseline（5はバランス変更なので前後playtest＋--update-baseline、他はbaseline不変のはず）／演出・UI変更は`?show=clear`等のスクショ確認必須／pushしない。

> ## ★Codex追記（2026-07-03 BGM逆輸入＋gh-pages差分監査）
> - **実装**：`gh-pages:index.html` L843付近のBGM実装を、masterの音声ブロックへ逆輸入。8曲のパス定義（title/garage/shop/destination/stageA/stageB/stageC/stageD）と、シーン別キー選択を追加。ステージ曲は地域単位で、鳥の空域=A、雲海=B、嵐=C、星脈=Dに割り当てた。masterに無かった `assets/bgm/*.mp3` 8ファイルも gh-pages から取り込み済み（日本語名の未追跡音源は参照していないのでコミット対象外）。
> - **モバイル自動再生制限対応**：BGMの `play()` は `gestureAudio()` / `go(...,{bgm})` 経由で、click/touch/key などユーザー操作ハンドラ内だけから呼ぶ形にした。遷移カバーの中点で `scene` が変わるケースは、入力イベント中に次のBGMキーを先に渡して再生開始する。load/RAF/timeoutなど非ジェスチャー側ではBGM再生を開始しない。
> - **音量/ミュート統合**：masterの既存設定は `settings.mute` のみなので、BGMは固定音量 `0.28` で鳴らし、ミュートONで `stopBgm()`、ミュートOFF操作時にユーザー操作内で再開する。gh-pages側の `seVol` UI/保存形式は master に無いので移植しない（設定スキーマ変更になるため）。
> - **master側既存変更との共存確認**：`sfxGraze` / `BD_SFX` / `audioCtx.state==='suspended'` のresume修正 / `worldStopT` と `dtGlobal` によるworldDt系処理は残存確認済み。gh-pagesの古い音声ブロックで上書きせず、BGM管理だけを合流した。
> - **gh-pages限定のBGM以外の差分監査**：`git diff master gh-pages -- index.html` を確認。BGM以外には、遊び方文言・光るコイン/かけら/シークレット面、`ロケット`→`ふつう`改名、旧価格/旧機体性能（ふうせんhitW 1.75、ダッシュ横ブリンク等）、`settings.seVol`、リスクコイン描画、結果画面/撮影モード文言、A5解放などがあった。いずれも現masterのBD/グレイズ/バランス調整/進行設計を巻き戻す、または別機能の大きな仕様変更なので**今回移植しない**。BGM以外のgh-pages差分を黙って落としたものは無し（この監査結果に記録）。
> - **検証**：構文チェック（`new Function(<script>)`）OK、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK、`node tools/playtest.cjs --check-baseline` OK（基準値と一致・差分なし）。
> - **要実機確認チェックリスト**：①タイトル/ヘルプ/ホーム/ショップ/行き先/各地域ステージでBGMが想定曲へ切り替わるか ②設定の音OFFでBGM/SEが止まり、音ONでユーザー操作後に再開するか ③実スマホ（特にiOS Safari）で初回タップ後に実際に音が鳴るか ④遷移カバー中に曲が早く切り替わっても体験上違和感がないか（必要なら後続でフェード/タイミング調整）。

> ## ★難度カーブ調整・第2波＝B2/D2だけを締める＋playtest.cjsに進捗率判定を追加（2026-07-03・Claude）
> - **正本**：v3レポート([2026-07-03_curve-v3.md](docs/audits/2026-07-03_curve-v3.md))で明確に浮いていた**B2「霧の裂け目」35%/84%・D2「流星の拍」85%/99%（クリア率/進捗率）の2面だけ**を対象に実施。[開発計画.md](開発計画.md)「★バランス調整ラウンド1の方針」項目1（新ギミック禁止・配置と頻度の傾斜のみ）に従い、他ステージ（A/C系の全面0%含む）は一切触っていない。
> - **① playtest.cjsに進捗率追加**：`runN()`が「平均到達進捗率」(=平均到達高度÷ステージ高さ。[08要件§7.5裁定](../yukiya-private/projects/game-dev-flow/08-自動プレイテスト要件定義.md))を返すようになり、`--report`のマトリクス・Markdownに`クリア率%/進捗率%`で併記。難度カーブの合否（地域内単調非増加・地域間逆転なし）を**クリア率でなく進捗率で自動判定するセクションを`--report`末尾に追加**（ロケット・方策1固定）。クリア率は詰み/緩みの外れ値検出用として引き続き併記。コミット済み・baseline構造(playtest_baseline.json)は不変。
> - **② 診断＝B2/D2はそれぞれ地域内で1種類のビートが丸ごと欠落していた**：`STAGES`を機械集計した結果、B2は`dense`ビート(急な密な降下陣形)が0個(B/B3/B4/B5は全て1個持つ)、D2は`shutter`ビート(開閉タイミング溜め系)が0個(D/D3/D4/D5は1〜4個持つ)と判明。既存部品を地域内の他ステージ同様に配置するだけの「配置と頻度の傾斜」対応（新ギミックではない）。
> - **③ 実装＋前後計測(ロケット・方策1・N=20)**：
>   - **B2**：クライマックス中盤(`float`と`swoop`の間)に`dense`ビート(len:400)を1個追加。**35%/84% → 10%/79%**。
>   - **D2**：中盤(`elite`と2本目`pulsar`の間)に`shutter`ビート(len:250)を1個追加。**85%/99% → 0%/82%**。
>   - **実装上の学び（要注意）**：最初、密度ビートをクライマックス最後尾（ゴール前calmの直前）に足したところ、**前後の数字が小数点まで完全一致＝一切効果なし**という事態が発生した。原因＝ステージ末尾は`stageTopY()`の100m丸め＋`stageTop-70`px圏内の敵Yクランプにより、末尾に追加した分がクランプ圏に押し込まれ、botが到達する前にクリア判定が出てしまうため。**密度を追加するときはクライマックスの中盤〜前寄りに置くこと**（knowledge昇格候補＝[[ゲームの自動検証]]に「ステージ末尾への配置追加は効果が出ない場合がある」を追記する価値あり、判断はyukiya待ち）。
> - **④ 検証**：`node tools/validate.cjs`・`node tools/smoke.cjs`・構文チェック全てOK。`--update-baseline`実行→`--check-baseline`で一致確認済み。前後計測はロケット/B2・D2の単体`playtest.cjs`実行に加え、全機体×全ステージの`--report`(N=20・policy=1)で確認。
> - **⑤ カーブ判定の結果**：B/D地域それぞれの「1面だけ突出したスパイク」は解消（B2は地域内で3番目、D2はD4と近い水準に）。ただし**全体の進捗率カーブ判定は依然×不成立**＝スコープ外のA地域(51→54→86)・C地域(66→91→95)・B3→B4・D3→D4の逆進、およびA<B<C(64%<74%<86%)の地域間逆転が残存（詳細・数値根拠は[2026-07-03_curve-v3b.md](docs/audits/2026-07-03_curve-v3b.md)「残るexternalities」節）。これらは「1種類のビート丸ごと欠落」という今回のような明確な構造的根拠が見つかっていない箇所であり、次の難度カーブ調整（第3波）の候補。
> - **並行中のCodex素材作業（`tools/cutout.cjs`・`assets/sprites/cloud_*`等）には一切触れていない**（作業ツリーの未コミット差分はそのまま）。
> - **次の一手候補（yukiya判断待ち）**：①難度カーブ第3波（上記残存逆進の構造診断）②**BGM逆輸入**（下の訂正済み発注文＝gh-pages→masterのBGM移植、index.htmlを触るので単独実行）。

> ## ★playtest.cjsにv3(決定的ジッター)を実装＋項目6合格基準を再判定（2026-07-03・Claude）
> - **背景**：直前の`last`未リセット汚染バグ修正（下の追記）で全試行が完全に同一軌道になり、クリア率が0%/100%の二値にしか出せなくなっていた（[08要件定義§7.5](../yukiya-private/projects/game-dev-flow/08-自動プレイテスト要件定義.md)にFableが仕様化）。
> - **実装**：`tools/playtest.cjs`に、試行番号(trialIndex)だけをシードにした最小LCG(`jitterFor()`)を追加。①`G.hipX`に±8px(HIPPO_R=24の1/3程度)の決定的オフセット②bot反応(`decideInputs`呼び出し開始)を0〜3フレーム決定的に遅延、の2つを試行ごとに生成する。**Math.randomは不使用**＝同じtrialIndexなら常に同じ揺らぎ。`runTrial()`に`trialIndex`引数を追加し`runN()`のループindex(`i`)をそのまま渡す形＝機体/ステージ/方策には依存しない(要件通り)。exportオブジェクトに`hipX`のsetterを追加（既存の`hippoY`と同じ手口）。**index.htmlは触っていない**（読み取り専用の指示どおり）。
> - **検証1(決定性)**：`node tools/playtest.cjs 20 A --all-veh --policy=1`を2回連続実行→出力が完全に一致（node実行時のPID依存warning行を除きバイト単位で同一）。同条件なら常に同じ結果＝決定性は維持。
> - **検証2(二値→段階化)**：同条件でN=20・全機体×ステージA単体を見ると、以前は0%/100%しか出なかったのに対し、v3では例えば ロケット/B2=35%・ロケット/D2=85%・つばさ/B4=75%・つばさ/C=40% のような中間値が複数のステージで出るようになった（詳細は[2026-07-03_curve-v3.md](docs/audits/2026-07-03_curve-v3.md)のマトリクス参照）。
> - **基準値**：`--update-baseline`(N=5のまま)→`playtest_baseline.json`の全120セル中11セルが0%/100%以外の中間値（20/40/60/80%）になったことを確認＝二値ではなくなった。直後の`--check-baseline`で「基準値と一致・差分なし」を確認済み。**基準値更新+実装をコミット**(`7a84835`)。
> - **新カーブレポート**：`node tools/playtest.cjs 20 --report --policy=1`を実行し[docs/audits/2026-07-03_curve-v3.md](docs/audits/2026-07-03_curve-v3.md)に新規保存（既存の`2026-07-03_curve-after-fix.md`は上書きしていない）。
> - **項目6の合格基準（ロケット方策1・地域内単調非増加・地域間の逆転なし）をv3の数字で再判定した結果＝× 不成立**。ロケット方策1の地域別クリア率＝A `0/0/0/0/0`（平均0%）／B `0/35/0/5/0`（平均8%）／C `0/0/0/0/0`（平均0%）／D `20/85/0/0/0`（平均21%）。
>   - **B地域**：B→B2(0%→35%=20試行中7クリア)、B3→B4(0%→5%)の2箇所で逆進。
>   - **D地域**：D→D2(20%→85%=20試行中17クリア)で大幅な逆進。D2の85%はロケットの全120セル中の最高値で、「地域の導入面より2面目が明確に易しい」という設計意図と正反対。
>   - **地域間**：B平均(8%)・D平均(21%)がA/C平均(共に0%)を上回り、後半地域の方が易しいという逆転も発生。
>   - A/Cは全面0%で「非増加」自体は数値上成立するが、判別力ゼロの床効果＝実質ノーカウント（旧汚染データの「空虚な成立」と同型）。**結論＝ラウンド1項目6で"なだらかに揃った"としていたカーブは、汚染除去後の本当の数字(v3)でも実現できていない**。詳細・数値根拠は[2026-07-03_curve-v3.md](docs/audits/2026-07-03_curve-v3.md)末尾「項目6合格基準の再判定」に記載。
> - **次の一手候補（yukiya判断待ち）**：①**カーブ再調整**＝B2「霧の裂け目」・D2「流星の拍」が同地域の隣接面より著しく易しい(配置密度/通路幅がその面だけ緩い疑い)ので、この2面を最優先に実機/配置を見直す ②**BGM逆輸入**（下の訂正済み発注文＝gh-pages→masterのBGM移植、index.htmlを触るので単独実行）。どちらを先にやるかはyukiya判断待ち。
> - **並行中のCodex素材作業（`tools/cutout.cjs`・`assets/sprites/cloud_*`・`assets/states/cloud_*`・`_cloud_candidates_preview.html`）には一切触れていない**（作業ツリーの未コミット差分はそのまま）。
> - コミット：`7a84835`(playtest.cjs v3実装+基準値)・`f6fd0aa`(カーブv3レポート)。**未push**。

> ## ★playtest.cjsのトライアル間汚染バグを修正（2026-07-03・Claude）
> - **項目6の申し送り通り、`tools/playtest.cjs`の`last`未リセットを修正した**（コミット`cea0d26`）。原因＝index.htmlのゲームループは`let last=performance.now()`をモジュール読み込み時に1回だけ初期化する設計（実ブラウザはRAFのnowが単調増加するので無害）。playtestスタブは各トライアルで`now`を0から再スタートするのに`last`はリセットされないため、2回目以降のトライアルの1フレーム目に前トライアルの終了時刻からの巨大な負のdtが混入し、結果が前試行の終了状態に汚染されていた。
> - **修正内容**：exportedオブジェクトに`last`のgetter/setterを追加し、`runTrial()`内で`G.launch()`直後に`G.last=0`を強制（申し送り通りの1行修正）。**同一条件を2回連続実行し、完全に決定的な結果になることを確認済み**（`node tools/playtest.cjs 5 A --policy=1 --all-veh`を2回実行→全機体で同じクリア率/平均到達高度/平均被弾が出力）。
> - **基準値の作り直し**：`--check-baseline`で旧基準値との差分87件を確認（例：ロケット/A 40%→0%、つばさ/A4 20%→100%等、汚染除去で数字が大きく動くのは想定内）。`--update-baseline`で再生成し、直後の`--check-baseline`で「基準値と一致・差分なし」を確認済み。`node tools/validate.cjs`・`node tools/smoke.cjs`も両方OK。
> - **新レポート**：`node tools/playtest.cjs 30 --report=docs/audits/2026-07-03_curve-after-fix.md --policy=1`で汚染除去後のロケット方策1カーブを保存（既存の`docs/audits/2026-07-02_playtest-phase2.md`は上書きしていない＝新規ファイル）。
> - **`docs/audits/2026-07-02_playtest-phase2.md`の復元指示について**：`git log --oneline -- docs/audits/2026-07-02_playtest-phase2.md`で確認したところ、このファイルは現時点で既に「Phase 2のFable検収判定」を含むコミット(`150db60`)の内容と完全一致していた（`git diff 150db60 -- docs/audits/2026-07-02_playtest-phase2.md`が空）。項目6の各コミット(`4e08ca4`等)はこのファイルに触れておらず、前後の生データは別名(`2026-07-03_playtest-round1-item6-{before,after}.md`)で保存されていた＝**実際には上書きは発生していなかった**。念のため復元コミットは作らず、現状維持とした（何か見落としがあれば要確認）。
> - **項目6の合格基準（ロケット方策1・地域内単調非増加・逆転なし）を、汚染除去後の本当の数字で再判定した結果**：
>   - `2026-07-03_curve-after-fix.md`の実測＝ロケットは **A `0/0/0/0/0` ／ B `0/0/0/0/0` ／ C `0/0/0/0/0` ／ D `100/0/0/0/0`**。
>   - **文字通りの基準（地域内で増加=逆転がないか）は4地域とも成立**（どの地域も後の面が前の面より高クリア率になっていない）。
>   - **ただし中身は別物**：以前の前後比較レポート（`2026-07-03_playtest-round1-item6-after.md`）が示した「20ステージ中19が50%に揃った、なだらかな地域内カーブ」は**汚染由来のノイズだった**。真に決定的な数字では、ロケットは20ステージ中19が完全な0%（詰み）で、地域の導入面(D)だけが100%という**二値的な崩壊**になっている。「単調非増加・逆転なし」は真だが、それは「差がなくなった(ほぼ全滅)から逆転しようがない」という空虚な成立であり、**項目6が目指していた"地域内でなだらかに難度が上がる"カーブは、本当の数字ではまだ実現できていない**。
>   - **次タスク候補（yukiya判断待ち・私は直していない）**：真の数字を前提に、ロケット方策1がA〜C地域でほぼ全滅している詰みを緩和する方向の再チューニングが必要。ただし「botの絶対値で目標を決めない」（08要件§8）の原則通り、0%=即詰みと短絡せず、実機・他方策(2/3)も合わせて見てから判断すること。
> - **並行中のCodex素材作業（`tools/cutout.cjs`・`assets/sprites/cloud_*`・`assets/states/cloud_*`）には一切触れていない**（作業ツリーの未コミット差分はそのまま残した）。

> ## ★ラウンド1・項目6完了（2026-07-03・難度カーブ＋ステージ差分＝Claude）
> - **ラウンド1の全6項目が完了**。今回やったのは残っていた項目6（[開発計画.md](開発計画.md)「★バランス調整ラウンド1の方針」項目1・2＝難度カーブ正規化＋主役ギミック濃度）。4地域(A→B→C→D)を独立コミット(順にA系/B系/C系/D系)＋最後に基準値更新の計5コミット。**新ギミックは足していない**（既存beatの追加・dmul調整・長さ調整のみ）。
> - **着手前に重大な発見＝`tools/playtest.cjs`にトライアル間の汚染バグがある**。`index.html`のゲームループは`let last=performance.now()`をモジュール読み込み時に1回だけ初期化し、以後`dt=(now-last)/1000`で経過時間を出す設計（本物のブラウザではRAFの時刻が単調に増え続けるので問題にならない）。しかしplaytestのスタブは各トライアルで`now`をローカルに0から再スタートするのに、ゲーム側の`last`はリセットされないため、**2回目以降のトライアルの1フレーム目に巨大な負のdtが混入**し、そのトライアルの結果が前のトライアルの終了状態に依存してしまう（トライアル跨ぎ・ステージ跨ぎで汚染が連鎖）。検証：`G.last=0`をトライアル開始時に強制すると、同一条件のN試行が**全て同じ結果**になる（真に決定的＝実は「クリア率」という概念自体が本来存在しない。1ステージ×1機体×1方策の答えは常にクリアかしないかの二択）。この汚染により、これまでのPhase 2レポート等で見えていた「47%」「53%」のような細かい数字は**本物の難度差ではなくノイズ**だった可能性が高い（大きな方向性の判断＝前任の項目5のhitW変更等はおそらく無事＝ノイズは小さい摂動なので大きな効果の方向は生き残る）。
> - **今回はこのバグを直していない**（`tools/playtest.cjs`は自分のスコープ外と判断＝index.htmlのみ触る合意、かつ既存の全ラウンドがこのツールの挙動を前提に判断してきた経緯があるため、独断で仕様を変えるのはリスクが高いと判断）。**次にこのツールを使う人/Fableへの申し送り**＝`G.last`をトライアル開始時にリセットする1行修正で解決できる（`runTrial`内で`G.launch()`直後に`G.last=0`を追加＋`playtest.cjs`のexported文字列に`set last(v){last=v}`を足すだけ）。直すなら**基準値(`playtest_baseline.json`)は全面的に作り直しになる**（既存の値は汚染込みの数字のため）。修正するかはyukiya判断待ち＝knowledgeへの昇格候補（[[ゲームの自動検証]]に「スタブ環境でトライアルを繰り返す時はゲーム内部のクロック変数も明示的にリセットすること」を足す価値あり）。
> - **今回の作業では、この汚染を逆手に取って測定した**：①`node tools/playtest.cjs N <stage> --policy=1`（公式ツール・タスク指定のN=10）で地域内の逆進を確認・修正の効果検証に使用 ②並行して自作の簡易スクリプト（`G.last=0`を強制する1トライアルの真の決定的結果）でground truthを取り、②で方向性を決めてから①で確認、の往復で進めた。
> - **前後比較（ロケット方策1・N=10・`node tools/playtest.cjs --report`）**：
>   - **before**（[before](docs/audits/2026-07-03_playtest-round1-item6-before.md)）：A `50/50/50/50/100` ／ B `50/50/50/50/100` ／ C `50/100/100/100/100` ／ D `100/100/50/100/50`。→ 各地域末尾の逆進（A5・B5・C2〜C5・D4）と、C/D地域がA/Bより軒並み高い逆転が確認された。
>   - **after**（[after](docs/audits/2026-07-03_playtest-round1-item6-after.md)）：A `50/50/50/50/50` ／ B `50/50/50/50/50` ／ C `50/50/50/50/50` ／ D `100/50/50/50/50`。→ **20ステージ中19が50%に揃い、地域内単調非増加・地域間の逆転を解消**（合格基準達成）。Dの1本目のみ100%が残るが、地域内不等式（後の面ほど低いか同等）には抵触しない（地域の導入=teach面としては許容範囲と判断）。
>   - 具体的な編集＝**A系**：dmulをA1.3→A2 1.36→A3 1.42→A4 1.5→A5 1.68の傾斜に整理＋A5クライマックスにタカ追加。**B系**：「霧の宝庫」宣言どおりB5にcloud(elite:true=タカ)を+2・denseを380→440。**C系**：C2〜C5のクライマックスに主役(風の反転/雷雲/上かぶり)をもう1往復ずつ追加密度（Cより短い分、遭遇総数が比例して減っていたのが原因）。**D系**：D4クライマックスにパルサー/シャッターをもう1組、D/D2/D3にもタカを追加。
> - **敵の当たり判定・機体性能・経済数値は不変**（配置＝beatsの追加/dmul調整のみ）。`node tools/validate.cjs`（通路保証・重なり検査等）と`node tools/smoke.cjs`は各コミット時に全てOK。`--update-baseline`実行済み・`--check-baseline`で一致確認済み。
> - **並行中のCodex素材作業（`tools/cutout.cjs`・`assets/sprites/cloud_*`・`assets/states/cloud_*`・`_cloud_candidates_preview.html`）には一切触れていない**（コミットにも含めていない＝作業ツリーに変更が残ったまま）。
> - **残課題**：①今回は「クリア率を50%floorに揃える」ことを優先したため、C/D系の追加ビートは主に密度を高める方向の調整で、被弾高度分布（序盤スパイクの実体）までは深追いできていない＝次ラウンドで実機の被弾体感を見てから微調整の余地あり。②上記のplaytest.cjsの`last`汚染バグの扱い（直すか・直すなら基準値の作り直し）はyukiya/Fable判断待ち。③つばさ/ふうせんの機体別クリア率（--reportの他行）は今回は触っていない＝項目6のスコープ外（ロケット方策1の地域内傾斜のみが対象）。
> - **その後の実行順＝BGM逆輸入（下の訂正済み発注文）**。

> ## ★ラウンド1の中断記録（2026-07-03・セッション上限で停止／続き2回目）
> - **完了・コミット済み**＝項目1 クリア画面崩れ修正(fdc93cb)／項目2 SFXのAudioContext修正(57d8454)＋BGM診断／**項目3 BDタメ=worldDt分離(89877f0)**／**項目4 マグ体感=riskyコイン吸引線(565051b)**／**項目5 ふうせんhitW1.75→1.4+BD風圧強化(74919db)**。
> - **項目3の詳細**：`activateBackdraft()`発動の瞬間に世界(物理/敵/スポーン)だけ`WORLD_STOP_BD=0.09`秒止め、パーティクル/シェイクは実dtで継続(`updateParts`/`updateSparks`はworldStop分岐の外で毎フレーム呼ばれる＝この構造は変えないこと)。フレーム落ちの実体＝発動フレームにburst+sparkle+jetで50〜80個のパーティクルが同時発生→`parts`/`sparks`にキャップ(`PARTS_CAP=260`/`SPARKS_CAP=220`)を追加。baseline不変(演出のみ)。
> - **項目4の詳細**：数値は不変。riskyコイン(ダイブ/上かぶり脇)がマグの磁力範囲内(`c.magPull`)にある間だけ金の吸引線+`chargeGold`粒子(`emitMagPull`)を出す。通常コインには出さない(過剰演出回避)。BDの全振り吸引(即時回収)もrisky翻訳(ポップ大/粒子増)に揃えた。ロジックはnode stub(`magcheck*.cjs`スクラッチ)で確認済み、headlessスクショでは実機のコインタイミングを狙い撃ちできず断念(логикは検証済みなので実害なしと判断)。
> - **項目5の詳細**：hitW 1.75→1.4、BD風圧(`VEH_META[2].backdraft.shock`) 142→190、push係数(`activateBackdraft`内balloon分岐) 0.55/0.35→0.75/0.5。**playtest N=10(20ステージ×--all-veh)で前後比較済み**＝0%ステージが16→14に減少(A2 0→20%, A3 20→70%, B5 0→10%, D4 10→20%)、0%のまま残るステージも到達高度は軒並み上昇。B3/C/C3/C5/D3/D5はほぼ変化なし＝hitW/風圧と無関係な別要因の詰みと判明(次ラウンドで王の再定義を検討する材料)。方向性は意図どおりなので`--update-baseline`済み(`機体設計.md`のふうせん記述も同期)。
> - **項目6 難度カーブ＋ステージ差分＝完了（2026-07-03・詳細は最新の追記を参照）**。
> - index.htmlの未コミットWIPは無し（各項目を独立コミット済み・作業ツリーは他エージェントのクラウド素材作業(tools/cutout.cjs・assets/sprites/cloud_*)のみ残っているが、これは私のスコープ外＝触っていない）。
> - その後の実行順＝**BGM逆輸入（下の訂正済み発注文）**。

> ## ★Claude追記（2026-07-03 【訂正】BGMの真相＝ブランチ分岐）
> - **先の診断「BGM配線が未実装」は誤り（masterしか調べていなかった）。真相＝BGMは gh-pages（公開版）のindex.htmlにフル配線済み**（8曲・L843付近。`f1a0234 BGM候補を差し替え`・`bfd92b8 deploy:音演出調整版を公開へ反映`等がgh-pages側にのみ存在）。**masterには逆輸入されていない**＝yukiyaがスマホの公開版で聴いていたBGMは本物。開発版(master)配信のQRでは鳴らない、が「モバイルで鳴らない」の正体。
> - **教訓＝ブランチ運用違反が起きていた**：AGENTS.mdのルールは「開発はmaster・gh-pagesは公開物のコピーのみ」だが、gh-pages側で直接開発された変更が存在する。**今後gh-pagesに直接コミットしない**（公開はmasterの成果物コピーのみ）。
> - SFX側のAudioContext suspendedバグ修正は有効（コミット57d8454・masterのSE安定化）。
> - **★次タスク発注（差し替え）：gh-pages→masterのBGM逆輸入＋全差分監査**。①`git diff master gh-pages -- index.html`で差分を列挙し、BGM実装一式をmasterへ移植（今日のmaster側変更＝グレイズ音/BD音/SFX修正と統合）②BGM以外のgh-pages限定変更が他にないか監査し、あれば移植かHANDOFF記録 ③移植後、フェード/選曲の微調整はknowledge[[ゲーム音響とBGM切り替え]]どおり実機（yukiya）で。**index.htmlを触るのでラウンド1エージェントの完了後に単独で実行**。

> ## ★実機FBバッチ#5（2026-07-02深夜 yukiya・演出改善後の実機）＋ラウンド1発注
> タグ＝〈実装/設計/バグ/要実機〉
> 1. **演出改善①②③＝OK**（不自然なし）→ **masterをoriginへpush済み**（公開はgh-pages側なので未公開のまま）。
> 2. 〈バグ〉**クリア時の画面が崩れている**→要修正。※なぜ検証で漏れたか＝smokeは「例外を出さないか」しか見ておらず、演出変更時に`?show=clear`のスクショ確認をしていなかった（検証規律に追加済み）。
> 3. 〈設計〉**マグが弱く感じる**（botでは26/60と強い→体感に翻訳されていない問題と診断）。
> 4. 〈実装〉**BD発動時にカクカクする・何が起きてるか分からない・一瞬の止めがあってもいい**→worldDt分離のタメ演出＋フレーム落ち調査。
> 5. 〈バグ疑い〉**モバイル環境でBGMが鳴らない**→自動再生制限の疑いを診断。
> 6. バランス（難度逆進・ステージ差分不明瞭・ふうせん）は**新ギミックを足さず数値と配置で直す**方針で承認。
> **→ 全項目の修正方針＝[開発計画.md](開発計画.md)「★バランス調整ラウンド1の方針」が正本。次のワーカーはそれに従い、項目ごとに独立コミット＋playtest前後比較で進める。**

> ## ★Claude追記（2026-07-02深夜 ジュース監査の改善候補1〜3を実装）
> **実装済み・未push。** [docs/audits/2026-07-02_juice-audit.md](docs/audits/2026-07-02_juice-audit.md)の改善候補①②③をコードで対応。敵配置/当たり判定/物理/経済数値は不変（演出のみ）。3コミットに分割（コミット5c8fcc4/cb5ee26/8e37247）。
> - **①グレイズの演出逆転を解消**：グレイズ成功時に画面フラッシュ（白〜金のビネット風・中心から縁を暗く落として明るい空でも読める）＋軽シェイク（数px・線形減衰）を追加。頻発イベントなので基本は"小"、連続グレイズ（combo≥3＝既存の×2倍率と同じ閾値）だけ"中"へ段階化。音も`sfx('graze')`の使い回しから、オシレーター2〜3層の時差レイヤー（`sfxGraze`）へ。これで成功(グレイズ)のチャネル数が被弾以上になった。
> - **②BD発動音を専用化**：`activateBackdraft()`の`sfx('tap')`使い回しを撤去し、機体kind(guard/wing/balloon/strike/magnet/dash)ごとに2層レイヤー音(`sfxBackdraft`/`BD_SFX`)を用意。既存の色分け(金/緑/ピンク/水色)と音でも呼応。あわせて充填完了(バックドラフト発動可)の瞬間にも小さな確定音(`sfxBackdraftReady`)を追加。
> - **③危険側コイン(ダイブ/上かぶり脇)の演出差別化**：`addCoin`系に`risky`optsを追加し、`spawnDive`のコイン弧と`spawnOverhang`の危険側報酬にタグ付け。取得時、音程(専用レイヤー`sfxCoin`)・パーティクル数・ポップの大きさ/色だけを1段強める。**コイン価値(経済数値)は変更していない**＝翻訳は演出側のみ。
> - **④ヒットストップは見送り**：被弾に3〜4フレームの時間停止を足す案は、実装するとメインループのdtが物理・敵移動・スポーンに広く使われており、雑にやると「物理を変えない」の厳守事項に抵触するリスクが高いと判断し保留。次にやるなら、`loop()`内で`dt`を丸ごと差し替えるのでなく、被弾時だけ数フレーム「worldDt=0（パーティクル/シェイクは通常dt）」を作る専用の変数分離が要る（設計だけ先にやる価値あり）。
> - **機械検証**：構文OK、`node tools/validate.cjs`OK(全ステージ通過/重なりなし)、`node tools/smoke.cjs`OK。バランス回帰ゲート(`--check-baseline`)はコミット1回目のpre-commit実行時のみ大量差分(72件)の警告が出たが、**直後に手動で4回・さらに5回=計9回連続で再実行したところ全て「基準値と一致・差分なし」**。ステージ配置(`stageRand`)や当たり判定には触れておらず、原因はWIP・未コミットの`tools/playtest.cjs`(Phase2作業中/pre-existing uncommitted)側の一過性フレークと判断（再現せず）。念のため実機/次回コミット時にも警告が出ないか要確認。
> - **実機で見てほしい点（最大3つ）**：(a) グレイズ成功時の白〜金フラッシュ＋軽シェイクが「気持ちいい」か、うるさくないか（連続時の"中"段階が特に）。(b) 機体を切り替えてバックドラフトを発動した時、機体ごとに音が違って聞こえるか（sfx('tap')の使い回し感が消えたか）。(c) ダイブ/上かぶり脇の危険コインを取った瞬間、通常コインと比べて「攻めて取った」感が音・見た目で伝わるか。
> - **次の一手候補**：ヒットストップの専用worldDt設計→実装、または気になってることリストの残り（#5敵バリエ、行き先2階層など）。

> ## ★Fable追記（2026-07-02深夜 ジュース監査＝演出の照合結果）
> **レポート＝[docs/audits/2026-07-02_juice-audit.md](docs/audits/2026-07-02_juice-audit.md)（読取のみ・実装未着手）。** 核心の発見：
> - **被弾（4ch）がグレイズ（3ch・画面反応なし）より強い演出＝意図表2行目「危険のそばが一番おいしい」と逆向き**。既知の反省「単発ニアミスの手応えが弱い」の根因が特定された。
> - 危険側コインの演出が通常コインと完全同一／**BD発動音が`sfx('tap')`の使い回し**（「分かりにくい」FBの音側の根因）／シェイク・ヒットストップ実装ゼロ／SE全体が単一オシレーター。
> - **次のワーカーへ**：改善候補6件が同レポートに素材要否・工数付きで整理済み。優先はグレイズの演出逆転の解消とBD発動音（どちらもコードで済む）。実装時は[knowledge 快感演出の型]を参照。

> **文書のみ・実装未着手・yukiya承認待ち。** BD追補6連発の振動を構造診断＝[docs/audits/2026-07-02_fable-design-review.md](docs/audits/2026-07-02_fable-design-review.md)。
> - 診断＝①「離すの価値化」に仕組み5層が積層（最古層ペリカン未確認のまま）②BD試作基準「まず2機」を同日に全機体+Lv強化+経済まで拡大＝検証単位の崩壊 ③計測器(08)なしの相対チューニング。
> - 処方＝①禁則1行「位置を勝手に動かす効果は作らない」②BD強化/価格を凍結しロケット1機の基礎リズム検証へ縮小 ③08自動プレイテストPhase1をSonnetへ発注 ④ペリカンを先に閉じる ⑤ロケット操作を基準宣言。
> - **yukiyaへ：実機で見るのは2点だけ**＝(a)ロケットの離す→再点火リズムは気持ちいいか (b)ペリカンは押しっぱなしを罰しているか。
> - **★実機結果（2026-07-02 yukiya口頭・チャットのみだったのでここに記録）**：**(a)＝◯確定。「落として押す→チャージ→再点火の基礎リズムが単体でめっちゃ気持ちいい・楽しい」**＝BDの根本仮説は本物。yukiya所感「だから強化(Lv)の話もあってよいかも」。**(b)ペリカン＝◯確定（同日実機・スマホ）**＝押し罰は機能。Fableレビューの実機宿題2点は両方クローズ→**BD採用候補の実装と08 Phase 2はブロッカーなし・着手可**。
> - (a)◯を受けた処方2の更新：BD強化の**実装は維持・遊んでよい**。ただし**数値の詰め（Lvカーブ・価格・機体別の強弱）は08の計測結果が出てから**確定する（手触りで支配性は判定できないため）。
> - **★次のワーカーへの発注（BD採用候補の実装・yukiya承認済み 2026-07-02）**：[機体設計.md](機体設計.md)「採用候補」表の6効果（ロケット=敵無敵／つばさ=フェザーバリア1回／ふうせん=ノックバック・エアー／コメット=ビーム突破／マグ=全振り吸引／ダッシュ=フェイズ透明化）を実装する。**厳守**＝①禁則「位置を勝手に動かす効果は作らない」②頻度原則「効く対象の出現頻度を先に確認」（ダッシュ透明化→そのステージ帯のゲート/シャッター出現率、コメットビーム→小鳥密度。足りなければ効果でなく配置を先に足す）③無敵と透明は別フラグ ④1機ずつ実装→validate/smoke→次へ。**index.htmlを触るのでCodex/Sonnetどちらか単独で**（並行禁止）。実機の最終判断はyukiya。
> - **★08自動プレイテストPhase1＝完成・受け入れ合格（2026-07-02 Fable判定）**：`node tools/playtest.cjs [N] [ステージ] [--all-veh]`。100試行3.4秒・**完全に決定的再現**（同条件なら毎回同じ数字＝数字が動いたらゲームが変わった証拠。回帰チェックに使える）。初回実測（ステージA・方策1安全優先・N=100）＝ロケット50%/コメット50%前後/ダッシュ59%/**つばさ5%/ふうせん0%**。
> - **★解釈の注意（重要）**：つばさ/ふうせんの低さは「弱い」確定ではない。方策1（常時上昇＋近接回避）が滑空/浮遊型と相性最悪＝要件§8「botの方策と遊び方のズレ」の実例。**単一方策では機体比較が交絡する→Phase 2（方策2欲張り・方策3直進の追加＋被弾高度分布）で括弧をつけてから機体差を結論する**。機体ごとに専用方策を作るのは不可（比較不能になる）。
> - **次の発注（08 Phase 2）**：方策2〈欲張り＝最近傍コインへ、脅威は寸前回避〉・方策3〈直進〉を追加し、機体×ステージ×方策のマトリクス＋被弾高度の分布出力。これが出たらBD強化/価格の数値詰めとrerun保留の支配性判定に進める。
> - **★バランス回帰ゲート稼働開始（2026-07-02・受け入れ合格）**：index.htmlを含むコミット時に validate（拒否）→smoke（拒否）→playtest --check-baseline（**警告のみ**・約20秒・N=5）が自動実行。バランスを意図的に変えたら `node tools/playtest.cjs --update-baseline` で基準値を更新してコミットに含める。**注意＝N=5は粗い網**（クリア率20%刻み）。細かい差の確認は手動で `node tools/playtest.cjs 100 <stage>` を回す。
> - **★ゲートの使い方（基準値≠目標値）**：基準値は「現状のスナップショット」であり良し悪しの審判ではない＝**意図しない変化の検知器**。警告が出たら3択＝(a)意図した変更→--update-baselineして基準値ごとコミット (b)意図してない→直近変更の副作用を疑う (c)判断つかない→実機。**botの絶対値で目標を決めない**（下手なbotの50%≠人間の50%）。目標は相対で見る＝ステージ進行でクリア率が単調に下がる形か／安全優先botで0%＝詰み疑い。目標帯の設計はPhase 2（方策3種＋被弾分布）後にやる。

> ## ★Claude追記（2026-07-02 監査表の復元と武器要件の所在）
> - **初回監査の29項目表（×9・修正前ベースライン）を復元保存**＝[docs/audits/2026-07-02_design-audit-initial.md](docs/audits/2026-07-02_design-audit-initial.md)。チャット止まりで未保存だったのはClaude側の落ち度。現在の正はCodexの[rerun表](docs/audits/2026-07-02_design-audit-rerun.md)。
> - **rerunの処方3「支配性は簡易方策テストで検出」の道具は要件定義済み**＝`~/Documents/yukiya-private/projects/game-dev-flow/08-自動プレイテスト要件定義.md`（bot3方策×機体×ステージのマトリクス計測。Phase 1＝方策1・クリア率のみ・1ステージから）。**ファイルは欠けていない**＝このリポとknowledgeの外（yukiya-private）にあるだけ。Codexのサンドボックスから読めない場合は、本文をプロンプトに貼って渡す（AI協業の運用の型）。
> - 監査スキル本体＝同フォルダの `07-設計監査スキル.md`。監査表は今後**必ず対象リポの docs/audits/ に表ごと保存**（スキル側にもルール化済み）。

> ## ★Codex追記（2026-07-02 バックドラフト2機体試作）
> **実装済み・未push。** Gemini案と監査指摘を受け、全機体展開ではなく `ふつう` と `コメット` だけにバックドラフトを試作。
> - **ふつう**：落下中にチャージし、再点火時にショートブースト＋短い無敵。押し直しの基本リズムを作る。
> - **コメット**：短い落下チャージで、再点火後の短時間だけ小鳥を突破。従来の「最高速なら常時突破」から、落下の助走を挟む能力へ寄せた。
> - **表示**：対象機体だけ、カバ周囲に小さなチャージリングを表示。発動中はリングが光る。
> - **検証**：構文OK、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK、`git diff --check` OK、ヘッドレスChrome撮影でリングの位置確認OK。
> - **次**：実機で「離す判断が増えたか」「コメットが強すぎ/弱すぎないか」を確認。問題なければ、つばさ/ふうせん/マグ/ダッシュの固有案を1機ずつ設計する。
> - **2026-07-02追補**：ユーザー実機FB「チャージ/無敵が分かりにくく死にやすい・ふつう/コメット差が弱い」を受け、リングを太くし、落下中の吸い込み粒子と再点火時の噴射バーストを追加。ふつう=長めの金色シールド＋金炎、コメット=短チャージ＋長めの青い突破時間へ再調整。
> - **2026-07-02追補2**：他機体にも固有バックドラフトを展開。つばさ=横スリップ、ふうせん=落下中小型化＋風圧、マグ=画面内コイン回収＋敵減速、ダッシュ=横大ブリンク。演出色も機体別に分離。
> - **2026-07-02追補3**：コイン用途としてショップにバックドラフト強化 Lv.0〜3 を追加。各機体ごとに60/140/260枚で強化し、チャージ短縮＋固有効果強化。ふつうもショップに強化対象として表示。
> - **2026-07-02追補4**：ユーザー実機FBで「横へ大きく動く系は制御できず弱い」と判明。つばさBDを横スリップ→風ベール（短い無敵＋敵スロー）、ダッシュBDを横大ブリンク→真上突破へ変更。ダッシュ通常ブリンクも横暴発を止め、押し始めの上ブリンクに寄せた。ふうせん/マグの発動エフェクトを強め、機体価格を600〜1400、BD強化を300/700/1500へ上げた。初期機体名は「ロケット」へ変更。
> - **2026-07-02追補5（設計のみ）**：ユーザー実機FBで、風ベール=実質無敵、真上突破=制御不能で弱いと判明。[機体設計.md](機体設計.md) に「バックドラフト再設計の型」を追加。次実装は、ロケット=敵無敵、つばさ=1回保険バリア、ダッシュ=ゲート/ブロックだけ抜ける透明化、マグ=吸引全振り、コメット=小鳥撃破/ビーム、ふうせん=風圧に分ける。無敵と透明は別フラグにする。
> - **2026-07-02追補6**：コイン入手効率を持つ機体は高級ラインに置く方針へ。ふうせんのコイン2倍を削除して防御/盤面整理機体に戻し、マグを経済主役として1800枚、ふうせんを1000枚に調整。デバッグ全解放（設定ボタン/`?debug`/Uキー）はバックドラフトLv.3も付与するよう変更し、最大強化の手触り確認をすぐできる状態にした。

> ## ★Codex追記（2026-07-02 初回監査表を発見）
> **文書化済み・未push。** Fable/Sonnetの初回監査表らしき復元版 [docs/audits/2026-07-02_design-audit-initial.md](docs/audits/2026-07-02_design-audit-initial.md) を確認。未追跡だったため、修正前ベースラインとして保存対象にした。
> - **読み方**：`initial` は修正前の×9、`rerun` はペリカン/ハート/ボーナス休符化後の現状。今後の判断は [docs/audits/2026-07-02_design-audit-rerun.md](docs/audits/2026-07-02_design-audit-rerun.md) を正にし、`initial` は差分確認用。
> - **注意**：`docs/.DS_Store` も未追跡で出ているが、作業成果ではないため触らない。

> ## ★Codex追記（2026-07-02 監査指摘の一次対応）
> **実装済み・未push。** 監査抜粋のうち、L0/報酬で低リスクに直せる部分へ一次対応。
> - **ペリカン上かぶり**：`spawnOverhang()` の板を穴の片側ではなく中央に戻し、押しっぱなし直進を罰しやすくした。左右の逃げ場は残し、`node tools/validate.cjs` で全機体の通路保証OK。
> - **ハート**：安全地帯に1個置く生成を撤去し、危険ダイブコイン側の低頻度報酬へ移動。満タン時は既存仕様どおりコイン扱い。
> - **監査表**：消えていた29項目表の代替として [docs/audits/2026-07-02_design-audit-rerun.md](docs/audits/2026-07-02_design-audit-rerun.md) を作成。ボーナス回廊は「欲張り」ではなく「休符」として[敵設計.md](敵設計.md)に明記し、現状は○18/△11/×0。
> - **検証**：構文OK、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。
> - **次**：サイド報酬/ハートをさらに危険側へ寄せる。次いで支配的戦略の簡易方策テスト、またはバックドラフト2機体試作。

> ## ★Codex追記（2026-07-02 設計監査の未反映タスクを正本化）
> **文書化済み・未push。** Fable5/Sonnet初回監査の全文表は見つからず、knowledgeには「29項目中×9、L0に集中」という要約のみ。ユーザー共有の抜粋と現物コードを照合し、[docs/audits/2026-07-02_design-audit-followup.md](docs/audits/2026-07-02_design-audit-followup.md) に保存。
> - **要対応の核**：ペリカン上かぶりが「離す必須」になり切らず、実質スイフト同様のX合わせに寄っている。ハートは危険側報酬の設計と逆に安全地帯配置。ボーナス回廊は休符としては有効だが「欲張りの決断」ではない。シャッター/ステップは役割被りと支配的戦略を要実機確認。
> - **次の順番**：①監査スキルで29項目表を再生成して `docs/audits/` に残す ②ペリカン/ハート/ボーナス回廊のL0修正 ③その後、Gemini案のバックドラフトを「ふつう＋コメット」だけで試作。
> - **注意**：バックドラフトは方向性として良いが、L0不備を覆い隠す形で全機体へ一気に入れない。

> ## ★Codex追記（2026-07-02 結果画面の言葉と内訳を整理）
> **実装済み・未push。** ユーザー指摘「いい上昇だったが意味不明」「クリア時の集計が作り手向けで、光るコイン等が枠からずれる」を受け、結果画面の表示をプレイヤー向けに整理。
> - **やった**：クリア時の重複バナーは前コミットで撤去済み。今回、結果画面の細かい加点チップ（収集/ニアミス/コンボ/機体/速さ等）を廃止し、「道中で拾った分」「クリア」「星ボーナス」の短い行に集約。枠付きチップをなくし、文字ズレ/はみ出しを避けた。
> - **言葉**：「穴」「ニアミス」「速さ星」など作り手寄りの表現を、「広い道」「近くでよける」「時間の星」などに丸めた。
> - **次**：実機で、結果画面の余白と文言の温度感を確認。まだ説明くさければ、内訳自体をさらに減らして「コイン +N」と星演出に寄せる。
> - **検証**：構文OK、`git diff --check` OK、ブラウザで `?cap&stage=A&show=clear&alt=520` のクリアモーダルを確認。

> ## ★Codex追記（2026-07-01 磨き込み優先度基準を追加）
> **文書化済み・未push。** 直前のグレイズ連続報酬について、ユーザーから「実際にユーザーが工夫する余地がなければ無用の長物」と指摘あり。会話だけでなく [開発計画.md](開発計画.md) に「磨き込み候補の優先度基準」を追加した。
> - **基準**：①狙えるか ②選べるか ③読めるか ④頻度があるか ⑤コアを強めるか。5/5は最優先、3〜4/5は不足条件も同時に作る、0〜2/5は保留/削除。
> - **今回の反省**：連続グレイズ倍率は「攻め得」の方向性は良いが、今の遭遇頻度では「連続して狙えるか」が弱い。次に触るなら、倍率を伸ばすより先に、単発ニアミスの手応えと、狙える危険報酬配置を作る。
> - **次の一手候補**：`GRAZE ×N` 表示を撤去/弱体化するか、累計ニアミス評価へ置き換える。そのうえでA系ステージに「狙えば取れる・怖ければ捨てる」危険報酬ポイントを少数追加して実機確認。

> ## ★Codex追記（2026-07-01 グレイズ連続報酬の可視化）
> **実装済み・未push。** ユーザー要望「磨き込み進めたい」を受け、素材追加なしで手触りに効く箇所として、既存のニアミス/グレイズを連続報酬として見える化。
> - **実装**：グレイズ処理を `awardGraze()` に集約。連続3回で `×2`、7回で `×3` の小さな追加コインになり、被弾または約2.7秒空くとコンボが切れる。敵配置・当たり判定・通路保証には触れていない。
> - **UI**：プレイ中、右上のコイン表示下に `GRAZE ×N / N連続` の小さな札を連続中だけ表示。結果画面の内訳にも「コンボ」を追加。
> - **狙い**：「よける」だけでなく、危険のそばを攻めて抜け続ける動機を少し強める。経済を壊しすぎないよう倍率は最大3で短時間維持。
> - **検証**：構文OK、`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK（全20ステージ通過・見た目重なり0）。


> ## ★気になってることリスト（ユーザー発・未解決のみ。これが正本）
> 2026-06-26 ユーザーが口頭で挙げた気になり点。Codexへ口頭で伝えたが上限で文字化できていなかった分を、ここに集約＝**散らさずここを更新する**。タグ＝〈✓済/要素材=画像生成/要実機=数値チューニング/設計=方針判断〉。
> 1. **雲が単調＋急にボンって出る**：「急に出る」は読み込みフェードで一次対応✓／「1種類で単調」は雲3種(薄雲/雲海/綿雲)の配線で✓（2026-07-03バッチ1項目8・縦長1種はyukiya違和感で不採用）→**要実機**で見た目の効き確認。
> 2. **スタートから近い雲が見えすぎ**（遠い雲が見えるのはOK、近くが多すぎ）：高度ゲートで一次対応✓→**要実機**で効き確認。
> 3. **全体のAI感・はりぼて感**（背景/雲/敵/オブジェ/ゴール/ボタン/モーダル）：**コード描画で潰せる分はほぼ消化**＝ゴール決勝バナー化✓・敵スプライトに接地影✓(貼っただけ感解消)・ボタン/モーダル/タイトル/ショップは監査済で既に良好(立体ボタン/影/ツヤ有)。**残るのは要素材**＝カバ/鳥/雲スプライトそのもののAI感(再生成しないと根本解決しない)＝Codexの画像生成案件。pad/地面は現状許容範囲。
> 4. **装備選択が崩れてる→カルーセルで選択中を大きく**：✓対応済（中央大プレビュー＋左右薄＋‹›、`?screen=SELECT&debug`で確認済）。
> 5. **鳥/障害物のバリエ・配置・ステージ増にまだ伸びしろ**：未着手。**★ルーティング指示＝敵バリエは従来Claude起点が多かったが、次はCodex起点の案を見たい**（CodexにキックオフでこのNo.5を渡す）。
> 6. **お金のマークが変**：`¢`→丸コイン描画に置換✓。
> 7. **クリア後カバがゴールでぴくぴく**：停止処理で✓（boost切/速度0/bob0/cleared時idle）。
> 8. **BGM/SE作る（Geminiで予定）→良いBGMリスト欲しい**：ステージとは別に**ショップ/装備/設定・ポーズも個別BGM**にしたい。候補は下の「Gemini向けBGM候補」に整理済み＝そのまま渡せる。
>
> **追加バッチ（2026-06-26 #2／Claudeが改善ループで対応）**
> 9. **装備画面の背景雲がフェードしない（popする）**：✓対応（drawMenuCloudsに入場フェード、sceneT基準でふわっと下から）。
> 10. **雲海ステージが突入後に初めて雲描画される**：✓対応（B限定で高度ゲートに下限を入れ突入直後から雲を敷く）。
> 11. **出発ボタン後の画面切替が唐突＝フリゲっぽい**：✓対応（`go()`遷移カバー＝カバーin→中点で実切替→カバーout、ロケット閃光。ゲーム⇄メニュー境界に適用）。
> 12. **ふうせんが「1.5倍だが1枚ずつ拾うので無意味なデメリット機体」**：✓対応（coinVal()で拾得時にライブ適用＝ふうせん2倍、+2ポップ表示。最終バランスは**要実機**）。
> 13. **機体の種類が少ない**：**方針確定(2026-06-26)**＝コビトカバ路線継続・**動物スキンは保留**・**こつぶ廃止✓**・**装備ごとに画像を書き出す**。**各装備の落下パニック画像＝✅完了(2026-06-26)**＝つばさ/ふうせん/コメット/マグの`veh_*_fall`をCodex生成(fall.png主参照でトーン統一)→cutout→drawHippoが状態別スプライトを自動使用(中央基準描画・左ズレ修正済)。`?cap&veh=N&pose=fall`で確認可。**残る不足画像＝boost/hit差分**(同方式で量産可、参照をboost/hit.pngに替えるだけ)。新機体そのものを増やすなら[機体設計.md]「★候補ブレスト拡張/次に出す機体」(ダッシュ/バブル＝発注文確定済)。
> 14. **コイン以外の収集物・強化・回復**：**収集→開放を採用(2026-06-26)**＝コイン(ショップ装備)と別トラックで、ステージ毎の隠し収集物コンプ→ショップ外の特別装備/(将来)動物スキン解放。設計＝[開発計画.md](開発計画.md)「★収集→開放(2トラック進行)」。強化要素の方向(rare回復ハート/グレイズ倍率可視化○、恒久強化×)も同doc。実装は装備カタログ確定後の中規模機能。
>
> **実機プレイFBバッチ（2026-06-26 #3／ユーザーが実機で触っての感想）** タグ＝〈実装=コードで直せる/設計=方針判断/要研究/新規=新画面・機能/要素材=画像生成〉
> ＝＝タイトル＝＝
> 15. **タイトルが上すぎ→設定⚙が隠れて押せない＋⚙がダサい**：〈実装+要素材〉タイトル位置を下げ⚙の当たり/位置を確保、⚙をKV画風へ再デザイン。
> 16. **ボタン内の文字が全体的につぶれてる**：〈実装/要素材〉文字込み画像ボタンの可読性。DOM文字に戻す or 画像を作り直す。
> 17. **遊び方ページが欲しい**：〈新規〉How-to-play画面をフローに追加（TITLEから）。
> 18. **出発演出が中途半端**（飛ぶ→出発ボタン再表示→カクッと次へ）。理想＝押す→タイトル/ボタン消える→炎ごごご→上へ飛び去る→ホワイトアウト→次へ：〈実装/演出〉発射シーケンスを作り直す。
> ＝＝雲上ガレージ＝＝
> 19. **「行き先をえらぶ/ショップ」のボタンの大きさ・デザインがダサい。ショップが小さい・並列が筋悪いかも**：〈設計+実装〉フッターのボタン階層を再設計（並列をやめる／主従を明確に）。
> ＝＝行き先をえらぶ＝＝
> 20. **マップ絵にモーダル(カード)が重なって背景が見えない**：〈設計+実装〉カードが背景を覆っている。シーン背景を活かす配置/透過に。
> 21. **行き先4つそれぞれに“複数マップ”を**（群れの空を押す→群れの空にズーム→ステージリスト→ステージ選択→突入）：〈設計/大きめ〉DESTINATIONを「地域→サブステージ」の2階層に。フロー＆コンテンツ拡張＝要設計（画面遷移.md更新要）。
> 22. **行き先ページのボタンもダサい**：〈実装〉#16/#19と同根（coralパレット/画像化で一括）。
> ＝＝ステージ(PLAY)＝＝
> 23. **開始時にゴール→カバまでカメラを下げてマップ全容を見せるイントロビュー**が欲しい：〈実装/演出〉ステージ導入フライオーバー（突入時に上から下へパン）。
> 24. **クリア時モーダルのボタンがずれてる**：〈実装/バグ〉RESULTモーダルのボタン位置を修正。
> 25. **コイン配置をもっと“誘導”っぽく＋基本は増やす**：〈要研究済→設計〉下記マリオ知見で配置を再設計。
> 26. **ステージ突入時の切替(トランジション)画面が短すぎて役割を果たせてない**：〈実装〉尺と内容を見直す（次の空の名前/ローディング感）。
>
> **★マリオのコイン配置＝調査メモ（#25用・出典 [gamedeveloper SMB3](https://www.gamedeveloper.com/design/super-mario-bros-3-level-design-lessons-part-1) / [gamedev.net](https://www.gamedev.net/tutorials/game-design/game-design-and-theory/super-mario-bros-3-level-design-lessons-part-2-r4707/)）**
> ①**道標**＝コインは進ませたい経路に沿って“線(トレイル)”で置く（斜めトレイルでジャンプ/移動を誘導）。②**自然な軌道上**＝ジャンプの放物線＝“うまく動けば自然に拾える”位置。③**リスク/リワード**＝危険な所(崩れ床・敵際)に置くと挑戦が報われる（hippoの**グレイズ**と相性◎）。④**先読み/誘導**＝安全レーンや分岐・秘密をコインで予告。⑤**量は多め**＝達成感＋一定数でご褒美。
> → hippo-rocket適用：**上昇の“抜け道”に沿ってコイン列**を置き敵の隙間へ誘導／**グレイズ際にコイン**を置いて攻めを報酬化／全体的に**増量**。ばら撒きでなく“線と弧”で配置する。
>
> **✅ バケットA（見た目クイック修正）対応済み（2026-06-26 #3 Claude）**：#15タイトル位置下げ＋⚙クリーン化(白丸/空青アイコン)／#16 CTAを画像→crispなcoral DOM文字／#19ガレージのフッターを主従縦積み(行き先=全幅主役・ショップ=小さい副)／#20行き先カードをガラス調(blur)にして島背景を透過／#22行き先ボタンcoral(Codex既済)／#24結果モーダルのボタンを左右対称(W/2±70・幅132)＋主ボタンcoral。
> **✅ バケットB（演出・アニメ）対応済み（2026-06-26 #3 Claude）**：#18発射シーケンス作り直し(出発押下→ロゴ/ボタン消える→炎噴射プルーム+ラムブル→カバ画面外へ→ホワイトアウト#whiteoutでGARAGEへ切替・go()被り廃止)／#26突入トランジション延長(0.42→1.15s)＋次の空の名前表示(go()にlabel/sub)／#23ステージ導入フライオーバー(突入カバー後にゴール高度→カバへcamOffsetをease-in-out・タップで素早く詰める)。**※#18#23は実機で手触り確認推奨**(クリック/ready起点でヘッドレス動画確認不可)。
> **残り**＝C行き先2階層(#21・要設計)／Dコイン配置(#25・マリオ知見で線と弧+増量)／E遊び方ページ(#17・新規)。**“ダサい”質感系は[[UIの構図は先人の型から起こす]]の原則どおりCSSでなく画像生成で根治**＝CTA/看板/小物のKV画風素材化が次の質感タスク。
>
> **実機FBバッチ（2026-06-27 #4／ユーザー実機・Claudeが構造バグを一括修正）** タグ＝〈実装=CSS/canvasで直る構造バグ／要素材=画像生成／設計=方針判断〉
> ＝＝今回Claudeが修正済み（コミット f944d37〜直近）＝＝
> 27. ✅ **GARAGEでコイン表示と⚙が完全に重なる**：戻る/⚙を左に並べ、coinを右上単独に。
> 28. ✅ **DESTINATIONの二重フレーム**：`.dstcard.stage`/`.dstpick`がborder-image(雲パネル)＋背景の二重重ね→単層の角丸ガラスに統一。
> 29. ✅ **選択中ステージが分からない**：`.dstcard.stage`(2クラス)が`.on`を上書きしてcoralにならず→3クラス`.dstcard.stage.on`で明示。
> 30. ✅ **ステージ文字の白抜き/黄色が見えない**：`.dstmeta`が金(#FFCB3E)で明パネル上不可視→濃色に。region名は濃色＋淡ハロ。
> 31. ✅ **サブステージが二重フレーム＋縦長**：min-height 156→78pxに圧縮し“ズームした一覧”感に。
> 32. ✅ **「これで出発」改行**：ボタンにwhite-space:nowrap。
> 33. ✅ **設定の進行データ説明文がはみ出す**：中央寄せ明示＋短文化。
> 34. ✅ **SHOP看板が大きすぎ**：border-imageスライス幅を細く＋棚位置を上げ商品スペース回復。
> 35. ✅ **遊び方の図解(穴/かすめ/コイン)でカバと鳥/コインが接触**：図箱を92×62→122×82に拡大し、両端/すきま/斜め線で必ず離す配置に。
> 36. ✅ **初期配置の鳥が同一Yへ潰れて重なる(特に小鳥)**：突入事前配置で`aheadSpawnY`が全spawnを画面上端クランプ→開始付近の複数陣形が同一Yへ。preSpawning中はクランプ除外し真Yに展開。validateに過密スタック検査追加。
> 37. ✅ **TITLEの出発するボタンが下すぎ**：bottom 48→96。
> 38. ✅ **敵/雲の貼り付き感**：霧バンクを呼吸ゆれ、鳥に羽ばたき傾き、雷雲にドリフト＋**落雷(13%周期のギザギザ＋フラッシュ)**。`?cap&demo=storm&flash`で確認可。
> ＝＝未対応（残す）＝＝
> 39. ✅ **行き先の地域ネームプレートを“島の位置”に置く**：背景4島の座標(`REGION_POS`)に絶対配置＝島が見えたまま名前が島に乗る。下部2×2ドックは廃止。
>
> **実機FBバッチ（2026-06-27 #5／DESTINATION再設計＋細部。Claude）** ＝下記すべて✅対応済み（コミット済み・未push）
> 40. ✅ **地域プレートが島と被って下2島が見えない**：島座標へ絶対配置＋プレートを小型ピル化＝島が見える。
> 41. ✅ **地域未選択なのに「鳥の空域」が選択状態**：事前`.on`ハイライト廃止（タップで決まる）。
> 42. ✅ **星/「x/x OPEN」/代表が紛らわしい・店舗っぽい・不要**：撤去し名前＋最小状態（ベスト/ロック）のみに。
> 43. ✅ **サブステージで機体を変えたい**：dstpickに‹›を追加＝ガレージに戻らずその場で機体変更(`dstVeh`)。
> 44. ✅ **地域→サブステージの階層が伝わる演出**：ズームイン(`dstZoom`)＋プレートpop(`platePop`)。
> 45. ✅ **設定のサウンド/感度ラベルが右ズレ**：`coinHud`が`textAlign='left'`のまま戻さず後続が左寄せ→末尾で中央復帰。
> 46. ✅ **SHOP看板が戻るボタンと窮屈**：中央バナー化して距離を確保。
> 47. ✅ **クリア後つぎへのスプラッシュが短い/古い**：他遷移に合わせ次の地域名を見せる尺ある`go()`に。
> 48. ✅ **遊び方がタイトルからしか開けない**：GARAGE右上に`?`追加＋`helpBack`で開いた元へ戻す。
> 49. ✅ **つばさの落下スプライトが飛行絵よりデカい**：`fall.bodyH 0.66→0.90`で同寸に。撮影モードもフライオーバー除外。
> ＝＝未対応（画像生成=Codex待ち / 設計判断）＝＝
> 50. 〈要素材〉**設定画面の背景が古い**：DOM化 or KV画風背景の生成（質感タスク）。
> 51. 〈設計/要素材〉**地域ごとの特色を立てる**：地域別の敵/ギミックのセット＋同挙動でも別モチーフ画像＋地域別プレイ背景＝[敵設計.md]「★地域別の特色＋幅出し提案」に整理。**着手の主タスク候補**。
> 52. 〈設計〉**高難度でも上昇の爽快感を残す面/区間**：開けた空＋上昇トレイル＋スピードゲート＝[敵設計.md]同節に。
> 53. 〈設計〉**機体差別化（コメット弱い→最高速で小鳥を弾く等）**：[機体設計.md]「★機体差別化の強化 提案」に。次の機体追加とまとめて。
>
> ## ★戦略メモ（2026-06-27 ユーザー発・方向性）
> - **マップ(ステージ配置/コイン譜面)は最終的に“人が手作業で調整”した方がよい節がある**＝そのための **マップ制作用パレットツール**（敵/コイン/ギミックをグリッドに置いて譜面を書き出す簡易エディタ）を作ることになるかも。固定譜面化(`stageRand`)とstageデータ構造は既にあるので、それを編集→書き出すツールは現実的。
> - **ただし優先度はその前に「敵・ギミックの幅出し」**（種類・挙動・組み合わせのバリエーション拡張）。気になりリスト#5（敵バリエ・配置・ステージ増＝Codex起点の案がほしい）と直結。**＝次の主タスク候補はマップツールより“敵/ギミックの幅出し”**。
> - 設計の置き場：敵の幅出しは [敵設計.md]、ツール化は将来 `tools/` に。方法論は knowledge [[ゲームの中身を設計する順序]]（コア→部品→文法→ステージ）。
> - **2026-06-27追補（地域別の特色）**：地域ごとに敵/ギミックのセットを変え、**同じ挙動でも画像モチーフを地域別に差し替え**、背景も地域別に描き直す＝[敵設計.md]「★地域別の特色＋幅出し提案」に実行手順（部品→見た目→配置）を記載。**マップ設計自体（配置）は手作業＋将来ツールなので後**＝敵/ギミックを出すことと画像発注を先に。雷雲は“演出のみ”から**当たり判定のある落雷ギミック**へ昇格候補。**高難度でも上昇の爽快感を残す面**（開けた空＋上昇トレイル＋スピードゲート）も同節。**機体差別化（コメット強化）**は[機体設計.md]提案に。これらは画像生成＝Codex上限回復後 or 挙動だけ先行実装可。
>

## ゲームのコア（確定）
- **1文**：重いコビトカバがロケットで上を目指す縦スクロール。ブーストで昇り、**あえてエンジンを切って落下し**、鳥などをかわして高度を競う。
- **毎秒の駆け引き**：ブースト（高度↑だが上の障害に突っ込む）/ 落とす（避ける・距離を取るが高度↓と下の危険）。
- **設計の決定**：コストは1軸（位置）に絞る＝**燃料は入れない**。横位置固定＝左右向きスプライト不要。
- **キャラ表現はマスト**：状態ごとに体勢/表情が変わる（企画.md参照）。「クワッ大口」は**無敵/パワーアップ専用**に予約。

## 確定した素材・絵柄
- **絵柄**：3Dプリレンダ風の2Dスプライト（マットなマスコット、写実NG）。詳細 [art-bible.md](art-bible.md)。
- **主役マスター＝ `assets/ref/hero_master.png`（cand3：ぽっちゃり・キリッ）でロック済み**。以後すべてこれを `-i` 参照に一貫生成。
- **完成済みスプライト**：`assets/sprites/{idle,boost,fall,hit}.png`（透過・切り出し済み）。元の緑背景版は `assets/states/`。
- 学んだ外れ（art-bibleのルーブリックに反映済み）：**白目＝人間っぽい→黒目ドット維持／細長→チョンキー維持／大口開け＝きもい（無敵専用）／焼き込み炎は身長正規化でカバが小さく見える→炎なしで出し炎はコード**。


## 素材パイプライン（検証済みの叩き方）
- **生成（Codex内蔵画像生成・ChatGPT枠＝無料）**。マスターを参照に：
  ```
  printf '%s' "<英語プロンプト+chroma green bg+No text>" | codex exec --skip-git-repo-check \
    -C "/Users/yukiya/Documents/hippo-rocket" -s workspace-write \
    -i "/Users/yukiya/Documents/hippo-rocket/assets/ref/hero_master.png"
  ```
  - 生成先 `~/.codex/generated_images/<id>/*.png`。**最新フォルダから `cp` でrepoへ取り込むのは呼び出し側（Claude）でやる**。
  - SKILL.md読込で `windows sandbox: spawn setup refresh` エラーが出るが**無害**（生成は通る）。
- **透過抜き＋切り出し**：`node tools/cutout.cjs`（クロマキー緑→透過、bbox切り出し。sharpは hippo_fall の node_modules を絶対パス参照）。出力 `assets/sprites/`。
- **★ルール：画像を生成したら必ずプレビューを開く**。`assets/.../_preview.html`（`<img>`グリッド）を作って `Start-Process` で開き、人が並べて選ぶ。AI自身も Read で目視。
- **身長正規化の注意**：表示高さは index.html の `DRAW_H` で調整。焼き込み要素（炎等）があると枠が伸びてカバが小さく見える＝**炎なしで出して炎はコード**が鉄則。

## チューニング箇所
- [index.html](index.html) 冒頭の定数：`GRAV/THRUST/V_RISE/V_FALL`（手触り）、`DRAW_H`（表示サイズ）、`HIPPO_R`（当たり）。

## 関連ナレッジ（汎用手法・knowledge vault）
このゲーム固有でない手法は `~/Documents/knowledge` にある：
- ゲーム発想の進め方（目的→決断→操作→検証→可視化）
- ゲームの面白さの型（駆け引きの型と実例・審問リスト）
- 動画駆動開発（コンセプトを動画で可視化）
- Codexで画像生成する／visual-asset-kit スキル（素材の一貫量産）

## git
- hippo-rocket・knowledge とも全コミット済み・**未push**。push はユーザー指示時のみ。
