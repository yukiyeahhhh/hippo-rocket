# HANDOFF — hippo-rocket 引き継ぎ

> ## ★Codex追記（2026-06-29 設定画面に全解放ボタン追加）
> **実装済み・未push。** ユーザー要望「デバッグメニューで全ステージ解放・全機体解放を設定画面にボタン作って」に対応。
> - **実装**：設定画面に「デバッグ」ブロックと `全解放する` ボタンを追加。押すと廃止枠こつぶ以外の全機体、全20ステージ、テスト用コイン9999枚以上を解放する。
> - **検証**：構文OK、`node tools/smoke.cjs` OK。`smoke.cjs` に `SETTINGS_UNLOCK_ALL` チェックも追加済み。

> ## ★Codex追記（2026-06-29 サブステージ5枚表示の窮屈さ対策）
> **実装済み・未push。** 各地域5本化でDESTINATIONのサブステージカードが縦に詰まりやすくなったため、一覧をスクロール対応にし、カード高さを少し圧縮。
> - **修正**：`.dstgrid.stages` に縦スクロールを追加し、`.dstcard.stage` の最小高さ/余白を微調整。通常は5枚が収まり、文字量や端末差で溢れても選択できる。
> - **検証**：構文OK、`node tools/smoke.cjs` OK。

> ## ★Codex追記（2026-06-29 解放移行の回帰チェック追加）
> **実装済み・未push。** `tools/smoke.cjs` に `UNLOCK_MIGRATION` チェックを追加。初期解放 `A/B/C/D` が勝手にサブステージへ広がらないことと、同一地域内の途中解放だけ補完されることを自動確認する。
> - **検証**：`node tools/smoke.cjs` OK。

> ## ★Codex追記（2026-06-29 解放移行の開きすぎを修正）
> **実装済み・未push。** 20ステージ化後に、各地域の第1ステージ初期解放と `normalizeUnlockedStages()` が噛み合って、初期セーブでもA〜C地域のサブステージが開きすぎる可能性を発見して修正。
> - **修正**：移行補完をグローバルな `STAGE_ORDER` 最奥ではなく、`STAGE_REGIONS` の各地域内だけで見るよう変更。`A/B/C/D` だけ解放済みならその4本だけ維持し、`A4` まで持っている既存セーブならA地域だけ `A2/A3` を補完する。
> - **検証**：構文OK、移行ロジックのnode単体チェックOK、`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。

> ## ★Codex追記（2026-06-29 サブステージを各地域5本へ拡張）
> **実装済み・未push。** ユーザー案「サブステージ追加（2⇒5にするぐらい）」に到達。各地域5本、合計20ステージにした。
> - **追加ステージ**：`A5 群れの頂`（鳥の空域総合）、`B5 白霧の宝庫`（霧＋フロート＋マグ回収の総合）、`C5 嵐の目`（風向き/雷雲/上かぶり総合）、`D5 星脈終端`（パルサー/シャッター/スウープ総合）。
> - **狙い**：4本目が機体の強みを試す面、5本目が各地域の総合テスト。ここから先は面数追加より、実機で薄い面のbeats調整や、ステージ別背景/敵スプライトの見た目差を付ける方が効きそう。
> - **検証**：`node -e "...new Function(<script>)..."` OK、`node tools/validate.cjs` OK（全20ステージ通過・見た目重なり0）、`node tools/smoke.cjs` OK。

> ## ★Codex追記（2026-06-29 サブステージを各地域4本へ拡張）
> **実装済み・未push。** サブステージ追加の続きとして、各地域に4本目を追加し、合計16ステージにした。今回は「機体の強みを試す面」を混ぜる方針。
> - **追加ステージ**：`A4 はね橋ジャンプ`（ダッシュの上/斜めブリンク練習）、`B4 磁気雲道`（マグの吸引で霧中コイン線を拾う）、`C4 稲妻ブリンク`（風＋雷雲をダッシュで切る）、`D4 彗星航路`（コメットの速度/小鳥突破と、壊せない星門の待ち）。
> - **解放順**：`A → A2 → A3 → A4 → B → B2 → B3 → B4 → C → C2 → C3 → C4 → D → D2 → D3 → D4`。既存セーブ移行は前回追加の `normalizeUnlockedStages()` がそのまま効く。
> - **検証**：`node -e "...new Function(<script>)..."` OK、`node tools/validate.cjs` OK（全16ステージ通過・見た目重なり0）、`node tools/smoke.cjs` OK。

> ## ★Codex追記（2026-06-29 ダッシュを上/斜めブリンクへ再調整）
> **実装済み・未push。** ユーザーFB「横にスライドするよりワープ的でないと役に立たない／横軸より縦軸にワープしたくなる」を受け、ダッシュ機体を横専用から上方向寄りに変更。
> - **操作**：押し始めで `DASH_BLINK_Y=92` の上ブリンク。横入力中に押す/横入力を入れると `DASH_BLINK_X=96` も足して斜めブリンク。移動途中の当たり判定は発生せず、着地点でだけ判定される。
> - **弱点**：普段は重い操作のまま、`DASH_CD=1.05` のクールダウンで連打不可。横移動だけで全部解く機体ではなく、危険帯を縦/斜めに一回切る機体へ寄せた。
> - **文書**：[機体設計.md] のダッシュ説明を「横瞬間移動」から「上/斜めブリンク」に更新。

> ## ★Codex追記（2026-06-29 サブステージを各地域3本へ拡張）
> **実装済み・未push。** ユーザー要望「サブステージを追加（2⇒5にするぐらい）」への一次対応として、まず各地域を2本→3本に増やし、合計12ステージにした。
> - **追加ステージ**：`A3 風の階段`（上昇気流＋階段穴、推奨=つばさ）、`B3 霧の浮標`（霧＋フロート＋パルサー、推奨=ふうせん）、`C3 雷の抜け道`（風向き反転＋雷雲＋スウープ、推奨=ふつう）、`D3 星門`（シャッター＋パルサー、推奨=コメット）。
> - **解放順**：`A → A2 → A3 → B → B2 → B3 → C → C2 → C3 → D → D2 → D3`。既存セーブで後半ステージまで解放済みの場合、新しく途中に挟まったステージも自動解放する `normalizeUnlockedStages()` を追加。
> - **狙い**：まだ5本/地域までは増やさず、まず「各地域の強みがもう1回別角度で出る」状態を作った。以後は実機で薄い面を見て、4本目/5本目を足すか既存面を濃くするか決める。
> - **検証**：`node -e "...new Function(<script>)..."` OK、`node tools/validate.cjs` OK（全12ステージ通過・コイン重なり0）、`node tools/smoke.cjs` OK。
> ## ★Codex追記（2026-06-29 コイン導線の動的安全余白＋タイトル発射の軽量化）
> **実装済み・未push。** ユーザーの磨きこみ候補から「コインめがけて飛ぶと動く敵に普通に接触する」「タイトル画面のカバ飛び演出が少しかくつく」を先に対応。
> - **コイン導線**：`enemyCoinMotionPad()` を追加し、コイン配置時だけ敵の横揺れ/斜めダイブ/フロート/パルサー等の動き幅を安全余白として見る。見た目箱そのものは変えず、コインを置く判定だけ保守的にしたため、敵配置や当たり判定の難度は変えない。
> - **調整幅**：最初は安全余白が強すぎてA2などのコインが痩せたため、form/swoop/diver等の余白を抑え直した。現状コイン数は A133 / A2 73 / B159 / B2 136 / C238 / C2 136 / D118 / D2 90。
> - **タイトル演出**：DOMタイトルのカバ/発射演出を `translate3d` 中心にし、画面全体の小刻みrumbleを廃止。CTAの待機揺れも `margin-bottom` ではなく `transform` に変更し、レイアウト揺れを減らした。
> - **検証**：`node -e "...new Function(<script>)..."` OK、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。

> ## ★Codex追記（2026-06-29 機体見た目縮小の明示化＋ダッシュをブリンク化）
> **実装済み・未push。** ユーザーFB「大きさを変えるやつはやってないかも？」「ダッシュの操作性が悪い。横にスライド中に当たり判定が消えるより、ワープ的でないと役に立たないかも」を反映。
> - **サイズ**：前回は当たり半径(size=1.0)とbodyH正規化中心だったため、リグ込みの総シルエットがまだ大きく見えうる。`VEH_ART.scale` を追加し、コメット/マグを `scale:0.84`、ダッシュを `scale:0.94` にして、プレイ中と選択チップの両方に適用。
> - **ダッシュ**：速度型(`DASH_IMPULSE`)を廃止し、`DASH_BLINK_X=118` の横ブリンクへ変更。移動途中の当たり判定は発生させず、着地点でだけ判定される。boost中は `DASH_BLINK_Y=32` だけ上にも抜けるので、縦ワープしたくなる感覚も小さく試せる。
> - **検証**：`node -e "...new Function(<script>)..."` は `script syntax OK`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。アプリ内ブラウザの自動リロードはfile URLポリシーでブロックされたため、手動リロードで確認する。

> ## ★Codex追記（2026-06-29 機体サイズ固定＋性能差/上昇演出を強化）
> **実装済み・未push。** ユーザーFB「マグ/コメットが大きすぎる割に性能が見合わない」「各機体の性能差が薄い」「ふつう以外の上昇演出が乏しい」「将来は特徴強化にコインを使う案もあり」を反映。
> - **サイズ方針**：`VEHICLES[].size` を全機体1.0へ統一（廃止枠こつぶ含む）。当たり半径は体格差で難度を作らず、ふうせんの横長リスクは `hitW`、リグの見た目差は `VEH_ART.bodyH/cy/cx` で正規化。特に大きく見えていたコメット/マグとfall差分は表示を縮小。
> - **性能差**：つばさ=滑空/横追従を大きく強化し登坂を弱化、ふうせん=コイン2倍だが横長・落下調整苦手、コメット=縦速大幅UP＋最高速付近で小鳥突破、マグ=広域吸引(mag.r=225/pull=12)＋重め操作、ダッシュ=普段重いが長距離横ダッシュ(`DASH_IMPULSE=880`, `DASH_CD=0.86`)へ。
> - **演出**：機体別boost FXを追加。つばさ=広い風、ふうせん=浮遊泡、コメット=青白い尾、マグ=吸引粒子、ダッシュ=シアンの横裂け線。ふつうの炎は維持。
> - **設計メモ**：強化費用システムは未実装。今回は「最大状態で面白い差」を先に作ったので、将来強化段階を入れるならこの最大値から逆算する。
> - **検証**：`node -e "...new Function(<script>)..."` は `script syntax OK`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。ヘッドレス撮影は通常 `node` から `playwright` が見つからず未実施（このrepoにpackage.jsonなし）。

> ## ★Codex追記（2026-06-28 コース全体の理想通過線へコインを一本化）
> **実装済み・未push。** ユーザーFB「コイン配置ルールが複数あるのが問題。コース全体を通した理想の通過線を作り、その通過線上にコインを置くシンプルなルールにするべき」を受け、コイン配置の正本を一本化。
> - **修正**：各ビートはコインを直接置くのではなく `routeAnchors` に理想通過点を登録。全コース事前配置の最後に `generateRouteCoins()` でアンカーを補間した1本の通過線上へコインを敷き直す。既存の局所コインは最終的に `coins=[]` で破棄されるため、複数ルールの混在が消える。
> - **意図**：通常敵/特殊敵/bonusが別々の顔でコインを撒くのをやめ、コインは「このコースをどう抜けるか」の一本線として読む。量は多めに戻しつつ、点群ではなくルートとして見せる。
> - **現状**：総量は A157 / A2 93 / B194 / B2 169 / C244 / C2 135 / D142 / D2 103。配置シート上はほぼ一本線。残りラベルは、穴の中心を通る理想線を `敵間` と拾う箇所と、D/D2のパルサー `敵密度` が中心。
> - **検証**：`node tools/placement_contact_sheet.cjs` 更新、`node tools/visual_overlap_audit.cjs` は全ステージ `hard=0 warn=0`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。SVGから `.shots/placement-contact-sheet-*-annotated.png` 再生成済み。

> ## ★Codex追記（2026-06-28 コイン生成元を絞って根本整理）
> **実装済み・未push。** ユーザーFB「触ったがやっぱりコイン配置が汚い。根本的に対処できていないのでは」を受け、後処理フィルタ追加ではなく生成ルール側を整理。
> - **根本原因**：通常敵の `spawnBird()` が毎回コインを出し、さらに `bonus` が同じビート内でランダム線を複数生成し、最後に `addCoinSafe()` / 補充処理で少しずつ位置をずらすため、画面上では複数の意図が混ざって「雑な点群」に見えていた。
> - **修正**：通常敵はコインを撒かず、`swarm` / 特殊ビート / `bonus` だけがコインを出す構造へ変更。デザイン済みの線・弧・三角は `addCoinSafe()` のズラしを使わず、置けない点は捨てる。`bonus` は1ビート1本の整った線/弧に集約。美観整理で落としたコインの補充も停止し、汚いものを別の場所へ再付着させない。
> - **現状**：総量は A60 / A2 43 / B44 / B2 28 / C44 / C2 26 / D57 / D2 58。前回より枚数は減るが、配置シート上のラベルは A2 160m `敵間`、B2 373m `孤立`、D/D2の `敵密度` 程度まで減少。見た目はかなり清潔だが、経済量をさらに戻すなら「枚数を散らす」ではなくコイン価値/bonus報酬設計で戻すのが安全。
> - **検証**：`node tools/placement_contact_sheet.cjs` 更新、`node tools/visual_overlap_audit.cjs` は全ステージ `hard=0 warn=0`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。SVGから `.shots/placement-contact-sheet-*-annotated.png` 再生成済み。

> ## ★Codex追記（2026-06-28 図形コインを保護しつつ美観整理）
> **実装済み・未push。** ユーザーFB「整理の結果として一直線以外の形が淘汰されている。斜めガイド、円、△などの図形の遊びを残したい」を受け、直線化一辺倒にならないよう調整。
> - **修正**：`index.html` に `coinGroupIntentionalShape()` 系の判定を追加し、等間隔の斜め線・浅い弧・三角形っぽい小図形は `雑密集` / `折れ線` の整理対象から外す。V字スイフトは穴の中央に小さな三角マーク、パルサー後の報酬は横一列ではなく浅い弧マークへ変更。敵に突っ込む導線は引き続き `coinGuideSegmentHitsEnemy()` で落とす。
> - **監査**：`tools/placement_contact_sheet.cjs` も同じ図形判定を持つよう更新し、意図ある弧/三角を過剰に囲わないようにした。残りラベルは A/B の一部 `敵間` / `雑密集` と D/D2 の `敵密度` が中心。B420m など、縦列の横に余りが見える箇所はまだ人間レビュー対象。
> - **現状**：総量は A104 / A2 78 / B92 / B2 66 / C80 / C2 38 / D94 / D2 80。前回より総量を戻しつつ、斜め・弧・三角の語彙を復活。
> - **検証**：`node tools/placement_contact_sheet.cjs` 更新、`node tools/visual_overlap_audit.cjs` は全ステージ `hard=0 warn=0`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。SVGから `.shots/placement-contact-sheet-*-annotated.png` 再生成済み（sharp変換時にfontconfig cache警告あり、出力は成功）。

> ## ★Codex追記（2026-06-28 コイン美観フィルタを強化＋総量を戻す補充）
> **実装済み・未push。** ユーザーFB「コインの配置が意味なく密集/敵を挟んで獲得不能/ぽつんと1つで汚い」「ただし入手総量は減らしすぎない」を受け、重なり監査だけでなく配置シートの囲みを見ながら修正。
> - **修正**：`spawnBird()` の報酬コインを穴の外縁から内側へ寄せ、敵間に見えにくいラインへ変更。さらに全コース事前配置の最後に `tidyCoinAestheticsAfterPreSpawn()` を追加し、敵近くの単発コイン、敵に挟まるコイン、線として読めない雑密集を整理する。落としたコインの一部は既存の縦/横ラインへ接続して戻し、丸い塊は同じ枚数を直線へ整列する。
> - **効果**：初回の「捨てるだけ」整理よりコイン総量を回復。現状コイン数は A99 / A2 65 / B80 / B2 58 / C77 / C2 36 / D92 / D2 66。配置シート上の「雑密集」「敵間」はほぼ消え、残りは主にD/D2のパルサー列に対する「敵密度」と、列の一部を孤立扱いするラフ判定。
> - **検証**：`node tools/placement_contact_sheet.cjs` 更新、`node tools/visual_overlap_audit.cjs` は全ステージ `hard=0 warn=0`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。SVGから `.shots/placement-contact-sheet-*-annotated.png` も再生成済み（sharp変換時にfontconfig cache警告あり、出力は成功）。

> ## ★Codex追記（2026-06-28 コイン列の間隔ムラを等間隔化）
> **実装済み・未push。** ユーザーFB「コイン同士が近すぎ/遠すぎで、一直線でも間隔が違うと雑に見える」を反映。
> - **修正**：美観整理後に `normalizeCoinLineSpacing()` を通し、縦列/横列として読めるコイン群の間隔ムラを等間隔へ再配置。等間隔化で別列と合わさって雑密集に見える場合は適用せず、最後にもう一度 `straightenMessyCoinClusters()` を通す。総量は前回比ほぼ維持（A99 / A2 62 / B80 / B2 58 / C77 / C2 36 / D92 / D2 66）。
> - **検証**：`node tools/placement_contact_sheet.cjs` 更新、`node tools/visual_overlap_audit.cjs` は全ステージ `hard=0 warn=0`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。配置シート上の「雑密集」は消え、残りは主にD/D2の敵密度と列の一部を孤立扱いするラフ判定。

> ## ★Codex追記（2026-06-28 コイン導線の不合理判定を追加）
> **実装済み・未push。** ユーザーFB「近い/遠いが混ざる列、上にぴょこっと出る折れ、戻らないと取れない2列、敵にぶつかる前提のガイドが気になる」を受け、点の重なりではなく“プレイヤーがなぞる線”として判定を追加。
> - **修正**：`index.html` に `coinGuideSegmentHitsEnemy()` / `straightenKinkCoinGroups()` / `tidyEnemyGuidedCoins()` を追加し、敵箱へ向かうコイン間セグメントや4〜6枚の小さな折れ線を整理。パルサーは近接行に同じ報酬横列を重複生成しないよう抑制。`tools/placement_contact_sheet.cjs` には `敵誘導` / `折れ線` 判定を追加。
> - **現状**：総量は A99 / A2 61 / B80 / B2 59 / C77 / C2 36 / D78 / D2 55。D/D2はパルサー密度の構造上まだ `敵密度` が出る。A/Bの一部は新判定で `敵誘導` / `折れ線` が残るため、次にやるなら個別ビートのコイン生成（over/swoop/pulsar）を設計単位で見直す。
> - **検証**：`node tools/placement_contact_sheet.cjs` 更新、`node tools/visual_overlap_audit.cjs` は全ステージ `hard=0 warn=0`、`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。SVGから `.shots/placement-contact-sheet-*-annotated.png` 再生成済み。

> ## ★Codex追記（2026-06-28 人間の見方に寄せた重なり監査を追加）
> **実装済み・未push。** ユーザーFB「コインとエネミーの重なり、エネミー同士の重なりが解消されていない。確認方法が良くないのでは」を受け、従来の `tools/validate.cjs` とは別に、実プレイの縦画面に近い見方で検出する `tools/visual_overlap_audit.cjs` を追加。
> - **確認方法の変更**：全ステージを事前spawnしたあと、432×768の実プレイ画面を160px刻みの高度スライスで走査。画面内に入る敵の見た目box・コイン円を重ねて、`hard`=実質的な重なり、`warn`=目視で近すぎる候補として出す。結果は `.shots/visual-overlap-audit.html` に赤枠SVG付きで出力。
> - **今回の結果**：`node tools/visual_overlap_audit.cjs` は `hard=0`、`warn=52`。ローカル最新では敵-コイン/コイン-コインの候補は0で、残りは敵-敵の近接のみ。主にパルサー横列の余白が6〜8pxで、人間の目には重なり/密集に見えやすい。
> - **従来検証との違い**：`node tools/validate.cjs` は引き続き全OKだが、これは通路・固定譜面・導入表示の明確な重なりを中心に見る。今回の監査は「実プレイ画面で近すぎて嫌に見える」を拾うための補助検査。
> - **次の一手**：パルサー列の見た目余白とコイン配置は下の追記で対応済み。ユーザーが見ているのが公開URLなら、ローカル最新との差分/未pushも疑う。

> ## ★Codex追記（2026-06-28 パルサー周りの汚いコイン/敵密集を整理）
> **実装済み・未push。** ユーザーが `.shots/visual-overlap-audit.html` の #3〜#8 を見て「敵の間のコインがきもい」「敵下のぽつんと1枚は取る価値がない」「密集がバラバラで汚い」と指摘。これは“重なっていない”ではなく、報酬として読めず、美観も悪い配置の問題。
> - **修正**：`spawnPulsar()` の3体配置を `W*0.20/0.50/0.80` に少し広げ、パルサー用コインを敵の間/真下の斜め線から、抜けた先の横一列3枚マークへ変更。敵整理の余白も `enemyFitsFinal(..., pad=12)` に広げ、フロート等との近接も潰す。
> - **結果**：`node tools/visual_overlap_audit.cjs` が全ステージ `hard=0 warn=0`。`node tools/validate.cjs` OK、`node tools/smoke.cjs` OK。パルサー最大時の通路圧はまだ残る（通常機体 widest 23 < need 34）ので、待って抜ける役割は維持。
> - **追加の見方**：`node tools/placement_contact_sheet.cjs` は `.shots/placement-contact-sheet-*.svg/json` を出す。JSONから作ったPNGでは、紫点線で「孤立」「雑密集」「敵間」「敵密度」を囲める。現状はラフ判定なのでA/A2/Bの意図した線も拾うことがあるが、D/D2の敵密度やC2/B2の雑密集を人間レビューに渡す目印として有用。

> ## ★Claude追記（2026-06-28 コイン配置の歪み/密集を解消＝線を崩さない配置へ）
> **実装済み・公開反映済み。** ユーザーFB「コイン配置の歪さ・密集のさせ方が気持ち悪い。役割を明確にしつつ配置をきれいに」を反映。
> - **根因**：コインは線/弧(=役割)で設計しているのに、衝突回避の2段(`addCoinSafe`／`tidyCoinsAfterPreSpawn`)が個々のコインを最大±82〜100px動かして“線から引き剥がして”いた。引き剥がされた点が散らばり＝歪み・塊に見えていた。検証(validate)上は重なり0でも、見た目が散漫。
> - **修正方針**：ゲーム内ヘルプの「線や弧は進路のヒント。**無理な時は捨ててよい**」をコードでも実践。ズラし量を“線の上の小ズレ”(縦優先・最大±28〜52px)に限定し、収まらなければ**動かさず捨てる**(=線に隙間。隙間は意図に見え、飛んだ点はノイズに見える)。`addCoinSafe`／`tidyCoinsAfterPreSpawn` のオフセット表を小さく作り直しただけ＝役割を担う線/弧の形と密度はそのまま。
> - **効果**：散らばっていた“迷子コイン”が落ち、塊が解けた。コイン数は微減(A 120→97 / A2 93→63 / D 114→96 等)＝散漫さの除去で見た目だけ整理、設計トレイルは維持。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK(8面とも passability/固定譜面/コイン重なり/鳥スタック/見た目重なり 全項目グリーン、敵-コイン0・敵-敵0・コイン-コイン0)。加えてサンドボックスからStage Aのコイン/敵座標をSVG化→Chromeヘッドレスで撮影し、導入フライオーバー相当(全コース圧縮)と実プレイ尺(432×768スライス)の双方で、修正後に塊が解け線/弧が読めることを目視確認。

> ## ★Codex追記（2026-06-28 導入マップ重なり修正の再対応）
> **実装済み・未push。** 前回の「重なりなし」判定が甘く、ユーザー実機ではまだ重なって見えていた件を再修正。
> - **前回の見落とし**：`tools/validate.cjs` は敵の当たり判定半径や `cy` を見ていたが、実際の描画は `b.y` とスプライトの横幅/高さで見える。小鳥・雷雲・タカなどの絵は当たり判定よりかなり大きいため、検査上は0でも画面では重なっていた。
> - **修正**：見た目重なり判定を半径ではなく「描画に近い箱」に変更。導入フライオーバー用の全コース事前配置後、敵同士が重なる場合は上下へ逃がし、無理なものは落としてでもバグ見えを避ける。コインも新しい箱判定＋広めの余白で再配置。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK、script syntax OK。新しい見た目検査で A/A2/B/B2/C/C2/D/D2 全て 敵-コイン0・敵-敵0・コイン-コイン0。

> ## ★Codex追記（2026-06-27 導入マップの重なり・コイン配置整理）
> **実装済み・未push。** ユーザーFB「最初にマップ表示するときに、エネミー同士の重なり、エネミーとコインの重なりがまだあってバグに見える／コイン配置がいびつ」を反映。
> - **原因**：導入フライオーバー用の全コース事前配置では、通路検査・コイン同士の重なり検査はあったが、敵同士／敵とコインの“見た目のめり込み”検査がなかった。さらに、画面端へコインXをクランプする処理で端に不自然な縦並びができやすかった。
> - **修正**：敵の見た目半径を使った配置ガードを追加し、敵の近くのコインは近傍の安全位置へずらす/置かない方針に変更。上かぶり・パルサー・スウープ・ダイブなどの危険側コインも敵本体から少し離し、読みやすい線として見えるよう整理。イベント敵同士のY位置も軽く間引く。
> - **再発防止**：`tools/validate.cjs` に「見た目重なり検査（敵/コイン）」を追加。導入表示時の敵-コイン／敵-敵／コイン-コインの近すぎる配置を全ステージで検出する。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK（A/A2/B/B2/C/C2/D/D2 全て 敵-コイン0・敵-敵0・コイン-コイン0）、script syntax OK。

> ## ★Codex追記（2026-06-27 プレイ体験のコーチング追加）
> **実装済み・未push。** ユーザー指示「プレイ体験の磨きこみや調整に向き合って自走」を受け、難度や譜面を大きく変えずに初見の理解を補助。
> - **プレイ中コーチング**：押しっぱなし/離して落下/穴を追う/低空復帰/初コイン/初被弾の各場面で、1回だけ短いピル型ヒントを表示。操作を説明しすぎず、「落下で間を作る」「明るいすきまを追う」「当たっても立て直す」を体験中に伝える。
> - **結果画面の振り返り**：死亡/クリア時に、次に試す行動を1行だけ表示。墜落時は低空復帰、衝突時は次の穴、クリア時はノーダメ/速さ星などへ誘導。
> - **狙い**：現状の本筋である「よける以外がない」問題の前段として、既存の押す/離す/横移動/ニアミス/コインの意味が伝わるようにする。敵哲学の大改修はまだ未着手。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK、script syntax OK。ヘッドレスChromeのスクショはこの環境でファイル出力されず未取得（Chrome自体は存在）。

> ## ★Codex追記（2026-06-27 アイコン安全余白の修正）
> **実装済み・公開反映予定。** ユーザーFB「ダウンロードしたら殆ど見切れてた」を確認。
> - **原因**：アイコン画像のピクセル寸法は 1024/512/192/32px で正常だったが、旧アイコンは絵が端まで詰まり、OS/PWAの丸角・円形マスクで右手/耳/炎が切れやすかった。
> - **修正**：`assets/sprites/title_hippo_green.png` を `assets/ui/title_bg_v5.png` 背景へ再合成し、カバ本体を少し縮小・右上寄せを緩和。炎は下へ自然に抜けるが、主役の顔/胴/手足は安全枠内に収まる配置へ変更。
> - **再生成**：`assets/ui/app_icon_1024.png/.webp`、`icon-512.png/.webp`、`icon-192.png/.webp`、`favicon-32.png/.webp` を更新。
> - **検証**：manifest JSON parse＋アイコン実ファイル存在チェック OK、`node tools/smoke.cjs` OK。

> ## ★Codex追記（2026-06-27 アイコン配信確認＋head補強）
> **実装済み・未push。** ユーザーFB「アイコンがうまく設定されていないみたい」を確認。
> - **原因確認**：ローカルには `assets/ui/favicon-32.png` / `icon-192.png` / `icon-512.png` が存在し、寸法も 32/192/512px でOK。`manifest.webmanifest` もUTF-8としては正常。
> - **公開版の問題**：`https://hippo-rocket.pages.dev/assets/ui/icon-192.png` と `favicon-32.png` が、画像ではなく `text/html` として返っていた。つまり公開ブランチ/配信先にアイコン画像が未反映の状態。次に公開用 `gh-pages` へ配信対象ファイルを反映してpushすれば解消見込み。
> - **今回の補強**：`index.html` のheadに 192/512px favicon link を追加し、apple-touch-icon に `sizes="192x192"` を明記。`manifest.webmanifest` の icon purpose を `any maskable` に変更。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK、manifest JSON parse＋アイコン実ファイル存在チェック OK。

> ## ★Codex追記（2026-06-27 アプリアイコン＋WebP軽量化）
> **実装済み・未push。** ユーザーFB「アイコンがない」「生成アイコンがキモい」「炎が中心で変。デザインの中心はコビトカバ」を反映。
> - **アイコン**：生成AIの新規顔は採用せず、既存の `assets/sprites/title_hippo_green.png` を元に、カバの顔/胴体を中心へ寄せた `assets/ui/app_icon_1024.png` を作成。炎は補助要素として下端に抜ける程度に抑えた。派生 `icon-512.png` / `icon-192.png` / `favicon-32.png` / WebP版も作成。
> - **配線**：`index.html` に favicon / apple-touch-icon / `manifest.webmanifest` / theme-color / title主要画像のpreloadを追加。
> - **WebP軽量化**：配信対象の `assets/ui` と `assets/sprites` のPNG 70枚に対応するWebPを生成。ゲーム本体のDOM/CSS/JS参照は基本WebPへ変更。旧PNGは原本として残す。
> - **初期表示のチラつき修正**：タイトルの旧DOM雲/鳥/旧heroが後続CSSで復活していたため、非表示指定を強化。タイトルは `title_bg_v5.webp` + `title_hippo_green.webp` + `title_logo_v2.webp` を先読みする。
> - **軽量化結果**：表示対象PNG合計は約106MB相当、対応WebP合計は約4.1MB。特にタイトル背景/ロゴは2〜3MB級PNGから50〜153KB級WebPへ。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。
> - **未反映**：push禁止ルールに従い、Cloudflare Pages本番URLへはまだ反映していない。公開する場合は公開用 `gh-pages` へ最小構成を出す。

> ## ★Codex追記（2026-06-27 地域の第1ステージ初期解放）
> **実装済み・未push。** ユーザーFB「最初にステージのロックがかかりまくってるので、各地域の第1ステージは最初から解放でOK」を反映。
> - **初期解放**：新規セーブの `unlocked` を `A/B/C/D` に変更。各地域（鳥の空域/雲海/嵐の空/星脈）の1本目は最初から選べる。
> - **既存セーブ移行**：すでに遊んでいるブラウザでも、`STAGE_REGIONS` の各先頭ステージが未解放なら自動で追加して保存する。
> - **進行ループ**：第2ステージ（A2/B2/C2/D2）は従来どおりクリアで解放。

> ## ★Codex追記（2026-06-27 公開URL発行）
> **公開済み。** 他の人に触ってもらうため、Cloudflare Pages本番URLを発行。**一般ユーザーへ渡すURLは Cloudflare Pages の方を正とする。**
> - **本番プレイURL（Cloudflare Pages）**：https://hippo-rocket.pages.dev/
> - **予備URL（GitHub Pages）**：https://yukiyeahhhh.github.io/hippo-rocket/
> - **GitHub repo**：https://github.com/yukiyeahhhh/hippo-rocket
> - **公開方式**：`master`全体はpushせず、公開用の `gh-pages` ブランチに `index.html` と `assets/sprites` / `assets/ui` を出す。Cloudflare Pages/GitHub Pages とも `gh-pages` を見る。開発文書・HANDOFF・生成元 `assets/states` は公開ブランチに含めない（ただし初回公開ブランチには一部プレビューHTMLも含まれる）。
> - **Cloudflare Pages設定**：Project=`hippo-rocket`、Production branch=`gh-pages`、Build command=空、Output directory=`/`。WorkerではなくPages。`npx wrangler deploy` が出る画面はWorker側なので戻る。
> - **確認**：Cloudflare本番URL HTTP 200、`assets/sprites/idle.png` HTTP 200を確認済み。GitHub Pagesも status=`built`/HTTP 200確認済み。
> - **ローカル状態**：`origin` は `https://github.com/yukiyeahhhh/hippo-rocket.git` に設定済み。通常の開発コミットは引き続きローカル `master` に積む。外部公開更新時だけ `gh-pages` へ公開用最小構成をpushする。

> ## ★Codex追記（2026-06-27 ダッシュ機体追加）
> **実装済み・未push。** ユーザーFB「機体の追加とかいろいろ進めて」に対応し、新機体 **ダッシュ** を追加。
> - **追加FB対応**：初版画像は通常ロケットとの差分が分かりにくかったため、左右へ大きく張り出す青いサイドブースター＋矢印フィン＋稲妻マークの画像へ差し替え。`VEH_ART`も `bodyH:0.92/cy:0.52` に調整してゲーム内サイズを抑えた。
> - **画像生成/透過**：`assets/states/veh_dash.png` をCodex内蔵画像生成で作成し、`tools/dechroma_green.cjs`で `assets/sprites/veh_dash.png` へ透過切り出し。生成元は `C:\Users\mr_ba\.codex\generated_images\019f0798-09d9-7461-bcac-648665ccb3fd`。
> - **機体配線**：`VEHICLES/VEH_ART/VEH_META` に `ダッシュ`(vehIdx=6) を追加。価格220、普段の横旋回は重め、横入力の初動だけ `DASH_IMPULSE` で瞬間回避。`U`全解放と `1-7` デバッグ選択にも反映。
> - **見た目/手応え**：ブースト中はブースター振動、ダッシュ発動時は横残像＋キラキラ。自機周囲にクールダウンリングを表示し、使える時は淡く発光。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。`assets/sprites/veh_dash.png` は四隅alpha=0/中央alpha=255を確認。Chromeヘッドレス撮影はこの環境のCLIから出力されず未取得（既存の通常`node`からはPlaywrightも未導入）。
> - **次の一手**：実機でダッシュ距離/クールダウン/リングの視認性を見る。強すぎるなら `DASH_IMPULSE` を480前後へ、弱いなら560前後へ。次の新機体は「バブル（1回シールド）」か、各装備のboost差分画像。

> ## ★Codex追記（2026-06-27 上昇気流の視認性改善＋コメット強化）
> **実装済み・未push。** ユーザーFB「上昇気流みたいなやつ、視認性がめっちゃ悪い／画像の切れ目が分かりやすすぎて浮いてる」「機体の追加とか進めて」を反映。今回は画像生成ではなくコード描画と既存機体差別化。
> - **上昇気流(updraft)改善**：`drawUpdrafts()`を、四角い光柱＋硬い縁ラインから、上下フェードの広い発光＋曲がる明るい芯＋上へ流れるリング/粒へ変更。上下端の切れ目をぼかし、乗れる範囲と上向きの流れが一目で分かる方向に調整。乗っている時は芯/粒/リングが強くなる。
> - **コメット強化**：`COMET_BREAK_VEL`追加。コメット(vehIdx=4)がブースト中かつ最高速付近なら、`small`系のスイフト/スウープ接触を被弾ではなく`突破+1`にして小鳥を弾く。ペリカン/雷雲/フロート/パルサー/タカは対象外＝壊せない脅威は残す。
> - **文書更新**：[機体設計.md]のコメット案を実装済みに、[敵設計.md]のupdraft説明を視認性改善版へ更新。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。`?demo=updraft`のChromeヘッドレス撮影は環境側でtimeout（コード/機械検証は通過）。実機で上昇気流の見え方とコメット突破の頻度を要確認。
> - **次の一手**：本当の新機体追加なら、[機体設計.md]の優先順どおり「ダッシュ（横瞬間回避）」または「バブル（1回シールド）」を追加。アートが必要なので、実装前に `visual-asset-kit` で1枚生成→`VEHICLES/VEH_ART/VEH_META`へ追加。

> ## ★Codex追記（2026-06-27 非鳥敵スプライト底上げ＋見た目アニメ強化）
> **実装済み・未push。** HANDOFF最優先の「非・鳥の敵スプライトの底上げ」を4種まとめて実施。既存の挙動・当たり判定は変えず、見た目の差し替えと描画時アニメだけを強化。
> - **生成/差し替え**：`assets/states/{pelican,hawk,storm,float}.png` をCodex内蔵画像生成で作り直し、`tools/dechroma_green.cjs`/`tools/dechroma_magenta.cjs`で `assets/sprites/` へ透過出力。生成元は `C:\Users\mr_ba\.codex\generated_images\019f0798-09d9-7461-bcac-648665ccb3fd`。
> - **見た目**：ペリカン板＝横長の重い板鳥、タカ＝急降下が読める斜めシルエット、雷雲＝紫灰のもくもく障害物、フロート＝泡クラゲ。
> - **アニメ**：`drawPelican()` のパタパタ/ホバリングを強化、`drawFloat()` にふわふわ漂流＋拍動＋首振りを追加、`hawk` は共通鳥描画内で羽ばたき/bob/傾きを少し強めた。いずれも描画だけで、敵の実座標・通路判定は不変。
> - **確認**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。ヘッドレス撮影は `?demo=over` と `?demo=float` を確認済み（`.shots/enemy_over.png` / `.shots/enemy_float.png`）。`storm`/`dive` のChrome単発撮影は環境側でタイムアウトしたため、素材単体の透過alphaと機械検証まで確認。
> - **次の一手**：実機で4敵の動き量を見る。足りなければ小鳥/タカの実フレーム化（翼上げ/下げ）か、雷雲の画面内サイズ調整。次候補は機体差別化（コメット強化）または実機チューニング。

> ## ★★Codexへの引継ぎ（2026-06-27 Claude→Codex／優先順位つき）★★
> **担当をCodexへ。Claude分は全コミット済み・未push（push禁止／HANDOFF常時更新）。**
> ユーザーFB（最新）：「鳥の地域変種はこだわりすぎ・他の敵もやって」「敵の動きが少ない＝チープ(羽ばたき等)」。
> **動き側はClaudeが全敵に手続きアニメ済み**（下記DONE）。**Codexの本線＝“非・鳥の敵の見た目底上げ”（＝画像生成。Codexの得意領域）**。
>
> ### すでにDONE（やり直さない）
> - 地域別スイフト bird_{small,mist,storm,stars}＋`regionBirdSprite()`/`swift:true`（描画時解決）。
> - 4地域の空(`skyImgs`/sky_*)＋足元の世界(world_launch)＋地域トーン(WORLD_TINT)。
> - 設定背景＝星雲v2(`settingsBG()`)。
> - **全敵の手続きアニメ**（draw内・当たり判定b.yは不変）：羽ばたき(縦スケール)/ペリカン板ホバリング/フロート泡クラゲ拍動/雷雲もくもく。
>
> ### 優先順位（Codex）
> **① 非・鳥の敵スプライトの底上げ〔最優先・画像〕**：今は手描き/簡易な4種を、KV`title_kv_v5`トーン＋art-bibleで魅力的に作り直す。**×4地域には広げない**（こだわりすぎ回避＝まず各敵“正”を1枚ずつ）。敵は[visual-asset-kit]ルーブリックの「敵＝別トーン(くすみ・ほつれ・哀愁)」で。対象と表示寸法(身長基準)：
>   - `pelican.png`（上かぶり板＝翼を広げた“重い板”。横長）→ `drawPelican` 2024付近、`?cap&demo=over`。
>   - `hawk.png`（ダイブするタカ＝鋭い。drawH≈103）→ `spawnDiver`、`?cap&demo=…`（diverはelite beat）。
>   - `storm.png`（**雷雲＝障害物**。機体名ではない。drawH≈108〜127）→ `spawnObstacles`、`?cap&demo=storm&flash`。
>   - `float.png`（下からせり上がる泡/クラゲ。表示≈55px）→ `drawFloat`、`?cap&demo=float`。
>   生成→透過は `tools/dechroma_green.cjs in out`（緑キー＋bbox切出＋デスピル）か `tools/dechroma_magenta.cjs`（緑を残したい時）。出力 `~/.codex/generated_images/<id>/*.png`→`cp`で `assets/sprites/` へ。プレビューHTMLを足してStart-Processで見せ、ユーザーOKを取る。
> ② **羽ばたきの実フレーム化〔②・要・実機判断〕**：①の後、手続き羽ばたきが実機で弱ければ小鳥を2フレーム(翼上げ/翼下げ)生成しスプライト交互（`regionBirdSprite()`の返りをフレームで切替）。**まずyukiyaが実機で“動き十分か”を見てから**着手。
> ③ **機体差別化（コメット強化ほか）〔設計→実装〕**：[機体設計.md]に提案あり。最高速で小鳥を弾く等。
> ④ **実機チューニング（要yukiya同席）**：導入フライオーバー尺/8面の長さ/C2のコイン薄さ/落雷頻度(STRIKE_*)/updraftブースト量(UPDRAFT_*)/つばさfall寸法。
>
> ### 共通の作法（厳守）
> - 検証：`node tools/smoke.cjs`＋`node tools/validate.cjs`（passability/固定譜面/コイン重なり/鳥スタック）。
> - 撮影：`chrome --headless=new --disable-gpu --window-size=432,768 --screenshot=… "…?cap&stage=A&alt=150&demo=<type>"`（`--virtual-time-budget`/`--run-all-compositor-stages`は付けない＝ハング）。demo種＝form/over/float/storm/swoop/dive/pulsar/updraft/step/shutter。
> - **コミットするがpushしない。** art-bible/設計docも同フォルダで更新。

> ## ★Claude追記（2026-06-27 敵の動き付け＝“貼っただけ感/チープ”の根治・全敵）
> **担当＝Claude。全コミット済み・未push。** ユーザーFB「鳥にこだわらず他の敵もやって／敵の動きが少ない(羽ばたき等)＝チープ」。**静止スプライトだった敵に手続きアニメを付与（生成不要・全敵横断）**。
> - 判明：**ペリカン板とフロート泡はスプライト時に完全静止**（procedural fallbackだけ動いていた）。鳥/タカは tilt±0.07 のみで羽ばたきに見えない。
> - 対応（draw内・当たり判定b.yは不変＝見た目だけ）：
>   - **羽ばたき**＝翼を縦スケールであおる(縮む=下げ)＋揚力の上下動＋機首傾き。小鳥は速く深く(hz12/amp0.18)、タカは遅く浅く(hz6.5/amp0.10)＝滑空感。
>   - **ペリカン板**＝ホバリング上下動＋大翼の重い羽ばたき(縦スケール)。
>   - **フロート泡**＝クラゲの拍動(横伸び縦縮み・面積保持)＋ゆらり上下。
>   - **雷雲**＝横ドリフト＋もくもく呼吸(縦横逆相スケール)＋落雷。
> - 確認：`?cap&demo=form/over/float/storm` で静止フレーム描画OK（歪み無し）。動きの手応えは要実機。
> - **次（ユーザー指摘の“他の敵の見た目”）**：ペリカン/タカ/雷雲/泡の**地域別モチーフ画像**はまだ＝鳥と同様にCodex生成で展開可（やるか要確認）。羽ばたき等を2フレーム実スプライト化する案もあるが、まず手続きアニメで様子見。

> ## ★Claude追記（2026-06-27 画像リッチ化②＝地域別の敵モチーフ＝スイフト4種）
> **担当＝Claude。全コミット済み・未push。** 「おすすめでどんどん進めて」継続。敵モチーフ画像化＝**挙動共通・絵だけ地域別**でスイフト(form/swoop/step/overhang入口列)を4地域別に。
> - 生成：master`bird_small`を`-i`参照に、緑クロマ背景で3種生成→`tools/dechroma_green.cjs`(緑キー＋bbox切り出し＋デスピル)で透過。`assets/sprites/bird_{mist,storm,stars}.png`。鳥の空域はmaster`bird_small`流用。
>   - **mist**＝真珠白＋桜/空色の翼＋雲うず腹／**storm**＝鈍色＋稲妻アクセント／**stars**＝藍＋星屑の翼＋金顔。
> - 配線：`REGION_BIRD`＋`regionBirdSprite()`。birdに`swift:true`を持たせ**描画時に地域スプライトを解決**（spawn時bakeだと遅延読込でbird_smallが焼き付く＝`?cap&demo`で実証）。読込前のみbird_smallフォールバック。
> - 確認：`?cap&stage=A|B|C|D&demo=form`（新規demo追加）で4地域の隊列を撮影＝地域別モチーフが空背景上で飛ぶのを確認。プレビュー＝`assets/ui/_bird_preview.html`。validate/smoke全OK。
> - **次の画像タスク**：設定画面背景（ユーザー既出「古い」）。あと任意で地域別プレイ背景の更なる作り込み。

> ## ★Claude追記（2026-06-27 画像リッチ化フェーズ＝足元の世界＋4地域の空を生成）
> **担当＝Claude。全コミット済み・未push。** ユーザー指示「画像で」＝画像リッチ化フェーズ。`visual-asset-kit`スキルに沿い、承認済みKV`title_kv_v5`をトンマナの正に小分け生成。
> - **★Codex画像生成はBashのsandbox無効化(dangerouslyDisableSandbox)でのみ通る**：直接`codex exec`はSessionStartフック失敗、rescueサブエージェントはwebsocket sandbox(os error 10013)で不可。**正＝`codex exec ... -i <ref>` をsandbox無効で叩く**（生成→`~/.codex/generated_images/<id>`→`cp`で取り込み）。
> - **足元の世界**(`world_launch.png`)：遠くの町＋木立＋発射台。マゼンタキー生成→`tools/dechroma_magenta.cjs`で透過(緑の木を残すためマゼンタキー)。**透過素材なので全地域共通**。draw()で旧hillsを置換、登ると後退。
> - **4地域の空**(`sky_birds/mist/storm/stars`)：グラデでなく“中身のある空”。`skyImgs`に配線、draw()でグラデ上に敷き、登るほど画像上部へ視差スクロール。ユーザーFB「星雲が写実すぎ」→星脈をv2(フラットな絵本調星雲)に再生成。
> - **art-bible追記**：「足元の世界は陸を描いてよい(空=陸禁則は中空〜上のみ)」。プレビュー＝`assets/ui/_world_preview.html`/`_sky_preview.html`（Start-Processで開く）。
> - **次の画像タスク**：①足元の世界を地域別にトーン替え(現状は朝の明るさ固定で夜/嵐空と僅かに不一致＝任意) ②地域別の敵モチーフ画像(同挙動・別見た目) ③設定画面背景。いずれもsandbox無効のcodex execで。

> ## ★Claude追記（2026-06-27 敵・ギミックの幅出し＝新タイプ5種を実装。Codexと相談）
> **担当＝Claude。全コミット済み・未push。** ユーザー指示「敵/ギミックの幅出し、codexと相談して挙動も考えて」。Claude独立案×Codex独立案を突き合わせ（結論を先渡しせず）、**画像リスキンでなく挙動・実体として新タイプを5種追加**。詳細＝[敵設計.md]「★幅出し 統合プラン」。
> - **★Claude(上昇気流)とCodex(ブースト芯)が独立に“縦の上昇ブースト柱”へ収束**＝高難度で死ぬ「上昇の爽快感」を取り戻す本命。
> - 実装5種：①**上昇気流コア(updraft)**＝長押しで柱に乗ると上昇加速+上限UP(StageA) ②**ステップ穴帯(step)**＝穴が階段状にズレ登りながら横補正(StageA) ③**落雷レーン(lightning)**＝雷雲のstaticを予告→落雷の機構へ昇格・真下の細柱を横回避(StageC) ④**シャッターゲート(shutter)**＝中央開口が開閉、開いた瞬間に抜ける溜め系(StageD) ⑤**追い風(gust)**＝横風をポジティブ化・風下のコインを乗って拾う(StageC)。各 `?demo=<type>` で確認、validate全項目OK。
> - **次フェーズ＝画像リッチ化**（ユーザー指示「幅を増やしてから画像の種類増やす/リッチに」）：地域別の敵モチーフ画像＋地域別プレイ背景＋設定背景＝**Codex画像生成**（挙動共通で見た目だけ地域別）。アート方向はユーザーと相談。
> - **要実機チューニング**：updraftのブースト量(UPDRAFT_ACC/VMAX)、落雷の厳しさ(STRIKE_*)、shutterの開閉(SHUTTER_*)、各teachの難度。

> ## ★Claude追記（2026-06-27 実機FB#5＝DESTINATION再設計＋細部UI＋つばさfall＋設計提案記録）
> **担当＝Claude（Codex上限のため代行）。全コミット済み・未push。** ユーザー就寝前の追加FB（#40-49）と方向性メモをコード単独で一括対応＋画像/設計待ちは提案として文書化。
> - **DESTINATION再設計（#40-44）**：地域プレートを背景4島の座標(`REGION_POS`)へ絶対配置＝島が見えたまま名前が乗る／星・OPEN数・代表を撤去／事前選択ハイライト廃止／サブステージに機体‹›スライダー(`dstVeh`)／地域→サブのズーム演出(`dstZoom`/`platePop`)。地域ビューをヘッドレス目視で確認済。
> - **細部UI（#45-48）**：設定ラベルの右ズレ（`coinHud`のtextAlign戻し）／SHOP看板を中央バナー化して戻るボタンと距離／クリア後つぎへを尺ある遷移に／遊び方をGARAGEからも（`?`＋`helpBack`）。
> - **つばさfall（#49）**：`fall.bodyH 0.66→0.90`で落下絵の体が約1.4倍に膨らむ不具合を是正（飛行と同寸）。撮影`?cap`もフライオーバー除外で姿勢確認可に。
> - **設計提案を文書化（#50-53・画像/設計待ち）**：[敵設計.md]に「地域別の特色＋敵幅出し」「上昇の爽快感を残す面」、[機体設計.md]に「機体差別化（コメット強化）」を実行手順つきで追記。**マップ配置自体は手作業＋将来ツールなので後**。
> - **次の一手**：①敵/ギミックの幅出し＋地域別モチーフ（挙動は先行実装可／画像はCodex）②質感の画像化（設定背景含む・Codex上限回復後）③実機チューニング（フライオーバー/8面の長さ/C2コイン薄さ/落雷頻度/つばさfall寸法）。
>
> ## ★Claude追記（2026-06-27 実機FB#4のUI構造バグを一括修正＋敵/雲アニメ＋鳥重なり根治）
> **担当＝Claude（Codex上限のため代行）。全コミット済み・未push。** ユーザー実機FB（#27-38＝下の気になりリスト正本）をコード単独で直せる構造バグとして一括対応。質感の画像化はCodex画像生成待ちで未着手。
> - **UI重なり/可読性バグ（#27-35,37）**：GARAGEコイン⚙重なり／DESTINATION二重フレーム・選択中不明・黄文字不可視・サブステージ縦長／これで出発の改行／設定説明文はみ出し／SHOP看板過大／HELP図解の接触／TITLE CTA位置。全画面ヘッドレス目視で回帰なし確認。
> - **初期配置の鳥の潰れ（#36・根治）**：`aheadSpawnY`の画面上端クランプが突入事前配置で開始付近の陣形を同一Yへ潰していた→preSpawning中はクランプ除外。validateに過密スタック検査追加（全8面で最大同帯3〜6体＝陣形1つ分）。
> - **敵/雲の貼り付き感（#38）**：霧の呼吸ゆれ・鳥の羽ばたき傾き・雷雲のドリフト＋落雷。`?cap&demo=storm&flash`で確認可。
> - **★戦略メモ記録**：マップは将来“人が手作業＋パレットツール”、ただし優先は先に「敵・ギミックの幅出し」（下の戦略メモ＆気になり#5）。
> - **次の一手**：①敵/ギミックの幅出し（Codex起点案も歓迎・#5）②質感の画像化（Codex上限回復後）③実機チューニング（フライオーバー速度/8面の長さ・C2のコイン薄さ/落雷頻度）④#13機体・#14収集＝方針確認待ち⑤行き先ネームプレートを島位置へ（#39・マップツールと一体）。
>
> ## ★Claude追記（2026-06-27 Codex作業の検証取込＋全画面QA＋コイン重なり回帰検査）
> **担当＝Claude（Codex上限のため代行）。全コミット済み・未push。** ユーザー指示「codex上限なので進められるところ進めまくって」を受け、Codexが上限で未コミットだった分の取込と、コード単独で進められるQA/回帰防止を実施。
> - **Codex未コミット分を検証して取込(f944d37)**：`#ui *` への `box-sizing:border-box`、画像ボタンへの明示高さ、HELPカードのスクロール化＋下部ボタン分離、SHOP商品棚の開始位置下げ。`smoke`/`validate` OKを確認の上コミット。
> - **全DOM画面の回帰QA（ヘッドレス432×768で目視）**：TITLE/GARAGE/SHOP/HELP/DESTINATION/SETTINGS の6画面、**box-sizing全適用後も崩れなし**を確認。設定ボタンの扁平も解消済み、HELPはスクロールでボタンと分離、SHOPは看板と商品の重なり解消、DESTINATIONは地域2階層の島カードが良好。スクショは`.shots/scr_*.png`（gitignore）。※ヘッドレスは`--virtual-time-budget`系フラグ無し＋sleep後kill が安定（フラグ有りはハングしがち）。
> - **コイン重なりの回帰検査を`tools/validate.cjs`に追加(b905d01)**：Codexがencoding問題で見送っていた検査。全8面でコイン中心間距離<r_i+r_jのペアが0件（最接近余白12px＝addCoinガードの+12と一致）を確認。実機FB「コインが重なって描画されるとバグっぽい」の再発防止。
> - **データで気づいた観察（実機判断向け・未対応）**：8面のコイン数/長さ＝A:97/400m, A2:85/300m, B:103/600m, B2:65/500m, C:70/700m, **C2:36/400m(=0.09/m＝全面で突出して薄い)**, D:108/500m, D2:101/500m。Codexの「薄い面があればbeats調整」の最有力候補は**C2**。密度を上げるか要実機判断（一方的な再バランスは避け記録に留める）。
> - **次の一手（ユーザー/Codex判断待ち）**：①質感の画像化（CTA/看板/各画面背景＝Codex画像生成・上限回復後）②実機チューニング（フライオーバー速度/8面の長さ・難度・C2のコイン密度）③#13機体追加・#14収集トラック＝方針確認待ち。
>
> ## ★Codex追記（2026-06-27 画像UIパーツ重なり修正＋遊び方図解改善）
> **実装済み・未push。** ユーザーFB「遊び方がボタンと重なってスクロールできない」「穴に合わせる/かすめて得する画像が意味不明」「ショップ看板が売り物に重なる」「画像ボタン化した所が全体的に重なる」を反映。
> - **原因**：`border-image` の画像UIパーツがボーダー分だけ要素サイズを膨らませ、既存レイアウトを押していた。DOM UI全体に `box-sizing:border-box` を適用し、画像ボタンには明示的な高さを設定。
> - **HELP**：カード領域をスクロール可能にし、下部ボタンと重ならない `bottom`/余白へ調整。「穴に合わせる」は鳥2体の間の安全ゾーン、「かすめて得する」は赤い近接リング＋`+1`表示に差し替え。
> - **SHOP**：画像看板の高さに合わせ、商品棚の開始位置を下げて売り物と重ならないよう調整。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。PlaywrightはこのNode環境に無く、ブラウザ実寸の自動計測は未実施。
>
> ## ★Codex追記（2026-06-27 ブランドスプラッシュの余韻調整）
> **実装済み・未push。** ユーザーFB「最初のスプラッシュもっとゆっくり見せて、雰囲気出して」を反映。
> - **実装内容**：`SPLASH`の自動遷移を `1.55s → 3.35s` に延長。背景の淡い光/雲だまり、ロゴのゆっくりしたフェード、下線の伸び、`tap to skip` の遅れて出るアニメを追加。
> - **操作**：タップ/Enterでスキップできる挙動は維持。
> - **検証**：`node tools/smoke.cjs` OK。
>
> ## ★Codex追記（2026-06-27 サブステージ4本追加）
> **実装済み・未push。** 地域→サブステージ2階層を実際に活かすため、各地域に2本目のステージを追加。
> - **追加ステージ**：`A2 鳥の輪`（横列/V字/streamの輪を読む）、`B2 霧の裂け目`（霧+フロート+パルサーの待ち）、`C2 逆風回廊`（横風反転で位置を作り直す）、`D2 流星の拍`（パルサーとstreamの拍）。
> - **解放順**：`A → A2 → B → B2 → C → C2 → D → D2`。各地域のサブステージ一覧には2本ずつ表示される。
> - **ドキュメント**：`画面遷移.md` / `拡張設計メモ.md` / `開発計画.md` を更新。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。固定譜面チェックはA/A2/B/B2/C/C2/D/D2すべて✓。
> - **次の一手**：実機で8ステージの長さ/難度差を見る。短すぎる・薄い面があればbeatsの長さと組み合わせを調整。次の大物は「ステージ別モチーフ画像の差し替え」または「ショップ/機体ラインアップ再設計」。
>
> ## ★Codex追記（2026-06-27 固定譜面化の第一歩）
> **実装済み・未push。** ユーザーFB「ステージ毎のギミックとコイン配置は固定で、何度プレイしても同じにしたい」を受け、ステージ生成のランダムを固定化。
> - **実装内容**：`index.html` にステージ譜面専用の疑似乱数 `stageRand()` / `stageSeed(stageKey)` を追加。`reset()`ごとにステージキーから同じseedを作り、敵・コイン・雲バンクの配置乱数だけを `stageRand()` に置換。粒子・キラキラなど演出のランダムはそのまま。
> - **効果**：同じステージをリトライしても、敵の穴位置、ダイブ/スウープ方向、コイン線/弧、雲バンクの高さが同じになる。コイン配置が「意味のある譜面」として覚えられる土台。
> - **検証追加**：`tools/validate.cjs` に固定譜面チェックを追加。同じステージを2回生成し、敵/コインの座標シグネチャが一致するか検査。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK（Stage A〜D 固定譜面チェックすべて✓）。
> - **次の一手**：固定化したうえで、A-2/B-2/C-2/D-2や既存A〜Dの譜面そのものを手設計へ寄せる。特に「コイン線＝進路」「危険側の弧＝欲張り」「雲の裂け目＝通路」を一致させる。
>
> ## ★Codex追記（2026-06-27 UIパーツ横展開＋サウンド設計＋実機確認サーバ改善）
> **実装済み・未push。** ユーザー指示「どんどん進めて」「BGM入れどころリストほしい」を受けて着手。
> - **UIパーツ横展開**：`assets/ui/parts/*_v1.png` をTITLE以外にも展開開始。HELPカード/HELPボタン、GARAGE主ボタン/副ボタン、DESTINATION主ボタン/副ボタン/ステージカード/選択中パネル、SHOP看板に9スライス風 `border-image` を適用。
> - **サウンド設計**：`サウンド設計.md` を追加。`brand_jingle` / `title_theme` / `garage_loop` / `destination_loop` / `shop_loop` / `help_loop` / `stage_a_loop`〜`stage_d_loop` / `clear_jingle` / `dead_hit_jingle` / `dead_fall_jingle` の用途と方向性を整理。
> - **実機確認サーバ**：スマホで動作確認できない件を切り分け。`127.0.0.1:8124` にも最初は接続不可＝ゲームコードではなく `tools/serve_lan.cjs` が起動していない状態。Codexの隠し起動はこの環境で残らないことがあるため、実機確認時はターミナルで `node tools/serve_lan.cjs 8124` を前面起動し開きっぱなしにするのが確実。`serve_lan.cjs` は起動時にPC用/スマホ用URLとFirewall注意を表示するよう改善。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK、`node tools\serve_lan.cjs 8124` は起動ログで `smartphone URL: http://192.168.10.119:8124/` 表示を確認（timeoutで停止しただけ）。
>
> ## ★Codex追記（2026-06-27 大量FB対応・遊び方/ブランド/行き先/風船/設計メモ）
> **実装済み・未push。** ユーザーFBを受け、即効で直せるUI/バランスと、次パス設計メモを反映。
> - **遊び方**：`HELP`画面をテキスト中心から画像中心に作り直し。既存スプライト（boost/fall/idle/bird/hawk/coin）で「押す/離す/穴/ニアミス/コイン線」を図解。TITLE右上 `?` に加え、TITLE下部に見える「遊び方」ボタンを追加。HELP内に「出発する」導線も追加。
> - **ブランドスプラッシュ**：通常起動を `SPLASH → TITLE` に変更。`BRAND_NAME='YUKIYA GAMES'` を短く表示し、自動遷移/タップ/Enterでスキップ可能。`?screen=...` 指定時は従来通り直接画面確認可能。
> - **TITLE調整**：タイトル画面の飛ぶコビトカバを少し下へ（`top:66%→70%`）。
> - **行き先画面**：地域一覧は下部ドック寄りの小さめカードにして背景マップを見せる。サブステージ一覧は1カラム大カードにして、押す前後が同じリストに見えないよう階層感を強めた。
> - **コイン/風船**：コイン収集時のsparkleを `10/150→4/64` に抑え、エフェクト重なりを軽減。ふうせんは「軽くて旋回しやすいが横幅が大きい」へ寄せ、`smax/sacc` を上げ、重すぎる確定ダメージ感を緩和。
> - **設計メモ**：`拡張設計メモ.md` を追加。固定ステージ化、BGM入れどころ、敵/ギミック候補、ステージ別モチーフ、機体/ショップ価格案、UIパーツ展開、雲境界線修正方針を記録。
> - **検証**：`node tools/smoke.cjs` OK（SPLASH追加済み）、`node tools/validate.cjs` OK。`tools/serve_lan.cjs` は前面起動ログは出るが、バックグラウンド起動後のcurl接続が失敗する状態だったため、HTTP確認は未完。
>
> ## ★Codex追記（2026-06-27 UIパーツ切り出し＋TITLE CTAへ9スライス導入）
> **実装済み・未push。** `assets/ui/ui_parts_v1.png` から個別パーツを切り出し、TITLEの「出発する」CTAにcoralボタン画像をCSS `border-image` で導入。
> - **追加素材**：`assets/ui/parts/button_coral_v1.png` / `button_cream_v1.png` / `sign_wood_v1.png` / `icon_round_v1.png` / `panel_cloud_v1.png` / `badge_coin_v1.png`。`assets/ui/_ui_parts_preview.html` も個別パーツ表示に更新。
> - **実装内容**：`#t-title .cta` をCSSベタ塗りから `button_coral_v1.png` の9スライス的な枠描画へ変更。文字はDOMテキストのままなので、以前のcanvas画像引き伸ばしによる扁平文字問題とは別構造。
> - **検証**：`node tools/smoke.cjs` OK。`curl.exe -I --max-time 5 http://127.0.0.1:8124/index.html?screen=TITLE` はHTTP 200。`Invoke-WebRequest` は今回タイムアウト気味だったためcurlで確認。
> - **次の一手**：実機でTITLE CTAのサイズ/押し感を見る。よければ同じ方式をDESTINATION/GARAGE/RESULTの主CTAや看板へ展開。違和感があれば9スライス値（現 `94 155 / 28px 56px`）を微調整。
>
> ## ★Codex追記（2026-06-27 コイン重なりFB対応）
> **実装済み・未push。** ユーザー実機FB「複数コインが重なって描画されるとバグっぽい」を受けて、全コイン生成入口 `addCoin()` に既存コインとの最小距離チェックを追加。近すぎるコインは追加しないため、線/弧の誘導は残しつつ重なりを防ぐ。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。validate出力では上かぶり/フロートの近傍コインが間引かれていることを確認。
> - **補足**：`tools/validate.cjs` へ明示的な重なり検査追加も試したが、既存ファイルの文字化け混じり文脈で安全なパッチ適用が不安定だったため今回は見送り。実装側の単一入口ガードで再発を抑止。
>
> ## ★Codex追記（2026-06-26 質感改善の画像生成着手・UIパーツv1）
> **実装済み・未push。** #17コミット後、質感の脱チープ方針に沿って画像生成素材を1セット作成。
> - **生成物**：`assets/ui/ui_parts_v1_green.png`（緑背景の原本）、`assets/ui/ui_parts_v1.png`（クロマキー除去済み透過版）、`assets/ui/_ui_parts_preview.html`（透過版/原本比較プレビュー）。
> - **内容**：9スライス検討用のテキスト無しUIパーツシート。coral主ボタン、cream副ボタン、丸アイコン、木看板、雲ガラス風パネル、金コインバッジ。まだゲーム本体には配線していない（切り出し/9スライス設計の前段）。
> - **生成手順**：built-in `image_gen` で `stylized-concept` として生成。緑背景版を `$HOME/.codex/generated_images/...` からコピーし、`C:\Users\mr_ba\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` + `remove_chroma_key.py` で透過化。素の `python.exe` はログオンセッションエラーで使えなかった。
> - **確認**：`view_image` で透過版を目視。`Start-Process assets/ui/_ui_parts_preview.html` はAccess deniedだったため、代わりに `http://127.0.0.1:8124/assets/ui/_ui_parts_preview.html` のHTTP 200を確認済み。
> - **次の一手**：必要ならこのシートから個別PNGを切り出し、9スライス用の座標/スライス幅を決めて、CTA/看板/モーダルの描画へ段階導入する。各画面背景生成は次パス。
>
> ## ★Codex追記（2026-06-26 #17対応・TITLEから遊び方ページ追加）
> **実装済み・未push。** #25コミット後、#17を対応。
> - **実装内容**：新規DOM画面 `HELP` / `#t-help` を追加。TITLE右上の `?` から開き、戻るでTITLEへ戻る。内容は「押すと上昇／離すと落下／横移動／かすめて得する／コインを集める」の5項目に絞った。
> - **ドキュメント更新**：`画面遷移.md` に `TITLE → HOWTO → TITLE` を追加し、TITLEには操作説明本文を載せずHOWTOに分ける方針を明記。
> - **検証**：`node tools/smoke.cjs` OK（HELP追加済み）、`node tools/validate.cjs` OK。サーバ経由 `http://127.0.0.1:8124/index.html?screen=HELP` はHTTP 200確認済み。
> - **次の一手**：質感の脱チープ。CSS微調整ではなく画像生成で、9スライス前提のボタン/看板/小物、各画面のシーン背景を作る。画像生成したら必ず `_preview.html` を開く。
>
> ## ★Codex追記（2026-06-26 #25対応・コイン配置を線/弧/グレイズ際へ再設計）
> **実装済み・未push。** #21の2階層化コミット後、#25を対応。
> - **実装内容**：コイン生成を `addCoin` / `addCoinLine` / `addCoinArc` 経由に整理。隊列ゲートの穴の縁にグレイズ狙いの縦線コイン、スウープ外縁に斜め線コイン、ダイブに潜って戻る弧、上かぶり/フロート/パルサー/bonus回廊に複数コインの線・弧を追加。単発点置き中心から「読む線」と「欲張る弧」へ寄せた。
> - **設計意図**：記録済みのマリオ知見どおり、コインを安全な飾りではなく「進路の線」「危険側への誘導」「グレイズ際の報酬」にする。全体枚数も増やして、コインを拾う気持ちよさを前に出す。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。validate上でも上かぶり/フロートの近傍コインが複数化していることを確認。
> - **次の一手**：#17 遊び方ページ（新規画面。TITLEから。`画面遷移.md`にフロー追加）。その後、質感の脱チープ＝画像生成素材（9スライス前提ボタン/各画面背景）。
>
> ## ★Codex追記（2026-06-26 #21対応・DESTINATIONを地域→サブステージの2階層へ）
> **実装済み・未push。** ユーザー確認で案A採用＝`DESTINATION`画面の中だけを2階層化（地域一覧→サブステージ一覧）し、sceneを増やさず既存 `DESTINATION` DOMを拡張した。
> - **実装内容**：`STAGE_REGIONS`を追加し、既存 `STAGES A/B/C/D` を `鳥の空域/雲海/嵐の空/星脈` に1本ずつぶら下げ。GARAGEの「行き先をえらぶ」からは地域一覧へ入り、地域カードを押すと同じ画面内でサブステージ一覧へ切替。詳細表示中の戻る/フッターは地域一覧へ戻り、地域一覧からはGARAGEへ戻る。クリア後の「つぎへ」は次ステージの地域詳細へ入る。
> - **ドキュメント更新**：`画面遷移.md`を「DESTINATION＝地域→サブステージ」に更新。今後 `A-2` のような追加ステージを地域配下に足せる前提を明記。
> - **検証**：`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。実機確認用に `node tools/serve_lan.cjs 8124` をバックグラウンド起動し、PC内 `http://127.0.0.1:8124/index.html?screen=DESTINATION&debug` はHTTP 200確認済み。ヘッドレス撮影は使っていない。
> - **未追跡**：作業開始時点から `assets/sprites/_ui_preview.html` が未追跡で存在。今回変更では触っていない。
> - **次の一手**：#25 コイン配置の再設計（線と弧／グレイズ際／全体増量）。その後 #17 遊び方ページ、質感画像生成。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #5・実機FB大量対応＋導入フライオーバー＋ボタン歪み修正。上限直前バトン）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。Claude分は全コミット済み・クリーン。`smoke`/`validate` 全通過。
> ### この回(Claude)でやったこと（実機プレイFBの#15-26＋追加修正を順次。気になりリストに✅反映済み）
> - **バケットA(見た目クイック)**：#15タイトル位置下げ＋⚙クリーン化／#16タイトルCTAを画像→crispなcoral DOM文字／#19ガレージのフッターを主従縦積み／#20行き先カードをガラス調で背景島を透過／#24結果モーダルのボタン左右対称。
> - **バケットB(演出)**：#18発射シーケンス(ボタンはける→ごごご間→飛び去る→ホワイトアウト→GARAGEフェードイン。`#whiteout`使用、go()被り廃止)／#26突入トランジション延長＋次の空の名前表示／#23**ステージ導入フライオーバー**。
> - **★導入フライオーバー(#23)を実機FBで何度も調整→現状**：突入時カメラがゴール高度から**一定速度(`INTRO_SPEED=700px/s`)でゆっくり下降**(中盤も読める/酔わない)→終端で減速→カバの所で「押してスタート！」点滅。**tapでスキップ**。実装の肝＝(a)`screenY`と`drawHippo`に`camOffset`を足してカメラを上(ゴール)へ＝カバは降りて来るまで画面外(b)`launch`/`relaunch`で`spawnAhead(stageTop+200)`して**コース全体を事前配置**＝障害物/鳥を下見可。(c)far-off-screen(`screenY<-H*1.3`)の敵は更新スキップ＝近づくまで凍結(挙動維持)。(d)鳥カリングの上限を`hippoY+1.6画面`→`stageTop`に(事前配置が消えるバグ修正)。(e)`spawnFloat`を`yBeat`基準に(hippoY基準だとスタート下にクラゲ集中する不具合)。(f)速度メーターは`camOffset<40`でだけ表示。
> - **★ボタン歪みの根治**：`btnDraw`が幅96px以上のボタンに`ui_btn_orange/back.png`を**指定サイズへ引き伸ばし**＝もどるが扁平＋色指定無視(全部オレンジ)だった。**画像パスを無効化(`imageButton=false`)→CSS描画に統一**(crisp文字/正しい色/歪み無し)。※これが前回の「画像が横に引き伸ばされ文字が扁平」の正体(canvasの伸びではなかった)。**将来画像化するなら9スライス必須**。
> - **設定画面**：ボタン等幅(212)・感度3択等間隔・配色に意味付け(青ON/coral選択/slate中立/赤破壊)。**クリアモーダル**：内容を上げ・加点を1行表示で重なり解消・透明枠を少し下げ。**fit()堅牢化**(visualViewport基準/load・回転で再fit)。
> - **knowledge追記**：[[UIの構図は先人の型から起こす]]に「**“ダサい/チープ”はCSS微調整では治らない＝構造はCSS/質感は画像で根治と仕分ける**」原則。
> ### ★残タスク（気になりリストの未対応＝バケットC/D/E）
> 1. **#21 行き先の2階層化**（地域→サブステージ。群れの空を押す→ズーム→ステージ選択）＝要設計・`画面遷移.md`更新。
> 2. **#25 コイン配置の再設計**（マリオ知見＝“線と弧”で誘導・グレイズ際に置く・全体増量。**調査メモは気になりリストに記録済**）。
> 3. **#17 遊び方ページ**（新規画面。TITLEから。`画面遷移.md`にフロー追加）。
> 4. **質感の脱チープ**＝原則どおり**CSSでなく画像生成**：CTA/看板/小物のKV画風素材化(9スライス前提)。背景は各画面のシーン生成([[画面フローを先に描く]]/[[Codexで画像生成する]])。
> ### 実機確認待ち（Claudeはヘッドレスで動画/PLAY中を撮れない＝要実機）
> - 発射シーケンス#18のタイミング感／フライオーバーの**下降スピード(`INTRO_SPEED`)が好みか・酔わないか・構成が見えるか**／事前配置の鳥が下見できて飛行中も残るか／設定ボタンの扁平・色が直ったか。※**ヘッドレス撮影が不安定(非432×768やPLAY中はハングしがち)**＝実機 or `node tools/serve_lan.cjs`でスマホ確認。
> ### 検証・運用
> - `node tools/smoke.cjs`／`node tools/validate.cjs`／`?screen=GARAGE|DESTINATION|SHOP|SETTINGS(&debug/&reset)`。画像生成したら必ず`_preview.html`を開く。コミットするがpushしない。
>
> ## ★Codex追記（2026-06-26 #4対応・GARAGE/DESTINATION分割＋背景v2採用）
> **実装済み・未push。** 旧SELECTを実ゲーム上で **GARAGE（機体選び）/ DESTINATION（行き先選び）** に分割した。`?screen=SELECT`は互換でGARAGEに読み替え、確認用は `?screen=GARAGE` / `?screen=DESTINATION`。
> - **DESTINATION未合意論点の採用方針**：選んだ機体の小表示＋推奨機体バッジは出す／DESTINATIONにコインは出さない／順序は `TITLE → GARAGE → DESTINATION → PLAY`。
> - **DESTINATION背景を生成**：最初の`destination_bg_v1.png`は細密ファンタジー寄りで、コビトカバ/鳥スプライトと絵柄差が強いため不採用・repoから除外。採用版は **`assets/ui/destination_bg_v2.png`**（丸い玩具レンダー寄せ、空に浮かぶ島/領域）。プレビューは **`assets/ui/_destination_preview.html`**。
> - **実装内容**：`index.html`に`#t-destination` DOM画面を追加。`#t-select`はGARAGEとして背景=`select_bg_v1.png`、ステージ選択を非表示にし「行き先をえらぶ」CTAへ変更。RESULTの「つぎへ」は次ステージを即開始せずDESTINATIONへ戻す。SETTINGSは開いた元画面へ戻るよう`settingsBack`を追加。初回DOM表示がバックグラウンド検証で出ない問題を避けるため、起動時に`syncDOMUI()`を一度即実行。
> - **ドキュメント更新**：`画面遷移.md`のDESTINATION要検討を採用方針へ更新済み。
> - **検証**：`node -e`構文OK / `node tools/smoke.cjs` OK（GARAGE/DESTINATION追加） / `node tools/validate.cjs` OK。in-app browserで`?screen=GARAGE`/`?screen=DESTINATION`を目視し、DOM表示・ロック状態・v2背景参照を確認。
> - **次の一手**：実機で画面遷移を触って、DESTINATIONカードの密度・ロック時の可読性・背景とUIの馴染みを微調整。余力があればSETTINGS/RESULTもDOM土台へ移行し、旧canvas版`drawSelect`/`drawShop`を整理。
> - **Codex追加微調整**：ユーザーFB「ガレージのカバが上すぎる」「タイトルのカバ上昇が中途半端」を反映。GARAGEの機体表示をデッキに乗る位置へ下げ、機体名/説明は白＋影で可読化。TITLE発射アニメは画面外へ抜ける距離と尺を増やし、GARAGE遷移開始を後ろへずらした。スマホ実機確認用に `tools/serve_lan.cjs` を追加（例：`node tools/serve_lan.cjs 8124`）。このPCのWi-Fi IPは確認時点で `192.168.10.119`、URL候補は `http://192.168.10.119:8124/`。PC内 `127.0.0.1:8124` はOK、LAN IP直打ちは接続不可だったため、スマホで開けない場合はWindows FirewallでNode.jsのプライベートネットワーク受信許可が必要。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #4・UI基盤の作り直し＝シーン背景/画面分割/フロー。上限直前バトン）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。Claude分は全コミット済み。
> ### この回(Claude)でやったこと＝UIの“上流”を作り直した（重要な方針転換）
> - **TITLE/SHOP/SELECTをDOM/CSS化済み**（前パス）。だが「ボタンがチープ/色がケンカ/急にオレンジ/変なグラデ」とFB。診断→**根本は“上流の設計工程”を飛ばしていたこと**だった。以下を順に確立：
> - **① 色テーマをKVから抽出**：`title_kv_v5.png`の実色を採取＝空青`#3A7AD4`/ピーチ`#F4B07A`/**ロケットの赤錆coral`#E85D43`(=主アクセント)**/cream`#FFF4E2`/ink`#2A3550`/plum`#5B4167`/gold`#FFCB3E`。「急にオレンジ」は不採用、**主アクション色=coral(ロケット色)**に。1画面=主アクセント1つ。根拠＝`assets/ui/_theme.html`。
> - **② “背景はグラデでなくシーン”に転換**（ユーザー指摘「ガレージならガレージらしい背景」）。**Codex画像生成で雲上ガレージ背景を生成→`assets/ui/select_bg_v1.png`**。UI合成プレビュー＝`assets/ui/_select_scene.html`（背景＋カバ＋coralボタン＝製品級になった）。**今後の背景は各画面のシーンを生成する方針**。
> - **③ 画面フロー(遷移図)を作成**（ユーザー指摘「遷移図描いてなかったから概念ズレた」）。`画面遷移.md`＋`assets/ui/_flow.html`。**旧SELECT(機体＋行き先)を GARAGE(機体だけ) と DESTINATION(行き先だけ) に分割**＝1画面1決定1場所。戻り(back)動線も明記。
> - **④ 画面ごとの要件 認識合わせ**：`画面遷移.md`「画面ごとの要件」に各画面の必須要素/載せないもの/見せ方を記述。**DESTINATIONは要合意の論点あり**（下記）。
> - 方法論はknowledgeに昇格：[[画面フローを先に描く]]（工程0＋画面ごと要件合わせ）/[[UIの構図は先人の型から起こす]]。**工程＝①フロー→②画面ごと要件→③構図(先人の型)→④DESIGN.mdトークン→⑤シーン生成・実装**。
> ### ★次の一手（この順で。まずDESTINATIONの認識合わせから）
> 1. **DESTINATIONの未合意論点をユーザーに確認**（`画面遷移.md`記載）：①選んだ機体の小表示＋推奨機体バッジを出すか ②コイン表示要否 ③GARAGE→DESTINATIONの順 vs 逆順。**行き先は「空に浮かぶ島/領域」として空間的に見せる**のが要件（リスト羅列にしない）。
> 2. **DESTINATIONのシーン背景を生成**（雲に開けた発射デッキ／空に行き先の島。画風＝KV/garageと統一。`-i assets/ui/title_kv_v5.png assets/ui/select_bg_v1.png`。生成→`assets/ui/`へcp→`_preview`を開く）。
> 3. **実ゲームに組込**：`scene`の`SELECT`を **`GARAGE`/`DESTINATION`に分割**。GARAGE＝機体カルーセル＋ステータス＋ショップ＋「行き先をえらぶ」、背景=`select_bg_v1.png`。DESTINATION＝行き先選択＋「これで出発」、背景=新規。**coralパレットを全DOM画面に適用**（`_theme.html`の役割）、変なCSSグラデは廃しシーン背景に。**戻り動線**は`画面遷移.md`通り。
> 4. **ボタン等の画像化**（ユーザー希望＝フル画像UI）：主役CTA/看板など“ご褒美”が要る所からKV画風で生成。小物(chip/coin/gear/pip)はCSS+coralで可。
> 5. 旧canvas`drawSelect`/`drawShop`は保険で残置中。全DOM化が固まったら整理。
> ### 検証・運用
> - `node tools/smoke.cjs`/`node tools/validate.cjs`、`?screen=...&debug`/`&reset`で目視。`.shots/`・`.chrome-*-profile`はgitignore済。**画像生成したら必ず`_preview.html`をStart-Processで開く**。コミットするがpushしない。
> ### 根拠ファイル一覧（消さない）
> - `画面遷移.md`(フロー＋画面ごと要件＝正本) / `assets/ui/_flow.html`(遷移図) / `assets/ui/_theme.html`(KV由来パレット) / `assets/ui/select_bg_v1.png`(ガレージ背景) / `assets/ui/_select_scene.html`(背景+UI合成) / `_shop_*.html`(SHOP構図根拠) / `_ui_polish.html`(チープ要因の診断)。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #3・SHOPをV3で確定→Claudeが実装まで完了）
> **担当はまだClaude（上限未達のため継続）。** push しない。`AGENTS.md`「双方向バトンパス」準拠。
> ### この回(Claude)でやったこと＝SHOPを参照ドリブンで設計→DOM実装まで
> - **参照ドリブン設計**（先人の型を実例深掘り→定石抽出→安いHTMLモックで構図確定）を実施。新ナレッジ `knowledge/UIの構図は先人の型から起こす.md` に工程を明文化。
> - **SHOP構図＝V3「下隅の案内役」型に確定**。根拠モック＝`assets/ui/_shop_mock.html`(A〜D)／`_shop_a_refined.html`(A定石反映)／`_shop_placement.html`(店主の置き方V1〜V3)。**`DESIGN.md`のShop節がV3仕様の正本**。不採用＝上ヒーロー型（「店主が大きすぎて商品が探しにくい」）。
> - **✅ V3を`index.html`にDOM/CSSで実装済み**（タイトルと同じ`#ui`オーバーレイ機構）：
>    - 新規 `#t-shop`（CSS＋HTML）。**店主＝左下に半見切れの固定オーバーレイ**(`shop_merchant_sunglasses.png`)＋吹き出し。**商品棚＝全幅スクロール**を店主の上に。**ヘッダー(看板/通貨右上/もどる左上)・店主は固定、スクロールは棚だけ**。空グラデ＋ひさし/看板/棚はDOM/CSS、商品＝機体スプライト＝**重いAI生成ゼロ**。
>    - JS：`syncDOMUI`が`scene==='SHOP'`で`#t-shop`を表示し`renderShop()`。`renderShop`は`VEH_META.buy`の機体だけカード化（絵→名前→一言→価格→購入の固定順、購入ボタンだけアクセント色）、所持=「そうび中/所持ずみ」、資金不足=グレー化。購入は`buyVeh`→即再描画。**スクロールが要る時だけ**「▾もっと見る」/フェード表示。canvasの旧`drawShop`は`domShown==='shop'`の時は描かない（`?screen`等の保険で残置）。
>    - 検証：`node tools/smoke.cjs` OK、ヘッドレスChromeで`?screen=SHOP&coins=120/200`を目視（`.shots/shop_v3*.png`＝gitignore）。購入可/不可の出し分け・店主固定・棚スクロールを確認。
> ### ✅ 追記：SELECTもDOM化完了（雲上ガレージ＝カルーセル型）
> - **`#t-select`をDOM/CSSで実装**（同じ`#ui`オーバーレイ）：ヘッダー(ギア/タイトル/コイン右上)＋**機体カルーセル**(中央大＋左右プレビュー＋‹›、所持1つの時は‹›と side を自動非表示)＋名前/説明/**ステータスバー(加速/軽さ/旋回の5pip)**＋**ステージ選択**(解放分のみ・★/ベスト表示・選択中ハイライト)＋フッター(出発する→`go(launch)`／ショップ→SHOP)。
> - JS：`renderSelect()`が`save`から全部組む。`selPick(±1)`で機体送り。canvasの旧`drawSelect`は`domShown==='select'`の時は描かない。`smoke.cjs`/`validate.cjs` OK、`?screen=SELECT&debug`/`&reset`で複数所持/単一所持の両方を目視確認。
> ### ★次の一手（仕上げ＋残り画面）
> 1. **SETTINGS / 結果画面も同じDOM土台へ**（最後のcanvasメニュー。`knowledge/UIの構図は先人の型から起こす.md`の工程で）。
> 2. **微調整・質感UP**（任意・実機）：SHOP店主の大きさ/位置、SELECTカルーセルの送り演出、ボタン/看板の画像素材化（DESIGN.md「Shared UI Parts」）。
> 3. 旧canvas版`drawSelect`/`drawShop`は`?screen`等の保険で残置中。全DOM化が固まったら整理してよい。
> ### 前回Codexの未コミット在庫の扱い（整理済み）
> - `shop_merchant*` / `ui_btn_*` スプライトは**今回のコミットに取り込み済み**（index.htmlのpreload/SHOPが参照するため）。`ui_btn_*`画像は現状の実装では未使用（SHOPのボタンはCSS製）だが、SELECT/結果のボタン画像化で使える在庫として残す。`assets/states/*`は各PNGのcutout元（再生成用）。
>
> ## ★Claude→Codex 申し送り（2026-06-26 #2・タイトル画面のデザイン刷新パス）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。全部コミット済み・クリーン。
> ### この回でやったこと（タイトル画面を“1枚のデザイン”として作り直し・全コミット済み）
> - **メニューUIをDOM/CSS化**（canvasと同じ仮想解像度432×768に`transform:scale`で重ねる`#ui`オーバーレイ＝同期はfit()の1行。プレイ画面はcanvasのまま）。**タッチで進めない不具合も修正**（DOMメニュー表示中=domShownはtouchのpreventDefaultをスキップ＝ボタンのclickを潰さない）。
> - **タイトルのキービジュアル(KV)を反復生成して確定**。最終＝`assets/ui/title_kv_v5.png`（背中ロケットで自力飛行・暖色の雲→星空へ上昇・健気な決意の表情・一貫ポップ画風）。**Codex画像生成をClaudeが駆動**。Geminiは却下。
> - **★KV=完成像 → “画風一致の動かせる素材”に分解して実装**（ユーザー方針）：①`title_bg_v5.png`=カバ抜き背景プレート ②`title_hippo_green.png`=背中ロケットで飛ぶカバを同画風+クロマキー緑で生成→`tools/cutout.cjs`で透過。DOMタイトル=背景プレート＋透過カバ（常時ふわふわ`hippoFly`＋「出発する」タップで上にビュン`hippoLaunch`→go()でSELECT＋バイブ）＋`twinkle`。**古いゲームスプライト/CSS層(hero/鳥/雲)は全部非表示=素材流用ゼロ・全部v5画風で整合**。`?screen=TITLE`不要、デフォルトでTITLE表示。
> - **art-bibleに設計の芯を明文化**（重要・厳守）：(a)**キャラの芯=「決意に満ちて応援したくなる健気なunderdog」。こぶし突き上げ/勝ち誇りは解釈違い**。(b)**飛び方の正=背中ジェットパックで自力飛行。ロケットに乗り込む/座るは概念ミス**。(c)**行き先=空の上/星・宇宙(星脈)。山・頂・陸は描かない**（「登山/頂上」は比喩）。(d)**KVは一貫ポップ画風で完結。半写実×トイの混在禁止。ゲーム素材を流用せずKV画風で新規素材を作る**。
> - **画像レビュー運用を徹底**：生成のたびに`assets/ui/_title_preview.html`を更新して`Start-Process`で開く（人がフォルダを掘らないで済むよう）。**Codexも生成したら必ずプレビューを開くこと**（HANDOFF素材パイプライン参照）。
> ### ★次の一手（タイトル仕上げ→他画面へ）
> 1. ✅ **ロゴのデザイン**：Codexで文字込み生成の`assets/ui/title_logo_v2.png`を採用候補として実装。既定の白文字/CSSフォント合成は撤回し、v5画風の雲プレート＋星＋ロケット噴射＋手描き風「カバロケット」ロゴに置換。生成元は`title_logo_v2_green.png`、旧CSS合成用ベースは`title_logo_base*.png`として比較用に保持。
> 2. **微調整**：透過カバの位置/サイズ(`.titlehippo` width/top)、CTA質感、きらめき量、背景の`object-position`。
> 3. **同じKV方式を他画面へ**：SELECT/SHOP/SETTINGS/結果も「KV(背景)＋画風一致の動かせる素材＋DOM UI」で“魂”を揃える。
> ### 開いた宿題（前パスから継続）
> - **画像素材の質**（敵/雲/UIを同じライティング/輪郭/影で一括更新）。**boost/hit差分スプライト**（fall同様の方式）。**落下fall.bodyHの実機微調整**（`?cap&veh=N&pose=fall`）。詳細は下の「気になってることリスト」#1-14。
> - **「初期は何で作るか」のベスプラをknowledge化**（UI=最初からDOM/CSS）。**中身設計の発散→収束**はknowledge追記済。
> ### 2026-06-26 Codex追記：タイトルロゴv2＋カバ位置調整
> - ✅ **文字込みロゴを生成・実装**：`assets/ui/title_logo_v2.png`を追加し、TITLE DOMは`<img class="logo-img">`で表示。日本語フォントの既製感が強かったため、ロゴ文字も画像側で作った。`assets/ui/_title_preview.html`も更新し、`Start-Process`でプレビューを開いた。
> - ✅ **カバ位置を下げた**：`.titlehippo`を`top:66% / width:68%`へ調整。上部ロゴの領域とカバの決意表情がぶつかりすぎない構図にした。
> - ✅ **検証**：`node -e`構文OK、`node tools/smoke.cjs` OK、ロゴPNGの四隅alpha=0確認OK。敵/機体/ステージは触っていないので`validate.cjs`は未実行。Chromeヘッドレスで`.shots/title_logo_v2.png`を撮影して目視確認（スクショ/Chromeプロファイルは一時ファイル）。
> - **次の一手**：タイトル内の微調整（CTAの位置/質感、ロゴの最終サイズ、カバの上下）を実機で見て詰める。良ければ同じKV方式をSELECT/SHOP/SETTINGS/結果へ展開。
> - **2026-06-26 Codex追加調整**：ユーザーFB「カバがまだ上すぎる」「ボタン/UIパーツが質素で統一感がない」を反映。`.titlehippo`を`top:66% / width:68%`へさらに下げ、CTA/設定ボタンをロゴと同じクリーム縁＋青リム＋オレンジ立体の質感へ寄せた。`node -e`構文OK、`node tools/smoke.cjs` OK、Chromeヘッドレスで`.shots/title_ui_polish.png`を目視確認（スクショ/Chromeプロファイルは一時ファイル）。
> - **2026-06-26 Codex追記：UI全体の画面構想をDESIGN.mdへ追加**：ユーザーFB「ボタン自体も画像にした方がよい/SELECTやSHOPも磨きたい/SHOPは商人コビトカバ案」を受けて、`DESIGN.md`に`Screen Direction`を追加。方針は①大ボタン/購入ボタン/戻るボタンを画像ベース+DOM文字へ ②SELECT=雲上ガレージ/発射デッキ ③SHOP=商人コビトカバの雲上屋台＋商品棚 ④SETTINGS/RESULTも共通素材へ。外部参考では、NPC/店構えが上、商品カードが下、通貨右上、購入ボタン商品ごとの型が多かったため採用候補にした。次はこの順で実装。
> - **2026-06-26 Codex追記：SELECT/SHOP磨き込み途中**：共通UI素材を生成・追加（`ui_btn_orange.png`=無地ボタン、`ui_btn_start.png`=文字込み「出発する」、`ui_btn_shop.png`=文字込み「ショップ」、`ui_btn_back.png`=文字込み「もどる」）。SHOP商人はユーザーFBでサングラス版`shop_merchant_sunglasses.png`を生成し、SHOP上部に雲上屋台＋商人＋商品棚を実装。SELECTは雲上ガレージ見出し/明るい展示台/文字込み「出発する」「ショップ」ボタンへ寄せた。文言は「出撃」ではなく「出発」に統一。`node -e`構文OK、`node tools/smoke.cjs` OK、PNG四隅alpha=0確認OK。ChromeスクショはSELECT/SHOP途中確認済みだが、追加撮影と一時ファイル削除/コミット操作は承認側の利用上限で止まる可能性あり。
>
> ## ★Claude→Codex 申し送り（2026-06-26・上限直前のバトン）
> **担当交代＝以後Codex。** push しない。`AGENTS.md`「双方向バトンパス」準拠。全部コミット済み・クリーン。
> ### この回(Claude)でやったこと（全コミット済み）
> - 見た目FB一次対応の取込／**ゴール決勝バナー化**／**敵スプライトに接地影**／**雲の入場フェード**＋**雲海の即時描画**／**画面遷移トランジション(go())**／**ふうせん経済の作り直し(coinVal=拾得時ライブ適用)**／**こつぶ廃止**。
> - **各装備の落下パニック・スプライト**(つばさ/ふうせん/コメット/マグ)をCodex画像生成→cutout→drawHippoが状態別スプライト自動使用。**落下時に体が激変する不具合を修正**(状態別`VEH_ART[].fall.bodyH`で体高正規化)。※**fall.bodyHの最終微調整はCodex任せ**＝中間値で仮置き。`?cap&veh=N&pose=fall`で確認。小さいほど体が大きい。
> - **メニューUIのDOM/CSS化を開始**＝canvasと同じ仮想解像度に`transform:scale`で重ねる`#ui`オーバーレイ(同期はfit()の1行)＋**タイトル画面をHTML/CSSで試作**。プレイ画面はcanvasのまま。`scene==='TITLE'`でDOM表示・タップ→SELECT/ギア→SETTINGS・DOM表示中はcanvas入力停止。
> ### ★次の大仕事＝「タイトル/UIに“デザインの魂(こだわり)”を入れる」(ユーザー指摘・最優先)
> 今のDOMタイトルは**構造の勝ち(DOM路線の実証)だが、デザイン哲学が無いスキャフォールド**。ユーザー批評そのまま：
> - 「**この画面でワクワクするのか？**」＝**狙う感情/コンセプトが無い**。まず「タイトルで何を感じさせるか」を1文で決める(例：ずんぐりカバが空へ挑む“コミカル×温かい×上を目指す高揚”)。それを構図・動き・色で実装する。
> - 「**後ろの雲が同じものの連続**」＝**手抜きに見える**。タイル反復をやめ、雲は形・大きさ・速度・ボケを変えた複数層＋遠景に“頂上(ゴール)”や山影、鳥/きらめき等で**奥行きと物語**を出す。
> - 「**タイトルにコイン情報いる？**」＝**情報設計の精査**。タイトル＝ブランド＋開始に絞る。コイン/所持は SELECT/SHOP の領分。**コインピルは外す**方向。ギアも控えめに。
> - 「**タイトルの出方これでいい？**」＝**登場演出**(ロゴがバウンドで降る/カバが発射台にトンと乗る/CTAが遅れて出る等の入場アニメ)。
> - 「**配色は？**」＝**意図あるパレット**を決める(例：朝焼け＝新たな冒険、空青＋暖色アクセント＋カバのプラム色)。**トークン化**して全UIで統一＝knowledge [[DESIGN.mdでUIのテイストを固定する]]に沿って**`DESIGN.md`(YAMLトークン＋意図の散文)を作る**→以後の全画面(SELECT/SHOP/SETTINGS/結果)もそれで起こす。
> - 「**デザイナーの魂が無い(こだわりが無い)**」＝**既定値の寄せ集めを脱する**。frontend-designの観点(明確な階層/1アクセント/意図的な余白/テンプレに見えない選択)で、**hero(カバ)を主役に・UPの引力・ゴールの示唆**を効かせる。カバは座らせるより**噴射で昇る/上を見上げる**等“動き”を。
> ### Codexの進め方(おすすめ順)
> 1. **`DESIGN.md`を作る**(コンセプト1文＋ムード＋カラートークン＋タイポ＋余白/角丸/影の基準)。knowledge [[DESIGN.mdでUIのテイストを固定する]]の雛形。
> 2. それで**タイトルを作り直す**(コンセプト→構図→非反復の奥行き背景→入場演出→情報を開始に絞る)。背景キービジュアルは必要ならCodex画像生成(1枚絵)も可。
> 3. 良ければ **SELECT/SHOP/SETTINGS/結果も同じDOM土台＋DESIGN.mdで順次DOM化**(座標/hotspotはcanvas版を参考に。プレイ画面はcanvasのまま)。
> ### 2026-06-26 Codex追記：タイトル/UIデザイン土台を実装
> - ✅ **`DESIGN.md`追加**：コンセプト1文「ずんぐりしたコビトカバが、朝焼けの空へ本気で挑むコミカルで温かい上昇感。」を正本化。朝焼け空/プラム/オレンジCTA、M PLUS Rounded 1c、余白・影・角丸・タイトル情報設計をトークン化。
> - ✅ **DOMタイトルを作り直し**：`index.html`のTITLE DOM/CSSを差し替え。横反復の雲を廃止し、個別配置の雲5層＋遠景の山/ゴール＋きらめきで奥行きを作成。ロゴ落下、カバ/発射台の着地、CTA遅延出現の入場演出を追加。
> - ✅ **情報設計を開始に絞る**：タイトル右上のコインピルを撤去。表示はブランド、短い気持ちの一文、開始ボタン、控えめな設定アイコンのみ。コイン/所持は従来通りSELECT/SHOP側の領分。
> - ✅ **検証**：`node -e`構文OK、`node tools/smoke.cjs` OK、`node tools/validate.cjs` OK。ヘッドレスChromeで432x768/1280x760のTITLEスクショを目視し、文字はみ出し・旧UI露出なし。
> - **次の一手**：このDESIGN.mdを読ませながら、SELECT/SHOP/SETTINGS/結果を同じDOM土台へ順次寄せる。まずSELECT（装備選択）で、コイン/所持/ステージ情報をタイトルから移した受け皿として整えるのが自然。
> ### その他の開いた宿題
> - **画像素材の質**(ユーザー希望＝安っぽくない/フリゲっぽさ薄い/AI素材感薄い)：素材生成時に**ライティング/輪郭/影のルールを固定**して敵/雲/UIを一括更新(art-bible＋DESIGN.md連動)。雲は3〜5種へ。
> - **「初期は何で作るか」のベスプラを knowledge 化**(未)：ゲームは“プレイ=canvas / UI=DOM/CSSを最初から”。canvas手描きUIを仮スキャフォールドにすると恒久的にダサい。固定解像度ゲームはscaleラッパー1枚でDOM重ねが安い。LLMはHTML/CSSのUIが最も得意=DOM化でAI支援も最大化。→ Claudeが次回 knowledge に起こす(ユーザー要望)。
>
> ## ★気になってることリスト（ユーザー発・未解決のみ。これが正本）
> 2026-06-26 ユーザーが口頭で挙げた気になり点。Codexへ口頭で伝えたが上限で文字化できていなかった分を、ここに集約＝**散らさずここを更新する**。タグ＝〈✓済/要素材=画像生成/要実機=数値チューニング/設計=方針判断〉。
> 1. **雲が単調＋急にボンって出る**：「急に出る」は読み込みフェードで一次対応✓／「1種類で単調」は**要素材**（遠景薄雲・中景雲海・近景綿雲・縦流れ雲片の3〜5種）。
> 2. **スタートから近い雲が見えすぎ**（遠い雲が見えるのはOK、近くが多すぎ）：高度ゲートで一次対応✓→**要実機**で効き確認。
> 3. **全体のAI感・はりぼて感**（背景/雲/敵/オブジェ/ゴール/ボタン/モーダル）：**コード描画で潰せる分はほぼ消化**＝ゴール決勝バナー化✓・敵スプライトに接地影✓(貼っただけ感解消)・ボタン/モーダル/タイトル/ショップは監査済で既に良好(立体ボタン/影/ツヤ有)。**残るのは要素材**＝カバ/鳥/雲スプライトそのもののAI感(再生成しないと根本解決しない)＝Codexの画像生成案件。pad/地面は現状許容範囲。
> 4. **装備選択が崩れてる→カルーセルで選択中を大きく**：✓対応済（中央大プレビュー＋左右薄＋‹›、`?screen=SELECT&debug`で確認済）。
> 5. **鳥/障害物のバリエ・配置・ステージ増にまだ伸びしろ**：未着手。**★ルーティング指示＝敵バリエは従来Claude起点が多かったが、次はCodex起点の案を見たい**（CodexにキックオフでこのNo.5を渡す）。
> 6. **お金のマークが変**：`¢`→丸コイン描画に置換✓。
> 7. **クリア後カバがゴールでぴくぴく**：停止処理で✓（boost切/速度0/bob0/cleared時idle）。
> 8. **BGM/SE作る（Geminiで予定）→良いBGMリスト欲しい**：ステージとは別に**ショップ/装備/設定・ポーズも個別BGM**にしたい。候補は下の「Gemini向けBGM候補」に整理済み＝そのまま渡せる。
>
> **追加バッチ（2026-06-26 #2／Claudeが改善ループで対応）**
> 9. **装備画面の背景雲がフェードしない（popする）**：✓対応（drawMenuCloudsに入場フェード、sceneT基準でふわっと下から）。
> 10. **雲海ステージが突入後に初めて雲描画される**：✓対応（B限定で高度ゲートに下限を入れ突入直後から雲を敷く）。
> 11. **出発ボタン後の画面切替が唐突＝フリゲっぽい**：✓対応（`go()`遷移カバー＝カバーin→中点で実切替→カバーout、ロケット閃光。ゲーム⇄メニュー境界に適用）。
> 12. **ふうせんが「1.5倍だが1枚ずつ拾うので無意味なデメリット機体」**：✓対応（coinVal()で拾得時にライブ適用＝ふうせん2倍、+2ポップ表示。最終バランスは**要実機**）。
> 13. **機体の種類が少ない**：**方針確定(2026-06-26)**＝コビトカバ路線継続・**動物スキンは保留**・**こつぶ廃止✓**・**装備ごとに画像を書き出す**。**各装備の落下パニック画像＝✅完了(2026-06-26)**＝つばさ/ふうせん/コメット/マグの`veh_*_fall`をCodex生成(fall.png主参照でトーン統一)→cutout→drawHippoが状態別スプライトを自動使用(中央基準描画・左ズレ修正済)。`?cap&veh=N&pose=fall`で確認可。**残る不足画像＝boost/hit差分**(同方式で量産可、参照をboost/hit.pngに替えるだけ)。新機体そのものを増やすなら[機体設計.md]「★候補ブレスト拡張/次に出す機体」(ダッシュ/バブル＝発注文確定済)。
> 14. **コイン以外の収集物・強化・回復**：**収集→開放を採用(2026-06-26)**＝コイン(ショップ装備)と別トラックで、ステージ毎の隠し収集物コンプ→ショップ外の特別装備/(将来)動物スキン解放。設計＝[開発計画.md](開発計画.md)「★収集→開放(2トラック進行)」。強化要素の方向(rare回復ハート/グレイズ倍率可視化○、恒久強化×)も同doc。実装は装備カタログ確定後の中規模機能。
>
> **実機プレイFBバッチ（2026-06-26 #3／ユーザーが実機で触っての感想）** タグ＝〈実装=コードで直せる/設計=方針判断/要研究/新規=新画面・機能/要素材=画像生成〉
> ＝＝タイトル＝＝
> 15. **タイトルが上すぎ→設定⚙が隠れて押せない＋⚙がダサい**：〈実装+要素材〉タイトル位置を下げ⚙の当たり/位置を確保、⚙をKV画風へ再デザイン。
> 16. **ボタン内の文字が全体的につぶれてる**：〈実装/要素材〉文字込み画像ボタンの可読性。DOM文字に戻す or 画像を作り直す。
> 17. **遊び方ページが欲しい**：〈新規〉How-to-play画面をフローに追加（TITLEから）。
> 18. **出発演出が中途半端**（飛ぶ→出発ボタン再表示→カクッと次へ）。理想＝押す→タイトル/ボタン消える→炎ごごご→上へ飛び去る→ホワイトアウト→次へ：〈実装/演出〉発射シーケンスを作り直す。
> ＝＝雲上ガレージ＝＝
> 19. **「行き先をえらぶ/ショップ」のボタンの大きさ・デザインがダサい。ショップが小さい・並列が筋悪いかも**：〈設計+実装〉フッターのボタン階層を再設計（並列をやめる／主従を明確に）。
> ＝＝行き先をえらぶ＝＝
> 20. **マップ絵にモーダル(カード)が重なって背景が見えない**：〈設計+実装〉カードが背景を覆っている。シーン背景を活かす配置/透過に。
> 21. **行き先4つそれぞれに“複数マップ”を**（群れの空を押す→群れの空にズーム→ステージリスト→ステージ選択→突入）：〈設計/大きめ〉DESTINATIONを「地域→サブステージ」の2階層に。フロー＆コンテンツ拡張＝要設計（画面遷移.md更新要）。
> 22. **行き先ページのボタンもダサい**：〈実装〉#16/#19と同根（coralパレット/画像化で一括）。
> ＝＝ステージ(PLAY)＝＝
> 23. **開始時にゴール→カバまでカメラを下げてマップ全容を見せるイントロビュー**が欲しい：〈実装/演出〉ステージ導入フライオーバー（突入時に上から下へパン）。
> 24. **クリア時モーダルのボタンがずれてる**：〈実装/バグ〉RESULTモーダルのボタン位置を修正。
> 25. **コイン配置をもっと“誘導”っぽく＋基本は増やす**：〈要研究済→設計〉下記マリオ知見で配置を再設計。
> 26. **ステージ突入時の切替(トランジション)画面が短すぎて役割を果たせてない**：〈実装〉尺と内容を見直す（次の空の名前/ローディング感）。
>
> **★マリオのコイン配置＝調査メモ（#25用・出典 [gamedeveloper SMB3](https://www.gamedeveloper.com/design/super-mario-bros-3-level-design-lessons-part-1) / [gamedev.net](https://www.gamedev.net/tutorials/game-design/game-design-and-theory/super-mario-bros-3-level-design-lessons-part-2-r4707/)）**
> ①**道標**＝コインは進ませたい経路に沿って“線(トレイル)”で置く（斜めトレイルでジャンプ/移動を誘導）。②**自然な軌道上**＝ジャンプの放物線＝“うまく動けば自然に拾える”位置。③**リスク/リワード**＝危険な所(崩れ床・敵際)に置くと挑戦が報われる（hippoの**グレイズ**と相性◎）。④**先読み/誘導**＝安全レーンや分岐・秘密をコインで予告。⑤**量は多め**＝達成感＋一定数でご褒美。
> → hippo-rocket適用：**上昇の“抜け道”に沿ってコイン列**を置き敵の隙間へ誘導／**グレイズ際にコイン**を置いて攻めを報酬化／全体的に**増量**。ばら撒きでなく“線と弧”で配置する。
>
> **✅ バケットA（見た目クイック修正）対応済み（2026-06-26 #3 Claude）**：#15タイトル位置下げ＋⚙クリーン化(白丸/空青アイコン)／#16 CTAを画像→crispなcoral DOM文字／#19ガレージのフッターを主従縦積み(行き先=全幅主役・ショップ=小さい副)／#20行き先カードをガラス調(blur)にして島背景を透過／#22行き先ボタンcoral(Codex既済)／#24結果モーダルのボタンを左右対称(W/2±70・幅132)＋主ボタンcoral。
> **✅ バケットB（演出・アニメ）対応済み（2026-06-26 #3 Claude）**：#18発射シーケンス作り直し(出発押下→ロゴ/ボタン消える→炎噴射プルーム+ラムブル→カバ画面外へ→ホワイトアウト#whiteoutでGARAGEへ切替・go()被り廃止)／#26突入トランジション延長(0.42→1.15s)＋次の空の名前表示(go()にlabel/sub)／#23ステージ導入フライオーバー(突入カバー後にゴール高度→カバへcamOffsetをease-in-out・タップで素早く詰める)。**※#18#23は実機で手触り確認推奨**(クリック/ready起点でヘッドレス動画確認不可)。
> **残り**＝C行き先2階層(#21・要設計)／Dコイン配置(#25・マリオ知見で線と弧+増量)／E遊び方ページ(#17・新規)。**“ダサい”質感系は[[UIの構図は先人の型から起こす]]の原則どおりCSSでなく画像生成で根治**＝CTA/看板/小物のKV画風素材化が次の質感タスク。
>
> **実機FBバッチ（2026-06-27 #4／ユーザー実機・Claudeが構造バグを一括修正）** タグ＝〈実装=CSS/canvasで直る構造バグ／要素材=画像生成／設計=方針判断〉
> ＝＝今回Claudeが修正済み（コミット f944d37〜直近）＝＝
> 27. ✅ **GARAGEでコイン表示と⚙が完全に重なる**：戻る/⚙を左に並べ、coinを右上単独に。
> 28. ✅ **DESTINATIONの二重フレーム**：`.dstcard.stage`/`.dstpick`がborder-image(雲パネル)＋背景の二重重ね→単層の角丸ガラスに統一。
> 29. ✅ **選択中ステージが分からない**：`.dstcard.stage`(2クラス)が`.on`を上書きしてcoralにならず→3クラス`.dstcard.stage.on`で明示。
> 30. ✅ **ステージ文字の白抜き/黄色が見えない**：`.dstmeta`が金(#FFCB3E)で明パネル上不可視→濃色に。region名は濃色＋淡ハロ。
> 31. ✅ **サブステージが二重フレーム＋縦長**：min-height 156→78pxに圧縮し“ズームした一覧”感に。
> 32. ✅ **「これで出発」改行**：ボタンにwhite-space:nowrap。
> 33. ✅ **設定の進行データ説明文がはみ出す**：中央寄せ明示＋短文化。
> 34. ✅ **SHOP看板が大きすぎ**：border-imageスライス幅を細く＋棚位置を上げ商品スペース回復。
> 35. ✅ **遊び方の図解(穴/かすめ/コイン)でカバと鳥/コインが接触**：図箱を92×62→122×82に拡大し、両端/すきま/斜め線で必ず離す配置に。
> 36. ✅ **初期配置の鳥が同一Yへ潰れて重なる(特に小鳥)**：突入事前配置で`aheadSpawnY`が全spawnを画面上端クランプ→開始付近の複数陣形が同一Yへ。preSpawning中はクランプ除外し真Yに展開。validateに過密スタック検査追加。
> 37. ✅ **TITLEの出発するボタンが下すぎ**：bottom 48→96。
> 38. ✅ **敵/雲の貼り付き感**：霧バンクを呼吸ゆれ、鳥に羽ばたき傾き、雷雲にドリフト＋**落雷(13%周期のギザギザ＋フラッシュ)**。`?cap&demo=storm&flash`で確認可。
> ＝＝未対応（残す）＝＝
> 39. ✅ **行き先の地域ネームプレートを“島の位置”に置く**：背景4島の座標(`REGION_POS`)に絶対配置＝島が見えたまま名前が島に乗る。下部2×2ドックは廃止。
>
> **実機FBバッチ（2026-06-27 #5／DESTINATION再設計＋細部。Claude）** ＝下記すべて✅対応済み（コミット済み・未push）
> 40. ✅ **地域プレートが島と被って下2島が見えない**：島座標へ絶対配置＋プレートを小型ピル化＝島が見える。
> 41. ✅ **地域未選択なのに「鳥の空域」が選択状態**：事前`.on`ハイライト廃止（タップで決まる）。
> 42. ✅ **星/「x/x OPEN」/代表が紛らわしい・店舗っぽい・不要**：撤去し名前＋最小状態（ベスト/ロック）のみに。
> 43. ✅ **サブステージで機体を変えたい**：dstpickに‹›を追加＝ガレージに戻らずその場で機体変更(`dstVeh`)。
> 44. ✅ **地域→サブステージの階層が伝わる演出**：ズームイン(`dstZoom`)＋プレートpop(`platePop`)。
> 45. ✅ **設定のサウンド/感度ラベルが右ズレ**：`coinHud`が`textAlign='left'`のまま戻さず後続が左寄せ→末尾で中央復帰。
> 46. ✅ **SHOP看板が戻るボタンと窮屈**：中央バナー化して距離を確保。
> 47. ✅ **クリア後つぎへのスプラッシュが短い/古い**：他遷移に合わせ次の地域名を見せる尺ある`go()`に。
> 48. ✅ **遊び方がタイトルからしか開けない**：GARAGE右上に`?`追加＋`helpBack`で開いた元へ戻す。
> 49. ✅ **つばさの落下スプライトが飛行絵よりデカい**：`fall.bodyH 0.66→0.90`で同寸に。撮影モードもフライオーバー除外。
> ＝＝未対応（画像生成=Codex待ち / 設計判断）＝＝
> 50. 〈要素材〉**設定画面の背景が古い**：DOM化 or KV画風背景の生成（質感タスク）。
> 51. 〈設計/要素材〉**地域ごとの特色を立てる**：地域別の敵/ギミックのセット＋同挙動でも別モチーフ画像＋地域別プレイ背景＝[敵設計.md]「★地域別の特色＋幅出し提案」に整理。**着手の主タスク候補**。
> 52. 〈設計〉**高難度でも上昇の爽快感を残す面/区間**：開けた空＋上昇トレイル＋スピードゲート＝[敵設計.md]同節に。
> 53. 〈設計〉**機体差別化（コメット弱い→最高速で小鳥を弾く等）**：[機体設計.md]「★機体差別化の強化 提案」に。次の機体追加とまとめて。
>
> ## ★戦略メモ（2026-06-27 ユーザー発・方向性）
> - **マップ(ステージ配置/コイン譜面)は最終的に“人が手作業で調整”した方がよい節がある**＝そのための **マップ制作用パレットツール**（敵/コイン/ギミックをグリッドに置いて譜面を書き出す簡易エディタ）を作ることになるかも。固定譜面化(`stageRand`)とstageデータ構造は既にあるので、それを編集→書き出すツールは現実的。
> - **ただし優先度はその前に「敵・ギミックの幅出し」**（種類・挙動・組み合わせのバリエーション拡張）。気になりリスト#5（敵バリエ・配置・ステージ増＝Codex起点の案がほしい）と直結。**＝次の主タスク候補はマップツールより“敵/ギミックの幅出し”**。
> - 設計の置き場：敵の幅出しは [敵設計.md]、ツール化は将来 `tools/` に。方法論は knowledge [[ゲームの中身を設計する順序]]（コア→部品→文法→ステージ）。
> - **2026-06-27追補（地域別の特色）**：地域ごとに敵/ギミックのセットを変え、**同じ挙動でも画像モチーフを地域別に差し替え**、背景も地域別に描き直す＝[敵設計.md]「★地域別の特色＋幅出し提案」に実行手順（部品→見た目→配置）を記載。**マップ設計自体（配置）は手作業＋将来ツールなので後**＝敵/ギミックを出すことと画像発注を先に。雷雲は“演出のみ”から**当たり判定のある落雷ギミック**へ昇格候補。**高難度でも上昇の爽快感を残す面**（開けた空＋上昇トレイル＋スピードゲート）も同節。**機体差別化（コメット強化）**は[機体設計.md]提案に。これらは画像生成＝Codex上限回復後 or 挙動だけ先行実装可。
>
> ## ★バトン受け渡しメモ（2026-06-26 Codex→Claude へ復帰／次は誰でも可）
> - **担当交代**：**双方向の交代制**＝自分の上限/切り上げ時は、止まる前にこのHANDOFFを最新化＋1コミットして相手へ戻せるようにする（ルールは [AGENTS.md](AGENTS.md)「上限での双方向バトンパス」）。**push しない**。
> - **2026-06-26 Claude復帰パス**：①Codexの未コミット作業（見た目FB一次対応）を検証して取り込みコミット(4152fbb)。②**ゴールラインを決勝バナー化**(b4d9fd2、`drawGoal()`、確認`?cap&stage=A&alt=388&hold`)。③上の「気になってることリスト」を正本化(a25a5b7)。④**敵スプライトに接地影**(a1ef6b6＝貼っただけ感の解消・カバと統一)。⑤TITLE/SELECT/SHOPを監査=既に良好で変更なし。**コード描画で潰せる視覚AI感はここで打ち止め**＝残りはスプライト再生成(要素材=Codex画像生成)。全部コミット済み・クリーン・未push。
> - **2026-06-26 Claude改善ループ第2弾**：ユーザー指示「言われなくても磨ける所を自分で回せ」＋新気になり6点(リスト#9-14)。コードで潰せる4点を実装＝⑨雲入場フェード/⑩雲海の即時描画/⑪画面遷移トランジション(go())/⑫ふうせん経済の作り直し(coinVal)。各コミット＋validate/smoke/構文/目視OK。#13機体追加と#14収集/強化は設計メモ化(機体設計.md「次に出す機体」/開発計画.md「収集/強化要素の方針」)＝**着手はユーザーの方向確認待ち**。
> - **Codexが戻ったら最優先で渡すもの**：(a)気になりリスト#5＝**鳥/障害物のバリエ・配置・ステージ増をCodex起点の案で**（ユーザー希望）。(b)#13＝**次の機体スプライト生成**（ダッシュ/バブル＝発注文は機体設計.mdに確定済、生成→1行追加で入る）。(c)#1#3の**要素材**＝雲3〜5種・敵/カバスプライトのAI感低減リテイク。(d)#2近い雲ゲートの実機確認はユーザー待ち。
> - **直近の状態**：git 全部コミット済み・クリーン。**敵の手触りはユーザーOK**（「元より相当良くなった」）。コア＝避け/グレイズ/登る＋コイン経済＋ショップ＋機体5種が一周回る。
> - **今回のCodex実装**：**パルサー**追加（膨張収縮する発光球＝縮んだ瞬間に抜けるタイミング敵）＋**D 星脈**追加（パルサー主役の4面）＋**設定画面/セーブ削除UI/ミュート付きWebAudio SE/横ドラッグ感度**追加。ステージ選択に★評価・ベスト高度/タイム表示、**ポーズ画面**、**リザルトのコイン内訳**、**tools/smoke.cjs**も追加。A→B→C→Dの線形解放、`?debug`全解放、`node tools/validate.cjs` / `node tools/smoke.cjs` 対応済み。
> - **2026-06-26 Codex追記（見た目FBの一次対応）**：雲のロード直後フェード・低高度の近景雲抑制・雲の反転/散らし、装備選択の中央大プレビュー＋左右カルーセル化、コイン記号`¢`撤去＋丸コインアイコン化、クリア後のカバ停止を実装。ユーザー指摘の「画像がはりぼて/AI感」「ゴールライン/UI/敵/背景をもっと良くしたい」は未解決の大きな素材制作テーマとして残す。
> - **次の一手（おすすめ順）**：①**実機チューニング**(D星脈のパルサー周期/半径・スウープ/グレイズ・配置頻度/SE音量/感度/ポーズ導線)②**落下表情**(全機体・art重め)③さらに機体④D以降のステージ増築⑤BGM。なお見た目の「AI感」対応は**コード描画で潰せる箇所**（ゴール済✓／次候補=発射台pad・地面のディテール・敵の影/輪郭統一）と**素材生成が要る箇所**（雲3〜5種・敵スプライト刷新）に仕分け済み＝前者はClaude単独で進められる。
> - **開いている懸念（ユーザー指摘）**：**画像/スプライトのサイズ感**が気になる箇所あり（機体やカバ・敵の相対サイズ/画面占有率）。具体箇所はユーザーに実機で確認しつつ、`DRAW_H`/`veh().size`(全1.0固定)/`VEH_ART.bodyH`/`BIRD.drawH` 等で調整。雲は画像種類が少ないので、最終的には`cloud_small/cloud_big`だけでなく遠景薄雲・近景綿雲・縦に流れる雲片の3〜5種へ差し替えたい。
> - **Gemini向けBGM候補**：タイトル/出発導入、装備選択（ガレージ/工房ループ）、ショップ（少しコミカルな買い物ループ）、設定/ポーズ（薄い待機ループ）、Stage A「群れの空」（明るい上昇感）、Stage B「雲海」（柔らかい霧と少し緊張）、Stage C「嵐」（風/雷/パーカッション強め）、Stage D「星脈」（パルス系シンセ/タイミング感）、クリアジングル、墜落/被弾ジングル。SEはコイン、グレイズ、被弾、購入、UI決定/戻る、ロケット噴射、横風、パルサー膨張収縮、雲バンク突入を後で置換。
> - **要実機チューニング**：経済の回り(初機体までの回数)・パータイム係数0.22・コメット/マグ/敵速度の手触り＝係数は即調整可。**`?debug`で全解放+デバッグ**、**`?reset`でセーブ初期化**。
> - **画面デザイン**：本番ビジュアルは外部ツール(Figma等)書き出し待ち＝canvas手描きUIで凝らない。

**新しいチャット/セッションはまずこれを読む。** 設計の本丸は [敵設計.md](敵設計.md)＋[機体設計.md](機体設計.md)＋[開発計画.md](開発計画.md)（★メタ含む全体ロードマップ）＋ knowledge [[ゲームの中身を設計する順序]]。次に [art-bible.md](art-bible.md)／[素材棚卸し.md](素材棚卸し.md)。
> ★方向性が拡大（2026-06-25 FB）：単発プロト→**メタを持つゲーム**（タイトル→装備選択→プレイ→コイン→ショップで機体購入→収集）。**初期はロケットのみ所持**。コビトカバ体格は固定（sizeで大小しない／小型は別キャラ）。こつぶは一旦外す。
> ★**Phase 0+1 実装済み(commit 6942613)**：シーン状態機械(TITLE/SELECT/SHOP/PLAY)＋セーブ(localStorage `hippo_save_v1`)＋タイトル/装備選択/ショップ/結果でコイン加算＋★評価＋機体購入＝**コイン経済が一周回る**。旧DOMバー撤去・canvas hotspotボタン。`?screen=TITLE/SELECT/SHOP`でメニュー撮影可。機体の正体は[機体設計.md]カタログ参照。
> ★**画面デザイン方針(決定)**：UI/画面ビジュアルは**別ツール(Figma等)で作って書き出す**。canvas手描きで凝らない。現状のメニューUI(commit dc3ea9d=立体ボタン/アニメ背景/カード)は**動く土台**＝外部デザインが来たら「描画を画像drawに差し替え」。座標/hotspotの仕組みは流用。
> ★**経済/評価/バランス実装済み(commit 1cceda9)**：コイン=収集/経済・★=腕前(①クリア②速さ=パータイム内③ノーダメ)。bankRun=収集+グレイズ×coinMul+クリア15+ノーダメ8+速さ12。runTime計測でパータイム(高さ×0.22s)＝**速さに価値**。ふうせんcoinMul1.5。体格は全機体1.0固定。価格つばさ80/ふうせん160。係数は[機体設計.md「実装バランス」]に記録＝**実機チューニング前提**。
> ★**Phase 2 進行中**：機体パイプラインをデータ駆動化(VEH_META: price/coinMul/fx/mag、VEH_ART: bodyH/cy/cx)＝機体追加は行追加+スプライトで済む。**機体5種**＝ふつう/つばさ/ふうせん(coin1.5×ハイリスク)/**コメット(速さ特化・price240)**/**マグ(コイン磁石=半径MAG_R引き寄せ・price140)**。こつぶ(3)はbuy:false。**B/Cにも反応脅威(斜めスウープ)展開**＋**パルサー(タイミング敵)**追加、さらに**D 星脈**追加＝全ステージで迫る脅威+グレイズ+一部タイミング判断が機能(`node tools/validate.cjs`確認済み)。drawHippoにcx(横アンカー)追加=左右非対称リグ対応。**`?reset`でセーブ初期化**(テスト用)。**`?debug`でデバッグON+全解放**(P=ON/OFF、I=無敵/H=当たり可視化/U=全解放/C・1-6=機体/J=+150m/K=頂上、?debug=hitで可視化も)。
> ★**次の候補**：①D星脈/パルサー/スウープ/グレイズ/SE音量/感度/ポーズ導線の実機チューニング②落下表情(全機体・art重め)③さらに機体(ホバー=静止/バブル=保険 等[機体設計.md])④D以降のステージ増築⑤BGM。デザイン本番は外部ツール書き出し待ち。**要実機(最重要)**：経済の回り(初機体までの回数)・パータイム達成可否・コメット/マグ/敵速度の手触り＝係数はすぐ調整可。
最終更新: 2026-06-26 Codex。ポーズ画面＋画面スモークテスト追加パス。**push はしていない**（指示があるまでしない）。ローカル実行＝`node`簡易サーバ(`scratchpad/serve.cjs`相当)→ http://localhost:8123/index.html （無ければ `node` でindex.htmlを8123配信）。

## ★最新の現在地（2026-06-25 / 実機FB7＝面白さの軸を①反応+②攻め得に転換）
**実機FBで「鳥の列を抜けて大きい鳥をよける、の繰り返しでつまらない／迫る障害物を反応で避ける遊びがない」と判明。** 上かぶり/フロート(下記)でY軸は埋めたが、遅い降下ゲート＝事前X位置取りの静的パズルに収束していた。面白さの軸を**①反応アクション(迫る脅威)＋②リスク/リターン(攻めて得する)**に転換し、**ステージAを叩き台に実装**(commit予定)。
- ✅ **横スウープ**＝端の高度レーンで予告(赤い危険ライン＋矢印)→高速横断。**押す/離すで高度をずらして反応回避**(ワンボタンが反射に効く)。①の本命。`spawnSwoop`/`swoop`ビート。
- ✅ **ニアミス加点**＝脅威を当てずにかすめて離れると**コイン+速度ご褒美**(全敵に常時)。避けるが"攻めて得する"に変わる(`GRAZE=22`/`popups`)。②の核。
- ✅ **危険ダイブコイン**＝安全な登り線の下へ伸びるコイン列(攻めて落ちて取る)。`spawnDive`/`dive`ビート。
- ✅ **Stage A再構成**＝スウープ主役、ゲートは少なめ・速め(`dmul=1.5`)、タカ/ダイブ/上かぶりをteach→混ぜる→畳み掛け。
- **検証**：構文OK／バリデータ(`scratchpad/validate.cjs`)で**A/B/C全ステージ例外なく頂上まで通る**／`?cap&demo=swoop/dive/over/float`で各メカを目視確認。
- **次の一手**：①**実機で手触り確認**(スウープ速度760/予告タイミングH*0.40/dmul/グレイズ幅)→数値はすぐ調整可。②良ければ**B/Cにも反応脅威を展開**(B/Cはまだ旧構成)。③機体の存在理由をコイン/タイムに。設計の詳細＝[敵設計.md]「方針転換(FB7)」。

## ★前パスの現在地（2026-06-25 / 敵メカ実装パス＝Y軸の空白を埋めた）
**ビジュアル/UI/機体/ステージ区間/コインは実装済み**で遊べる。**この回で[敵設計.md]ロードマップ①〜⑤を実装**し、核心課題だった「離す(落下)が勝ち筋でない＝Y軸の空白」を埋めた。**※下の「いまどこ」以降は古い記述＝末尾の実機FB記録＋[敵設計.md]の「実装状況」が最新。**
- **この回で実装(commit予定)**：
  - ✅ **①上かぶりゲート**(`spawnOverhang`／`over`ビート)＝入口スイフト列＋穴の真上に**ペリカン板**(box当たり)。板は穴より広く**直進(上げ続け)を罰し、離して脇から抜ける**。
  - ✅ **②下脅威フロート**(`spawnFloat`／`move:'float'`／`float`ビート)＝下からせり上がる泡(落とし罰)。横に必ず穴。
  - ✅ **③危険側コイン**＝板の脇/フロートの穴の低い所(攻めると取れる＝落下/横の報酬)。
  - ✅ **④生成バリデータ**＝`scratchpad/validate.cjs`(ブラウザstubでspawn実行)で**全機体×全パターンに横の通路あり**を確認。
  - ✅ **⑤難度カーブ**＝`diff=alt/40`(早熟)→**ステージ進捗全体**に。
  - 🔧 **既存構成のブラッシュアップ**(指摘「下からの話だけでない」)＝**"大きい鳥=デカいスイフト"を単独行から廃止**(役割重複解消・largeは母体専用)／スイフト列の**形状を設計値**で(line/vee/stream)／calm圧縮／各ステージ**teach→混ぜる→畳み掛け**で新メカ配置(A=上かぶり/B=フロート/C=風中の上かぶり)。
- **検証済み**：構文OK／ヘッドレスChromeで上かぶり・フロート・各ステージ描画OK(`?cap&demo=over/float`で新敵を中央固定表示＝確認用に追加)／バリデータで全機体通過OK。
- **次の一手**：①**実機チューニング**(CAP_DY/FLOAT_RISE/頻度/板hw/難度の効き＝要プレイ)②**上かぶりのポケットに薄い下脅威を重ねる**(L2の★最高レバレッジ＝1遭遇で「落とす→落としすぎ前に再点火」)③**機体の存在理由をコイン/タイムに結ぶ**(遅い安全機体が無条件で強い問題・保留中)④ペリカン/フロートの**Codex生成スプライトへ差し替え**(コード描画→`sprites.pelican/float`があれば自動使用)。

## いまどこ（マイルストーン）
**動くスプライト版プロトタイプまで完成。** 最初の「動画で発想 → コンセプト → コアの面白さ → 動くプロト＋一貫素材」が一周つながった状態。
- [index.html](index.html) をブラウザで開けば遊べる（押す＝上昇 / 離す＝落下、鳥を避けて高度を稼ぐ）。
- 主役カバはスプライト化済み（idle/boost/fall/hit）＋コード演出（炎パーティクル・風スピードライン・状態ごとの煽り揺れ）。
- **鳥2種を動き方で差別化**：小=速い斜めジグザグ／大=遅いゆらゆら（種類＝挙動で読みやすく）。
- **HP3制**（即死やめ）：被弾でHP-1＋無敵1.2秒＋点滅＋赤フラッシュ＋右上ハートUI。**地面落下は即死**。
- **接地スタート**：開始は発射台に接地（`START_Y`で浮き解消）。
- **背景/地面を画像素材化**（コードのシェイプは安っぽいので移行）：`ground`(草+土の全面塗り板/切出なし)・`pad`(発射台)・`hills`(遠景ティール)・`cloud_big/small`(雲2層パララックス)。空グラデのみコード。緑キー注意＝緑の草/丘は工夫（地面rawで使う／丘はティール化）。詳細[素材棚卸し.md](素材棚卸し.md)⑤。
- **スター無敵パワーアップ**：`star`取得で`INVINC=5秒`の無敵（`power`=クワッ大口スプライト＋金オーラ＋キラキラ＋HUDタイマー、鳥を弾き飛ばす）。`STAR_GAP`で出現間隔。
- **横移動＋乗り物プロト（検証中）**：タップ＝上昇は維持しつつ「軽い横移動」を任意の第2軸として追加（←→ / A D / ドラッグ、`hipX`）。`VEHICLES`に4種（ふつう/つばさ/ふうせん/こつぶ）。各乗り物は **上がりやすさ(thrust)・重さ(grav)・横の曲がり(steer)・大きさ(size=当たり判定&見た目)** を変える＝必ず「強み＋弱み」のトレードオフ。切替は数字1〜4 or 画面下ボタン。
  - **設計の決定**：移動変数は「既存の数値いじり＝安全」＋「横軸＝任意の第2軸」だけで作る。**燃料ゲージ／風／スコア通貨は当面入れない**（今の"のんびりタップ"を別物に変えるリスクのため、必要時に単独で議論）。"大きさで死にやすさを表現"、変わり種(例:HP減るほど強い"火事場")は**その乗り物だけの個性に閉じる**（全体ルールにしない）。
  - **直近の調整**：`vfall`(落下の最高速)を乗り物ごとに追加＝風船の「ゆっくり止まって急加速で落ちる」不自然さを解消。横移動は**相対ドラッグ＋慣性(`smax`/`sacc`)**にして「指でカバが隠れる」「ワイパーみたいに雑にごしごしで勝てる」を解消。
  - **ペーシング(ゾーン制)＋雲の壁ギミック（検証中）**：単調さ対策として、エンドレスに高度ベースの「ゾーン」(`ZONES`/`ZONE_SEQ`＝calm/normal/dense/bonus/wall)を導入し緩急を作る。新ギミック`雲の壁`=横一列に隙間があり**横移動で抜ける**(鳥と違う避け方／横軸に意味)。スターは**bonusゾーンに集約**（＝「スター置きすぎ」も解消）。画面上の「ZONE 〇〇」は検証用表示。これらは**ステージの部品として流用**する前提。背景の雲パララックス逆転バグも修正済み(登る=雲が下へ)。
  - **未解決（保留中）**：**速さに価値がない＝つばさ(操作性)最強**問題。原因は「ゆっくりでも同じ高さに行ける＝速さに報酬がない」。下からの追跡(ネガティブ)は入れない方針。**ポジティブ/時間記録ベース**で速さに意味を持たせる案（タイムアタック＋スター＝＋時間 等）は出たが**一旦保留**。ジェット系の速い機体を活かすにはいずれ要対応。

## ゲームのコア（確定）
- **1文**：重いコビトカバがロケットで上を目指す縦スクロール。ブーストで昇り、**あえてエンジンを切って落下し**、鳥などをかわして高度を競う。
- **毎秒の駆け引き**：ブースト（高度↑だが上の障害に突っ込む）/ 落とす（避ける・距離を取るが高度↓と下の危険）。
- **設計の決定**：コストは1軸（位置）に絞る＝**燃料は入れない**。横位置固定＝左右向きスプライト不要。
- **キャラ表現はマスト**：状態ごとに体勢/表情が変わる（企画.md参照）。「クワッ大口」は**無敵/パワーアップ専用**に予約。

## 確定した素材・絵柄
- **絵柄**：3Dプリレンダ風の2Dスプライト（マットなマスコット、写実NG）。詳細 [art-bible.md](art-bible.md)。
- **主役マスター＝ `assets/ref/hero_master.png`（cand3：ぽっちゃり・キリッ）でロック済み**。以後すべてこれを `-i` 参照に一貫生成。
- **完成済みスプライト**：`assets/sprites/{idle,boost,fall,hit}.png`（透過・切り出し済み）。元の緑背景版は `assets/states/`。
- 学んだ外れ（art-bibleのルーブリックに反映済み）：**白目＝人間っぽい→黒目ドット維持／細長→チョンキー維持／大口開け＝きもい（無敵専用）／焼き込み炎は身長正規化でカバが小さく見える→炎なしで出し炎はコード**。

## 次の一手（小分け／[素材棚卸し.md](素材棚卸し.md) 順）
1. ~~**鳥スプライト2種**を生成して三角を置換~~ ✅完了（`bird_small`=ティール/橙スイフト・小速い／`bird_large`=紫ペリカン・大遅い。原画は右向き、`draw()`で`b.dir<0`なら左右反転、羽ばたきbobはコード。高度が上がると大鳥比率↑）。
2. ~~**パワーアップ1種＋無敵（クワッ大口）**状態~~ ✅完了（`star`を取ると無敵`INVINC=5秒`：`power`スプライト=クワッ大口、金色オーラ＋キラキラ＋HUDタイマー、無敵中は鳥を弾き飛ばす。`STAR_GAP`間隔で出現）。
3. コイン/星などの収集物（コイン未。スコア/通貨を入れるならここ）。
※「動きの大半はコード（炎/煙/キラキラ/拡大/なびき）」。原画は状態の種類を小分け追加。アニメ連続フレームが要る時だけ"1枚シートでまとめて生成→コード切り出し"。

## いまの本筋：ステージ制（コンセプト違い3本）＋作りこみライン ←イマココ
単調さの本丸＝**決め打ちビート列のステージ**（理屈 knowledge [[敵ウェーブと緩急の設計]]）。難易度別でなく**コンセプト違い3本**（自由選択・各ステージ内で緩急カーブ・**頂上500m到達でクリア＝finite**）。**実装済み（Codexレビュー反映済み／実機チューニングは要）**：
- **枠組み**：`STAGES`＝高度キーの`beats`列。種類＝`calm/trickle/dense/bonus/cloud`＋イベント`swarm/elite(=ダイバー)/wind/obstacle`。`stageTopY()`がビート合計を100m丸め＝各**500mでクリア**→クリア画面→タップで次ステージ。`?cap&stage&veh&alt`で撮影モード(視覚レビュー用)。ランダムゾーン/`loopFrom`は廃止。
- **A 群れの空**：`swarm`＝整列フォーメーション(line/vee/arc・必ず通れる隙間＋最小間隔66・群れ全体が横ゆれ＝飛んでる)。空=青。得意=つばさ。
- **B 雲海**：`cloud`＝上下2枚の厚い霧(視界減fog)＋その中に**ダイバー(タカ)**が潜む。空=グレー。得意=こつぶ。
- **C 嵐**：`wind`＝**横風**（`WIND_ACC=650`でhvelに外力＝機体のsacc/smaxで抵抗が変わる）。`obstacle`=雷雲(静止)。空=暗。得意=ふつう。
- **敵は見た目で挙動が分かる**：スイフト(`bird_small`:速いジグザグ/群れ)・ペリカン(`bird_large`:大ゆったり)・**タカ**(`hawk`:画面内で起動→赤予告リング→ダッシュ離脱)・**雷雲**(`storm`:静止障害物)。`b.sprite`で描画上書き。
- **Codexレビューで是正済み(重要)**：①started前は敵/時間/ダイバー停止 ②ダイバーは画面内で起動＋予告 ③当たりは**スイープ判定**(prevHippoY→hippoY)でフレーム落ちすり抜け防止 ④**飛行中は機体変更ロック**(`canSwapVeh`) ⑤ふうせん縦性能ダウン(thrust0.8/vrise0.82) ⑥風は縦押し→**横押し**へ転換 ⑦頂上超えspawn禁止＋群れ隙間クランプ＋デッドコード除去。
- **要チューニング(実機)**：横風`WIND_ACC`(現650・少し強いかも)/各ビート長/フォーメーション密度・隙間/ダイバー追尾0.85秒/ステージ長500m。

**作りこみライン（並行で力を入れたい）**：背景のリッチ化／**機体ごとのコビトカバ画像**（つばさ=翼、ふうせん=風船…）。ゴール/クリア/全体の"チープさ"も**後でまとめて画像化**したい（ユーザー方針）。
- ★**機体画像の鉄則**：カバ本体は全機体**同じ大きさで描く**。大小は**機体リグ＋コード(`VEHICLES[].size`/`DRAW_H×size`)**で出す＝**絵でカバを大きく/小さく描かない**（二重適用防止）。詳細 [art-bible.md](art-bible.md)。

## このチャット末の引き継ぎ
- ~~**UI/UX修正(HIGH)**：①飛行中は検証ボタン非表示 ②「あとNm」見切れ ③HUD暗スクリム~~ ✅完了(commit 3b09788・ヘッドレスChromeで目視確認済み)。①は`canSwapVeh()`連動で停止時だけ表示／②右マージンW-12／③上部132pxに暗→透スクリム。
- **スクショ撮影＝実証済みコマンド**（Gemini視覚レビュー用。ヘッドレスChrome・要待機budget）：
  ```
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu \
    --hide-scrollbars --window-size=480,860 --run-all-compositor-stages-before-draw \
    --virtual-time-budget=2500 --screenshot="<out>/play_A.png" \
    "file:///c:/Users/mr_ba/Documents/hippo-rocket/index.html?cap&stage=A&veh=1&alt=150"
  ```
  `?cap`なしで撮ると待機画面(ボタン表示状態)。`alt`指定で敵を画面に出す。
- **レビュー体制**：Codex(ロジック/バランス)=稼働OK・反映済み。**Gemini(視覚/UX)=APIキーで疎通確認済み**（`~/.gemini/.env`にキー＋`settings.json`の`security.auth.selectedType="gemini-api-key"`必須／pro混雑時は`-m gemini-2.5-flash`）。**次やる＝上記スクショ→`gemini --skip-trust -m gemini-2.5-flash -p "…@shot.png"`で視覚レビュー**。手順 knowledge [[AI協業の運用]]/[[Gemini CLIの使いどころ]]。クロスチェックは**2人目に1人目の結論を渡さない**。
- ~~**未コミット**：knowledge「Antigravity記述の訂正」~~ ✅コミット済み(knowledge 2fb71b9)。hippo-rocketのコードも全コミット済み。
- **Gemini視覚レビュー＝2周完了・コード対応分はほぼ消化**（A/B/C 3ステージ・gemini-2.5-flash）。総合レビューで「**トンマナは非常に一貫・浮くステージなし／HUDは非常に読みやすい**」評価。コード反映済み：
  1. ~~嵐の背景シェブロン格子が安っぽい~~ ✅**風で寝た斜めストリークへ**(commit a030e90・Geminiで「ノイズ解消・風が伝わる」確認)。
  2. ~~雲が単調・奥行き不足~~ ✅**被写界深度**(b4b3eaf=遠景blur+大気ヘイズ/近景くっきり)。
  3. ~~3ステージのトンマナ不統一~~ ✅実質解決(↑の雲/風統一で「違和感なし」評価)。
  4. ~~のりものブロックが大きい~~ ✅**詳細ステータスは停止時(選択時)だけ表示**(f4bafe6)。
  5. ~~暗背景でカバが埋もれ／あとNmがバーと重なる~~ ✅**バックライト+ドロップシャドウ／ラベルをバー左へ**(5128bfc)。
  - 低優先の残り：ステージ名は黒縁(hudText)で可読だがGeminiは半透明パネルも提案(任意)。
  - **残るアート＝素材生成が必要なもの**：~~機体ごとのカバ画像~~ ✅**4種完成**(commit 99879c3。つばさ=羽/ふうせん=黄色い風船/こつぶ=プロペラ帽/ふつう=既存ロケット。hero_master参照でCodex生成→cutout透過)。★体サイズ二段構え実装＝原画は全機体同体高、drawHippoが`bodyH=DRAW_H[st]*veh().size`で体基準正規化＋`VEH_ART.cy`縦アンカー→「絵で大小を描かずコードのsizeで大小」を担保。噴射炎はふつうのみ。**残りの作りこみ**：地面/発射台/ゴールのリッチ化。
  - **機体アート＝Geminで検証済み**「4機体とも同一キャラ・リグで判別可・体サイズ意図(v2大/v3小/v0v1中)破綻なし」。①風船位置=結果良好 ②~~boost演出~~ ✅追加(commit 83ed486=空気パフ/上昇キラキラ/機体別ポンプ。炎はふつう専用)。残り③idle/boost/fallは機体base共用(hit/powerは共通)＝必要なら機体別の状態差。原画は`assets/states/veh_*.png`(緑背景)で再生成可。
  - ※cap撮影で稀にカバが半透明に写る=被弾iframe点滅をたまたま捉えただけ(バグでない)。cap撮影は`&show=dead`で死亡パネルも確認可。
  - **背景/UIの作り込み(追加)**：~~地面~~ ✅リッチ化(ec7bb28=土の濃淡/地層/石/根/草の小花)・~~遠景の丘~~ ✅多層大気遠近化(cd95d7b)・~~クリア/死亡画面~~ ✅専用カード化(0506234=星3/後光/ピル型ボタン+結果画面はプレイHUD非表示)。いずれもGemini開始画面スキャン(#1地面/#2丘)とレビュー方針に沿う。
  - **次の候補(任意)**：機体別の状態差(idle/boost/fall)・発射台padのリッチ化・スター/ハート等の小物アイコン・キービジュアル/タイトル。
- **実機プレイFB(1回目)反映済み**：①~~風船の体がデカい~~ ✅体は普通サイズ・大風船3つで吊る構図に再生成(ee3741d。size1.5→1.0)。②~~死因でメッセージ未分け~~ ✅衝突=「ぶつかった！」/落下=「墜落…」(die(cause))。③~~フォントがダサい~~ ✅"M PLUS Rounded 1c"丸ポップ体に全面差替。④~~各ステージで遊びが変わらない/つまらない~~ ✅**3ステージ再設計**(28fbf6b・knowledge[[敵ウェーブと緩急の設計]]):A=隊列読み主体(stream斜め帯追加)/B=霧主体+潜むタカ/C=風中に雷雲壁を挟み隙間スレッディング。汎用trickleを削減し主役メカを前面化。
  - **要・再プレイ確認**：ステージ再設計とバランス(隊列の隙間幅/風の強さ/障害壁の間隔)は実機チューニング前提。各ステージ約400m(ビート合計の100m丸め)。
  - **実機FB(2回目)修正済み**：①~~300m付近フリーズ~~ ✅(c090563=頂上手前で全ビート消化しbeatI=配列長→HUDのbeats[beatI].type未定義参照で例外停止。beatIをクランプ)。②~~鳥の種類違いが重複~~ ✅(同コミット=短いイベントビートを先読みが飛び越しnextSpawnが後戻り→重ね置き。event枝をnextSpawn=Math.max(nextSpawn,beatEndY)で前進のみに)。両方ユーザー実機で解消確認。
- **実機FB(3回目)＝設計の深掘り**。方針はユーザー選択「**敵の哲学だけで深く(新動詞なし)／スターは廃止して収集要素**」。knowledge[[ゲームの面白さの型]]([[敵ウェーブと緩急の設計]])準拠。反映済み(35f3c5b/b016182)：
  - ~~スター無敵が支配的(ギミック無視でゴール直行)~~ ✅**廃止→コイン収集**(スコア・避ける以外の正の動機)。invinc/power/aura全撤去。code描画コイン+HUD枚数+クリア画面。
  - ~~機体が没個性・操作感が変わらない~~ ✅**数値差を増幅**(つばさ=上昇激弱+超滑空+横俊敏/こつぶ=落ちやすい極小)。
  - ~~ふうせんが普通と変わらない・縦長で役割不明~~ ✅**hitW1.8の横長当たり判定+横広3風船に描き直し**=「上の障害に当たりやすい高リスク/ほぼ落ちない」役割を明確化(体は普通サイズ維持)。
  - ~~鷹の存在理由不明・脅威でない~~ ✅**再定義**=上部で狙い定め(縦の赤ビーム予告)→まっすぐ急降下し"列"を塞ぐ=同じ横位置で上げ続けを罰し横回避を迫る。
  - ~~距離が短すぎる~~ ✅**A700/B900/C700m**へ延長。
  - **★次パス(未着手・最重要)＝敵の哲学を本格化**：今は「天井ハザード(上げすぎ罰)/床ハザード(下げすぎ罰)/動く隙間」が未実装で、まだ「小鳥よけ＋鷹＋風/壁」止まり。ブースト⇄落下⇄横の3択を毎瞬迫る配置(上に居座る敵/下から迫る静止トゲ帯/横に動く隙間)を入れるのが核。これでvel<0(落下)に意味を与える。
  - **次パス2＝機体の表情差分**(idle/boost/fall顔)：今もbase共用でfall顔が無い。各機体のfall(パニック)顔を素材生成 or 顔オーバーレイ。art-bibleの2レイヤー方針。
  - 撮影モード: `?cap&stage=&veh=&alt=` + `&show=dead&cause=fall/hit`。ローカルは`node scratchpad/serve.cjs`→localhost:8123。
- **実機FB(4回目)＝操作ストレス/敵哲学**。方針=ユーザー選択「**通り過ぎる群れに全面作り直し＋被弾ノックバック**」。反映済み(fd2dc02)：
  - ~~速い縦横無尽な敵＋急ブレーキ不可で同じ鳥に何度も当たる~~ ✅**ジグザグ(diag)廃止→sweep(一方向に横切って画面外へ)**=読める・再衝突しない。鳥速度↓密度↓隙間↑。被弾ノックバック(鳥と反対へ＋少し上)で再衝突ループを断つ。
  - ~~同じ画像で別挙動~~ ✅**1種1挙動1見た目**(小鳥=速い小群れ/大鳥=遅い壁/タカ=予告急降下)。formも一方向へ流れる。
- **実機FB(5回目)＝敵が散漫/横スポーンが見える/縦軸の陣形がいい。AIレビュー回して自律で進める指示。**
  - ~~鳥が画面中央に湧く~~ ✅**Codexレビューで根本特定・修正**(0506234系→aheadSpawnY): イベントビートがnextSpawnでなく古いbeatStartYで生成→可視域に湧いていた。全生成経路を画面上端+SPAWN_TOP_PAD(140)より上にクランプ。
  - ~~横スポーン/横に消える/縦横無尽~~ ✅**「登って抜ける陣形ゲート」に作り直し**(spawnBird=横一列/斜め/V字に1〜2の穴、altを保持し上から来る、隊形ごと横流れ=穴も動く)。ジグザグ廃止。
  - ~~陣形が散漫で穴が読めない/雨線がノイズ~~ ✅**Gemini視覚レビューで可読化**(横一列基調・縦揺れ控えめ・穴広め・密度緩急・drawWind薄く)。Gemini「dense=穴が一目で分かる」確認。
  - ~~ステージ=1つの体験(区間設計)~~ ✅**区間構成に再構成**(導入静→波急→コイン回廊(敵なしご褒美nobird)→休符静→…→クライマックス→ゴール前静)。A600/B約700/C約600m。
  - ~~速い/同じ鳥に再衝突~~ ✅鳥速度↓・被弾ノックバック・1種1挙動1見た目。
  - 自己QA済み(Codexはタイムアウトで未完): コインはクランプ済nextSpawn使用で画面外/陣形は常に通れる穴あり/4機体とも登坂可/不可能な壁なし。
  - **★まだ残る大物=「よける以外がない」の本丸＝敵の哲学(天井ハザード:上げすぎ罰/床トゲ帯:下げすぎ罰/動く隙間)**。これでvel<0(落下)に意味。今は陣形ゲートを「登って横で避ける」が主で、落下の決断がまだ弱い。鷹(予告急降下)は入れたが種類が少ない。
  - 次候補2＝機体のfall表情(顔差分・素材生成。Codex画像生成は5分超でタイムアウトしがち=要分割/リトライ)。
  - **設計の残論点**：速さの価値づけ・コアの深み等は従来どおり保留。ローカル確認は`node`簡易サーバ(scratchpad/serve.cjs)でhttp://localhost:8123。
- **保留の設計論点**：速さの価値づけ(タイムアタック/時間記録)・チェックポイント+1kmゴール案・各ステージ配置の"癖"の深掘り・コアの深み(かわす行為自体)。

## 素材パイプライン（検証済みの叩き方）
- **生成（Codex内蔵画像生成・ChatGPT枠＝無料）**。マスターを参照に：
  ```
  printf '%s' "<英語プロンプト+chroma green bg+No text>" | codex exec --skip-git-repo-check \
    -C "c:/Users/mr_ba/Documents/hippo-rocket" -s workspace-write \
    -i "c:/Users/mr_ba/Documents/hippo-rocket/assets/ref/hero_master.png"
  ```
  - 生成先 `~/.codex/generated_images/<id>/*.png`。**最新フォルダから `cp` でrepoへ取り込むのは呼び出し側（Claude）でやる**。
  - SKILL.md読込で `windows sandbox: spawn setup refresh` エラーが出るが**無害**（生成は通る）。
- **透過抜き＋切り出し**：`node tools/cutout.cjs`（クロマキー緑→透過、bbox切り出し。sharpは hippo_fall の node_modules を絶対パス参照）。出力 `assets/sprites/`。
- **★ルール：画像を生成したら必ずプレビューを開く**。`assets/.../_preview.html`（`<img>`グリッド）を作って `Start-Process` で開き、人が並べて選ぶ。AI自身も Read で目視。
- **身長正規化の注意**：表示高さは index.html の `DRAW_H` で調整。焼き込み要素（炎等）があると枠が伸びてカバが小さく見える＝**炎なしで出して炎はコード**が鉄則。

## チューニング箇所
- [index.html](index.html) 冒頭の定数：`GRAV/THRUST/V_RISE/V_FALL`（手触り）、`DRAW_H`（表示サイズ）、`HIPPO_R`（当たり）。

## 関連ナレッジ（汎用手法・knowledge vault）
このゲーム固有でない手法は `c:\Users\mr_ba\Documents\knowledge` にある：
- ゲーム発想の進め方（目的→決断→操作→検証→可視化）
- ゲームの面白さの型（駆け引きの型と実例・審問リスト）
- 動画駆動開発（コンセプトを動画で可視化）
- Codexで画像生成する／visual-asset-kit スキル（素材の一貫量産）

## git
- hippo-rocket・knowledge とも全コミット済み・**未push**。push はユーザー指示時のみ。

> ## Codex追記: 2026-06-28 ルート外報酬とテンプレート型コインマーク
> **実装済み・未push。** 基本コインは理想通過線上に置く方針を維持し、外側の遊びとして大コイン、ハート回復アイテム、テンプレート型の矢印コインマークを追加。
> - `index.html`: `hearts` を追加。大コインは `kind:'big'` で5枚分、ハートはHPが減っていれば回復、満タンなら3枚分コイン。外側報酬は `sideRewardSpot()` / `sideRewardFits()` で空白に置ける時だけ配置。
> - コインマークは `COIN_MARKS` に完成済みテンプレートとして定義し、`addCoinMark()` で全ピースが置ける時だけ一括配置。ハート形コインは縮尺上読みづらかったため自動生成では使わず、現状は矢印のみ使用。ハートは回復アイテム本体に任せる。
> - `tools/placement_contact_sheet.cjs`: 表示用データに `kind/mark` を残し、マーク部品を雑密集/孤立/敵間/折れ線の過剰判定から除外。
> - 追加整形として `straightenWavyRouteSegments()` を入れ、短い通過線のガタつきを軽く直線化。ただしB/527mやD/D2の敵密度・孤立ラベルは別件として残るため、次に人間目視で潰す候補。
> - 検証: `node tools/validate.cjs` OK、`node tools/visual_overlap_audit.cjs` 全ステージ `hard=0 warn=0`、`node tools/placement_contact_sheet.cjs` 更新、`node tools/smoke.cjs` OK。`.shots/placement-contact-sheet-*-annotated.png` 再生成済み。
