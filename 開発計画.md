# 開発計画（hippo-rocket ロードマップ）

単発プレイのプロトから、**メタ（収集・経済）を持つゲーム**へ広げるための開発計画。
設計の前提は [機体設計.md](機体設計.md)／[敵設計.md](敵設計.md)。手順論は knowledge [[ゲームの中身を設計する順序]]。

## ★ゴール像（コアの外側にメタを乗せる）
```
[タイトル] →(押す)→ [装備選択(所持機体)] →(出撃)→ [プレイ(コア=避け/グレイズ/登る)]
     ↑                                                        │
     │                                              クリア/墜落 ↓
 [ショップ] ←(コインで購入)← [リザルト(コイン加算・記録)] ──────┘
```
- **初期は「ロケット」だけ所持**。コインで機体を買って増やす＝コレクションが「もう一回」の動機（[[ゲームの面白さの型]]：コアは尖らせ、外にメタを薄く）。
- フックは**コア側**（避け・グレイズの気持ちよさ）。メタは動機づけ。

## ★設計順序の原則
1. **骨組み(土台)を先に**：画面遷移＋セーブ＋経済の「配管」を、中身が薄くても先に通す。
2. **縦に薄く**：各フェーズで「タイトル→プレイ→コイン→1機購入」のように**一周遊べる単位**で出す。
3. **中身は後から流す**：機体・敵・ステージは"追加の仕組み"を作ってから量産（先に12機作らない）。
4. **手で遊ぶ前に機械検証**（[[ゲームの自動検証]]：構文→stub検査→ヘッドレス撮影→手触り）。

## ★進捗
- ✅ **Phase 0 + Phase 1 実装済み**(commit 6942613)：状態機械(TITLE/SELECT/SHOP/PLAY)＋セーブ(localStorage)＋タイトル/装備選択/ショップ/結果でコイン加算＋★評価＋機体購入。初期=ロケットのみ/ステージA解放。`?screen=`でメニュー撮影可。**速さの価値づけ(スコア/タイム)はPhase1で未実装＝Phase2へ持ち越し**。
- ⏭ **Phase 2 進行中**（機体追加パイプライン・新機体/新エネミー・ステージ増築＆解放）＋随時Phase 3の表情差分。新エネミーは斜めダイブ/フロート/上かぶりに加え、**パルサー(縮む瞬間に抜けるタイミング敵)**を追加済み。ステージは**D 星脈**を追加し、A→B→C→Dの線形解放へ拡張。

## ★フェーズ計画

### Phase 0 — 土台（シーン状態機械＋セーブ）※最優先・中身なしでOK
今は1本のループに死亡/クリアのオーバーレイを足した構造。メタを乗せるには**画面(シーン)の状態機械**が要る。
- **シーン**：`TITLE / SELECT / SHOP / PLAY / RESULT`。`scene`変数で update/draw/入力を分岐。
- **セーブ**(`localStorage`)：`{ coins, owned:[0], selected:0, best:{A,B,C}, unlocked:['A'], settings }`。全メタの土台。
- **done基準**：タイトル→(プレイは既存)→リザルト→タイトル、が遷移し、コインとbestが**再起動後も残る**。

### Phase 1 — 経済ループMVP（一周回る）
- **タイトル画面**：ロゴ＋「タップでスタート」＋総コイン表示（最小でOK、アートは後）。
- **装備選択**：所持機体を並べて選ぶ（初期は**ロケットのみ**）。性能の簡易ステータス表示（既存drawStats流用）。
- **リザルト**：そのランのコインを**総コインに加算**・best更新（既存クリア/死亡画面を流用）。
- **ショップ**：機体一覧＋価格、コインが足りれば購入→`owned`に追加。**まず2機だけ並べる**（例：つばさ／ふうせん＝既存アート流用、初期非所持に変更）。
- **速さの価値づけ**（ここで一緒に）：リザルトに**簡易スコア/評価**（到達m＋コイン＋ノーヒット＋タイム）を入れ、★1〜3。→ 登坂速度・無被弾に意味が出て、機体選択に理由が増える。
- **done基準**：ロケットで稼ぐ→ショップでつばさ購入→装備して再プレイ、が成立。

### Phase 2 — コンテンツ拡充の「仕組み」
- **機体追加パイプライン**：`VEHICLES`に`price/owned/artKey`を持たせデータ駆動に。新機体＝物理1行＋アート1枚＋価格で足せる形へ。
- **新機体**（[機体設計.md]カタログから順次。モチーフ＝コビトカバの可愛さを増長する**ちょっとシュール**な乗り物）。
- **新エネミー/障害物**（[敵設計.md]の4軸を埋める形で。各1動詞・被らせない）。
- **ステージ増築＋ステージ選択/解放**：ステージをデータ追加で増やせる形に。`unlocked`で解放制御（前ステージクリアで次解放）。
- **done基準**：機体/敵/ステージが「データ追加＋アート」で増やせ、解放が回る。

### Phase 3 — 演出・手触り（ジューシーさ）
- **表情差分**：各機体の**落下(パニック)顔**ほか（ふつうの落下顔が可愛い→全機体に）。アート2レイヤー方針。
- **タイトル/キービジュアル**、画面トランジション、購入演出。
- **サウンド/BGM**（手触りに最重要。未着手）。
- 全体チューニング（敵速度・コイン経済バランス・難度カーブ）。

#### 最新FBからの見た目/音の磨き込み（2026-06-26）
- **雲**：低高度では遠景だけに抑え、近景雲は登ってから出す。コード側の一次対応済み（ロード後フェード、近景抑制、反転/散らし）。次は素材として、遠景薄雲・中景雲海・近景綿雲・縦流れ雲片を3〜5種用意。
- **UI**：装備選択は中央大プレビュー＋左右カルーセルへ一次対応済み。残りはボタン/モーダル/ゴールラインを外部デザインまたは画像素材で作り直し、canvas側は配置だけに寄せる。
- **敵/オブジェクト絵**：鳥・障害物・ゴールラインが「AI素材を置いただけ」に見えやすい。次の素材制作では、同じライティング/輪郭/影のルールで敵・雲・ゴール・UIを一括更新する。
- **BGM候補**：タイトル/出撃導入、装備選択（ガレージ/工房）、ショップ、設定/ポーズ、Stage A「群れの空」、Stage B「雲海」、Stage C「嵐」、Stage D「星脈」、クリアジングル、墜落/被弾ジングル。
- **SE置換候補**：コイン、グレイズ、被弾、購入、UI決定/戻る、ロケット噴射、横風、パルサー膨張収縮、雲バンク突入。

## ★各システムの設計メモ（何が要るか）
- **セーブ**：`localStorage`単一キー(JSON)。バージョン番号を持たせ将来のマイグレーションに備える。**設定に「セーブ削除」**を用意済み（2段階確認で誤消去を防ぐ）。
- **ショップ経済の調整**：1ラン平均コイン × 想定プレイ回数 ＝ 機体価格、の釣り合い。**最初の1機は安く**（早期の達成感）。経済機体(ふうせん/マグ)は単価でなく**獲得量**を上げる。
- **ステージ選択**：現状はリスト型。解放は線形（A→B→C→D）で、解放済みステージに★評価・ベスト高度/タイムを表示。
- **装備選択**：機体ごとに「得意/弱点」を1行コピーで提示（選ぶ理由を伝える）。
- **スコア/評価**：到達m・コイン・ノーヒット・タイムの合成→★。**replay動機＋速さの価値**を同時に解決。

## ★抜けていそうな要素（追加提案）
- **サウンド/BGM/SE**（短いWebAudio SE＋ミュート設定は実装済み。BGMは未）。
- **スコア/評価システム**：実装済み。リザルトに収集/ニアミス/機体倍率/クリア/ノーダメ/速さのコイン内訳も表示。
- **オンボーディング**（操作チュートリアル。ステージAのteach beatで一部代替済み）。
- **ポーズ**：実装済み（プレイ中に停止→つづける/リトライ/そうびへ）。**設定**（セーブ削除・ミュート・横ドラッグ感度は実装済み）。
- **自動検証**：`tools/validate.cjs`＝敵/ステージ生成、`tools/smoke.cjs`＝主要画面描画(TITLE/SELECT/SHOP/SETTINGS/PLAY/PAUSE/結果)の無例外チェック。
- **コイン経済バランス**（数値設計。Phase1で仮置き→継続調整）。
- **実績/デイリー/ガチャ**（メタの拡張。後期。やりすぎ注意）。
- **構造**：単一HTMLが肥大化中。当面は単一＋セクション整理で進め、辛くなったら分割を再検討。

## ★画面デザインの方針（2026-06-25 決定）
- **UI/画面ビジュアルは別ツール(Figma等)で作って書き出す**。canvasに手描きで凝らない（安っぽくなる・遅い）。
- canvas側は「**書き出した素材(背景/ボタン/パネル/アイコン)を配置して描画するだけ**」に寄せる。現状の手描きUI(タイトル/装備/ショップ/結果)は**動く土台**として残し、外部デザインが来たら差し替える。
- レイアウト座標・タップ領域(hotspot)の仕組みは流用可。差し替えは「描画を画像drawに置換」が中心。

## ★アート/モチーフ方針
- 機体＝**コビトカバの可愛さを増長する、ちょっとシュールな乗り物**（例：浮き輪/アヒルボート/プロペラ帽/シャボン玉/打ち上げ花火/ジェットダック…）。体格は固定、リグで差。
- 別キャラ（ハムスター等）はサイズ/質感ニッチ＆コレクション映えの目玉。

## ★直近の着手単位（推奨）
**Phase 0 → Phase 1 を一気通貫で「縦に薄く」**：状態機械＋セーブ＋（タイトル/選択/リザルト加算/ショップで1機）。
これで「コインで機体を買って装備して挑む」基本ループが回り、以後は機体/敵/ステージを**足すだけ**になる。

## 関連
- 機体の差分哲学/カタログ → [機体設計.md]
- 敵・障害・ステージ → [敵設計.md]
- 設計手順/検証 → [[ゲームの中身を設計する順序]]／[[ゲームの自動検証]]／[[敵ウェーブと緩急の設計]]
