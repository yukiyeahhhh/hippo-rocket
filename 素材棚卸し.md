# hippo-rocket 素材棚卸し

「画像で作る素材」と「コードで足す動き/UI」を分けて管理する。
原則（[[Codexで画像生成する]] / visual-asset-kit）：**確実に使うコアだけ先に作り、投機的な在庫は脚本が要求した時に足す**（先に作ると膨張する）。
そして**"生き生きした動き"の大半はコードで出せる**（原画は少なくてよい）。

## ① 主役カバ：状態スプライト（マスター紐づけ）
| 状態 | 有無 | 原画フレーム | コードで足す動き |
|---|---|---|---|
| idle | ✅ | 1 | 上下bob・呼吸スケール |
| boost | ✅ | 1 | 体の小刻み揺れ＋**噴射炎はパーティクル(code)** |
| fall | ✅ | 1 | 回転ゆらぎ・手足ばたつきのtween |
| hit | ✅ | 1 | 着地の潰れ・点滅 |
| nearmiss（汗ビクッ） | ⬜ | 0〜1 | まずfall流用＋汗パーティクルで代用可 |
| powerup/無敵（クワッ大口） | ✅ | 1 | `power`(hero_master参照で一貫)。キラキラ/光輪/赤フラッシュはcode。本家ムーデンの見せ場 |
| 巨大化 | ⬜ | 0〜1 | **拡大はcode**。表情は流用可 |

→ **横向き/左右リーンは不要**（プロトは横位置固定で縦制御のみ）＝その分スプライトが要らない。

## ①' 装備ごとの状態スプライト＝不足画像の棚卸し（2026-06-26 確定）
**方針**：コビトカバ路線継続・こつぶ廃止・**装備ごとに画像を書き出す**（コード合成オーバーレイは採らない＝ユーザー判断）。
**現状**：ふつう＝idle/boost/fall/hit/power の5状態を個別所持✅。**つばさ/ふうせん/コメット/マグ＝各1枚(`veh_*`)を idle/boost/fall に共用**＝下記が欠け。落下時も“澄ました顔”のまま＝**パニックが無い**（ユーザー指摘）。
| 装備 | idle | boost(噴射/前傾) | **fall(パニック顔・装備つき)** | hit(被弾) |
|---|---|---|---|---|
| ふつう(ロケット) | ✅ | ✅ | ✅ | ✅ |
| つばさ | ✅(=base共用) | ⬜ 欲しい | **✅(2026-06-26生成)** | ⬜(汎用hit流用中) |
| ふうせん | ✅ | ⬜ | **✅** | ⬜ |
| コメット | ✅ | ⬜ | **✅** | ⬜ |
| マグ | ✅ | ⬜ | **✅** | ⬜ |
- ~~優先1＝各装備の fall(パニック)~~ ✅**完了**：`fall.png`を主参照にトーン統一（困り顔＋汗＋手上げ）＋リグ差し替えでCodex生成→cutout→drawHippoが`veh_<id>_fall`を自動使用（中央基準描画）。
- **次の優先＝boost差分**（各装備の踏ん張り姿勢）。その次＝hit差分（装備つき被弾）。同じ`fall.png`方式で量産可（参照を`boost.png`/`hit.png`に替えるだけ）。
- **生成手順**：`assets/ref/hero_master.png`＋該当`veh_*`を`-i`参照、緑背景・No text。`assets/states/veh_<id>_fall.png`→`tools/cutout.cjs`→`assets/sprites/`。ローダ配列とdrawHippoの状態分岐（idle/boost/fallのbase上書き→状態別sprite選択）に追加。
- 実装側の受け：drawHippoは今`useVeh`時に`va.base`1枚を idle/boost/fall 共用。**状態別スプライトがあれば`veh_<id>_<state>`を優先**する分岐に拡張すればよい（無ければbaseにフォールバック＝段階導入できる）。
- ※`power`(クワッ大口/無敵)はスター無敵廃止で**現状デッド資産**。新規装備のpowerは作らない。
- ※`veh_kotsubu.png`はこつぶ廃止で不要（削除可・当面放置）。

## ② 障害・敵（画像）
| 種類 | 有無 | 備考 |
|---|---|---|
| 鳥（小・速い） | ✅ | `bird_small`（ティール/橙のスイフト）。横切る基本敵。原画右向き→dirで反転 |
| 鳥（大・ゆっくり） | ✅ | `bird_large`（紫のペリカン）。圧のある敵。高度が上がると出現比↑ |
| 上かぶり板 | ✅ | `pelican`。横長の重い板鳥。drawPelicanでパタパタ＋ホバリング |
| 急襲タカ | ✅ | `hawk`。赤予告→急降下。描画時に羽ばたき/bobを強める |
| 雷雲 | ✅ | `storm`。静止障害物＋落雷。マゼンタキーで透過 |
| フロート泡/クラゲ | ✅ | `float`。下からせり上がる敵。drawFloatでふわふわ拍動 |
| 静的障害（雲塊/柱など） | ⬜ | 追加が必要になったら |

## ③ アイテム（要・画像 小サイズ）
| 種類 | 有無 |
|---|---|
| コイン/星（収集） | ⬜ | スター無敵は実装済み(下記)。コインは未 |
| パワーアップ（スター無敵/グライダー/マグネット 等のアイコン絵） | ✅(スター) | `star`。取得で無敵5秒(鳥を弾く)。他は未 |

## ④ エフェクト（基本コード／一部小スプライト）
噴射炎・煙・衝突の星・汗・キラキラ＝**パーティクル/コード**で。原画にしない。

## ⑤ 背景（画像素材＋コード合成に移行済み）
コードのシェイプは安っぽいので**部品を画像化**した。空グラデだけ code。
| 素材 | 有無 | 備考 |
|---|---|---|
| 空グラデ | ✅(code) | 高度で色が深くなる |
| 雲 cloud_big / cloud_small | ✅ | 透過。2層パララックスで使い回し、上空ほど薄れる |
| 遠景の丘 hills | ✅ | 透過。**ティール/青緑**（緑キーと分離するため＆遠景の霞）。接地線の上に置く |
| 地面 ground | ✅ | 草+土の**全面塗り板**（緑の草が緑キーで消えるので**切り出さない**＝raw） |
| 発射台 pad | ✅ | 透過。カバの真下、天面が接地線に来るよう配置 |
※緑チョマキーの注意：緑を含む素材(草/丘)は緑背景だと抜けるので、丘はティール化／地面は切り出さずrawで使う。

## ⑥ UI（画像にしない＝CSS/Canvas）
高度・スコア・ベスト・ボタン枠＝コード。

---
## 作る順（小分け）
1. **今ある4状態をプロトに配線**（＋コードでbob/揺れ/炎/潰れ）＝少ない原画でも"動いてる"を確認 ✅
2. **鳥スプライト2種**（グレイボックス三角を置換）＝小=bird_small/大=bird_large。配線＋dir反転＋羽ばたきbob済み ✅
3. 次：パワーアップ1種＋無敵（クワッ）状態 ←イマココ
4. 以降、脚本が要求したぶんだけ追加
