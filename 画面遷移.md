# 画面遷移図（hippo-rocket）

各画面が「何のための場所か（1つの決定）」を先に定義する。これを描かずに各画面を作ると、概念と場所がズレる（例：行き先選択をガレージの中でやってしまう）。
方法論は knowledge [[画面フローを先に描く]]。

## フロー図

```
            ┌──────────┐
            │ SPLASH   │  開発ブランド表示（短く自動遷移）
            └────┬─────┘
                 │
            ┌────▼─────┐
            │  TITLE   │  ブランド/発射前のワクワク
            │ (発射前)  │  決定＝「出発する」だけ
            └────┬─────┘
        出発する  │                 遊び方/? → HOWTO / ⚙ → SETTINGS
            ┌────▼──────┐
            │  GARAGE    │  ★機体(装備)を選ぶ
            │ 機体選び    │  場所＝雲上ガレージ（格納庫）
            │            │  ‹›で機体・ステータス確認
            └─┬───┬────┘
       ショップ │   │ 行き先をえらぶ        ⚙ → SETTINGS
        ┌──────▼┐  └─▼───────────────┐
        │  SHOP  │   │  DESTINATION       │  ★行き先を2階層で選ぶ
        │ 屋台   │   │  地域→サブステージ   │  場所＝雲に開けた発射デッキ/空
        │       │   │ (発射デッキ・空)      │  地域＝浮かぶ島/領域、サブステージ＝その中の挑戦
        └──┬────┘   └───┬───────────────┘
       もどる│  ←戻り先     │ これで出発
        (GARAGE)          ▼
                      ┌────────┐
                      │  PLAY   │  本編（canvas）
                      └───┬────┘
              クリア / 失敗  │
                      ┌─────▼──────┐
                      │  RESULT     │  もう一回 → PLAY
                      │  結果        │  装備を変える → GARAGE
                      │             │  つぎの空(clear) → DESTINATION
                      └────────────┘

  SETTINGS … 各画面の ⚙ から開く小パネル。閉じる＝元の画面へ戻る（行き止まりにしない）。
  HOWTO … TITLEから開く遊び方ページ。もどる＝TITLE。

  ── 戻り(back)動線 ──────────────────
   → TITLE へ戻れる：GARAGE「もどる」／ PLAY一時停止「やめる」／ RESULT「タイトルへ」
   → GARAGE へ戻れる：DESTINATION「もどる」／ SHOP「もどる」／ RESULT「装備をかえる」
   → DESTINATION へ戻れる：RESULT「つぎの空」(clear時)
   SETTINGS「閉じる」＝開いた元の画面へ（TITLE/GARAGE/DESTINATIONのいずれか）
```

## 各画面の責務（1画面＝1つの決定＝1つの場所）

| 画面 | 1つの決定（役割） | 場所/シーン | 主要操作 | 出口 |
|---|---|---|---|---|
| SPLASH | ブランドを見るか | 開発ブランド表示 | 自動遷移 / タップでスキップ | → TITLE |
| TITLE | 始めるか | 発射前の空（KV v5） | 出発する / 遊び方 / ? / ⚙ | → GARAGE / HOWTO / SETTINGS |
| HOWTO | 操作を確認するか | 発射前の空（KV v5） | 出発する / もどる | → GARAGE / TITLE |
| GARAGE | **どの機体で飛ぶか** | 雲上ガレージ（格納庫・展示台） | ‹›機体送り・ステータス確認・ショップ・⚙ | → DESTINATION / SHOP / SETTINGS ／ **もどる→TITLE** |
| DESTINATION | **どの地域→どのサブステージへ飛ぶか** | 雲に開けた発射デッキ／空（島で行き先） | 地域タップ→サブステージ選択・★/ベスト確認・これで出発 | → PLAY / 戻る=地域一覧→GARAGE |
| SHOP | 何を買うか | 雲上の屋台（店主＝下隅固定） | 商品スクロール・購入・もどる | → GARAGE |
| SETTINGS | 設定変更 | 軽い雲＋半透明パネル | 各設定・閉じる | → 元の画面 |
| PLAY | 飛ぶ | 本編ステージ（canvas） | 長押し上昇 ほか | → RESULT |
| RESULT | 次どうするか | 成功＝星/失敗＝雲クッション | もう一回・装備変更・つぎの空 | → PLAY / GARAGE / DESTINATION |

## 原則（崩さない）
- **1画面＝1つの決定**。決定が2つ以上あるなら画面を分ける（旧SELECTは「機体＋行き先」を混ぜていた＝分割）。
- **画面は「場所」として考える**。その決定が起きるのに相応しい場所か？（行き先選び＝外/空。装備選び＝ガレージ。）
- **戻り先を必ず定義**。どの画面からも前へ戻れる。行き止まり/孤児画面を作らない。
- この図を**先に描いてから**各画面の背景シーン・構図・UIを作る（[[UIの構図は先人の型から起こす]] はこの後）。

## 画面ごとの要件（認識合わせ）

フロー確定の次に、各画面に「何を載せるか（必須要素）」を合意してから、構図・背景生成・実装に入る。方法論＝knowledge [[画面フローを先に描く]]「フローの後：画面ごとに要件を認識合わせ」。

### SPLASH（実装済み）
- 必須：開発ブランド名。短く出して自動でTITLEへ。タップ/Enterでスキップ可能。将来のゲーム共通部品として、ブランド名はコード内定数で差し替えられるようにする。

### TITLE（実装済み）
- 必須：ロゴ／飛ぶカバ(KV)／「出発する」CTA／遊び方／?／⚙。**コイン・操作説明本文は載せない**（ブランドと開始だけ）。遊び方は `HOWTO` へ分ける。

### HOWTO＝遊び方（実装済み）
- 必須：押すと上昇／離すと落下／横移動／ニアミス／コイン収集の5項目。画像/スプライトを使い、文章より見て分かることを優先。TITLEから開き、もどるでTITLEへ戻る／出発するでGARAGEへ進む。プレイ前に必要な最低限だけにし、ショップやステージ詳細は載せない。

### GARAGE＝機体選び（実装済み）
- 必須：**機体カルーセル**（展示台の上に機体・‹›・左右に隣の機体プレビュー）／機体名＋一言／**ステータス（加速・軽さ・旋回）**／コイン(右上)／⚙／**「行き先をえらぶ」CTA**／もどる(→TITLE)／**ショップ導線**。
- 載せない：**行き先/ステージは出さない**（DESTINATIONの責務）。未所持機体はロック表示でSHOPへ誘導。
- 背景：生成済み `assets/ui/select_bg_v1.png`（雲上ガレージ）。

### DESTINATION＝行き先選び（2階層・実装済み）
- 必須：**地域一覧 → サブステージ一覧**の2階層。地域＝鳥の空域/雲海/嵐の空/星脈。サブステージ＝各地域内の挑戦。現状は各地域2本ずつ（A/A-2, B/B-2, C/C-2, D/D-2）をぶら下げる。
- 地域一覧：地域名／地域の雰囲気／その地域の開放済み本数／代表ステージの★/ベストを表示。地域カードを押すと同じDESTINATION内でズームした扱いのサブステージ一覧へ移る。
- サブステージ一覧：各ステージに **★スコア・ベスト高度・解放状態**（未解放はロック）／選択中ハイライト＋「どんな空か」一言／**「これで出発」CTA**／地域一覧へ戻る／⚙。
- 見せ方：地域を**「空に浮かぶ島/領域」**、サブステージを**その領域の中の挑戦**として空間的に見せる（ただのリスト羅列にしない＝“どこへ飛ぶか”が直感で分かる）。
- 採用方針：**選んだ機体の小表示＋推奨機体バッジは出す**／**コインは出さない**（買い物判断ではないため）／順序は **GARAGE→DESTINATION**（機体を決めてから行き先を締める）。
- 背景：生成済み `assets/ui/destination_bg_v2.png`（雲に開けた発射デッキ／空に浮かぶ島）。v1は細密ファンタジー寄りでコビトカバ/鳥スプライトと絵柄差が強かったため不採用。

### SHOP（V3実装済み）
- 必須：店主(下隅固定)＋吹き出し／商品棚（全幅スクロール・カードは絵→名前→一言→価格→購入）／通貨(右上)／もどる(→GARAGE)。

### SETTINGS
- 必須：音/感度/セーブ削除等／閉じる(→元の画面)。軽い雲＋半透明パネルで十分。

### PLAY / RESULT
- PLAY：本編(canvas)。一時停止＝つづける/リトライ/やめる(→TITLE)。
- RESULT：成功＝星/高度/コイン、失敗＝原因＋雲クッション／**もう一回**(→PLAY)／**装備をかえる**(→GARAGE)／**つぎの空**(clear時→DESTINATION)／タイトルへ。

## 実装メモ（現状との差分）
- 旧 `scene='SELECT'`（機体＋ステージ）→ **`GARAGE`（機体のみ）** と **`DESTINATION`（地域→サブステージ）** に分割済み。
- GARAGE背景＝生成済み `assets/ui/select_bg_v1.png`（雲上ガレージ）。DESTINATION背景＝生成済み `assets/ui/destination_bg_v2.png`。
- 順序は TITLE → GARAGE → DESTINATION → PLAY を既定とする（行き先を後にして「これで出発」で締める）。
