# 2026-07-02 設計監査フォローアップ

## 状態

- Fable5/Sonnetの初回監査は、共有ナレッジ上では「29項目中×9、L0に集中」と記録されていた。
- 後続確認で、初回監査表の復元版 `docs/audits/2026-07-02_design-audit-initial.md` が見つかった。修正前ベースラインとして保存する。
- 本文書は、ユーザーが共有した監査抜粋、初回監査表、現在の `index.html` / 設計文書を照合した要対応メモ。
- Codexで再監査表を `docs/audits/2026-07-02_design-audit-rerun.md` に保存した。

## 共有された監査抜粋

- 「押しすぎ」を罰する敵が機能していない。ペリカンが実質スイフトと同じ避け方になっている。
- ハートが設計と正反対。設計では「危険に置いてリスク/リターン」と書いているが、実装は敵のいない安全地帯にだけ置かれている。
- ボーナス回廊が「欲張りの決断」を作っていない。敵なしでコインだけなので目的不在。
- シャッター/ステップゲートが既存敵と役割被り。
- 支配的戦略の懸念3件が実機未検証のまま。

## 現物照合

| 対象 | 状態 | 判断 |
|---|---|---|
| ペリカン上かぶり | 中央塞ぎへ修正済み | 押しっぱなし直進は罰しやすくなったが、実機手触りは要確認 |
| ハート | 安全地帯の単独生成を撤去し、危険ダイブ側の低頻度報酬へ移動 | 設計方向へ寄せたが、頻度と危険度は要確認 |
| ボーナス回廊 | `spawnBonusCoins()` は敵なしの線/弧コイン | 休符としては有効だが、欲張りの決断ではない |
| ステップ穴帯 | 穴を階段状に追って上昇する | スイフト隊列との差分はあるが、求める行動が「X合わせ」に寄りやすい |
| シャッター | 中央開口が開く瞬間に抜ける | パルサーとの差分は「中央アパーチャ」だが、待つ/抜ける役割被りは要実機確認 |

## 対応方針

1. **監査レポートを再生成して保存する**（対応済み）
   - `07-設計監査スキル.md` の形式で、現在の設計文書と `index.html` を再監査する。
   - 次回から要約だけでなく、表そのものを `docs/audits/` に残す。

2. **L0の穴を先に直す**（一次対応済み）
   - ペリカンを「押しっぱなしでは損をする」部品に戻す。
   - ただし全機体が通れる通路保証は維持する。ふうせんが詰む縦回避専用にはしない。

3. **報酬の目的を分ける**
   - ボーナス回廊は「休符/気持ちよさ」として残すなら、その名前と役割を明記する。
   - 欲張り報酬は、別ビートとして危険側コイン/ハート/大コインに寄せる。

4. **ハートをいったん保留または危険側へ移す**（一次対応済み）
   - 安全地帯ハートは、監査上は目的不在。
   - 回復を残すなら、ダイブ/上かぶり/フロート周辺の「取りに行くリスク」がある位置へ置く。

5. **支配的戦略は自動プレイテスト要件へ接続する**
   - 文書監査では「未検証」までしか言えない。
   - `08-自動プレイテスト要件定義.md` の方針どおり、簡易方策での検出対象にする。

## バックドラフト案との関係

Gemini案の「固有バックドラフト」は、落下を能動化する方向として筋が良い。ただし、上の監査指摘を放置したまま全機体へ入れると、既存のL0不備を新システムで覆うだけになる。

推奨順は、ペリカン/報酬のL0修正 → `ふつう` と `コメット` の2機体だけでバックドラフト試作 → 再監査。
