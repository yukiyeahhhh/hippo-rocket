@AGENTS.md

# Claude Code 向け補足

- 上の AGENTS.md がこのリポジトリの正本（Claude/Codex共通）。「Codexに渡す」等の記述は文脈で読み替える。
- セッション開始時は**まず HANDOFF.md**（現在地の正本）。次に必要なら 企画.md → art-bible.md → 素材棚卸し.md。


## 見た目・体験のハードルール（視覚承認ゲート・2026-07-04 hippo-recipe事故から全ゲームリポ共通）
- 見た目に触れる変更（素材の配線・タイトル/背景/キャラ差し替え・トンマナ変更）は、①人のロック、または②実装者と別のAIによる独立視覚レビュー（結論を渡さない）を通過するまで「未承認」。未承認のまま「完成」と報告しない。
- 自己レビューで判定できるのは配線ミス・表示崩れまで。見た目・体験の合否は判定できない。
- 無人運転（放置指示）中に人間ゲートへ当たったら、採用せず提案で止める（複数案＋コンタクトシート）。「推奨で進めて」は見た目・体験の採用には効かない。
- codex exec への素材生成依頼には「repoのファイルを編集・保存するな」を明記。取り込みは呼び出し側で。
- 見た目に触れるコミットの前は視覚QA（knowledge「視覚QAを回す」＝文字沈み/はみ出し/フォールバック混入/画像事故/状態網羅）を通す。見た目のワーカー発注はチェックリストを完了条件に含める。
